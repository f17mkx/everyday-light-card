# CHANGELOG

## 2026-05-13 - v1.0.2 / P15.6-r69 (PA-0020): R352 instant mindmap re-anchor on parallel-inline compact↔expanded toggle (no more 1-2 frame lag)

- 🐛 fix(P15.6-r69 / v1.0.2) **R352 mindmap arm + dot re-anchors instantly when an embedded parallel-inline child toggles compact↔expanded (PA-0020)**: Stefan-Quote PA-0020: "when expanding and collapsing the paralell slider for hall boxes the mindmap node, dot and arm for Hall takes some time to find its place (the mindmap adjustment isnt instant)". Pre-r69 the parent group-layout-expanded only re-measured embedded children's icon-Y positions in `updated()` (after a Lit re-render), and that re-render was only triggered by either (a) the per-col ResizeObserver on `.member-col` HEIGHT (which fires async 1-2 frames after layout commit), or (b) `visible-leaf-count-change` events (which the parallel-inline toggle doesn't emit since the leaf-count stays at 1). Result: when boxes flipped compact (370 px tall) ↔ expanded (368 px), the parent didn't re-measure for 1-2 frames → mindmap arm visibly lagged behind. r69 introduces a new `nested-layout-change` event (CustomEvent, bubbles+composed): everyday-light-card fires it from `onParallelMindmapPick` immediately after flipping `_parallelInlineCompactRuntime`. Parent group-layout-expanded listens at the host level and calls `_measureNestedIconYs()` synchronously + on next microtask + on next animation frame (triple-tap to catch every layout-commit timing). Mindmap re-anchors in the SAME frame as the child's re-render — visually instant.

- 📚 docs(v1.0.2) **VERSION bump 1.0.1 → 1.0.2**: package.json + VERSION constant. Patch release for R352. v1.0.1 was published to the WRONG repo (superpro-light-card) by mistake — corrected publish target for v1.0.2 onward is `f17mkx/everyday-light-card` (Stefan-Direktive).

Build: clean. Tests: 156/156 green. Bundle path unchanged. v1.0.1 → v1.0.2: backwards compatible.

## 2026-05-12 - v1.0.1 / P15.6-r68 (PA-0019): R349 sibling-coordination via slider-width-need events (uniform widths across nested branches), R350 parallel-slider-row min-gap 14 px (no more 0-gap touching sliders), R351 embedded parallel-inline padding dropped (vertical alignment with siblings)

- 🐛 fix(P15.6-r68 / v1.0.1) **R349 sibling-coordination: uniform slider-widths across nested apartment branches (PA-0019)**: Stefan-Quote PA-0019 (config5 scenario): "when the space is a bit tight and you expand spots for instance then all sliders become smaller width, except the sliders in the same group as spots (door and boxes in the case of spots)". Pre-r68 each card's `_computedSliderWidthOverride` was scoped to that card's own `.member-cols` overflow detection. When spots-expansion increased hall's leaf-count (1 → 5), back's fr-template recomputed (kitchen 2-fr, bathroom 3-fr, hall 7-fr) → kitchen + bathroom cols got narrower → their resolveOverflow shrank their sliders to 48 px. Hall's col got WIDER → no overflow → hall's sliders stayed 60 px. Visual: hall's leaves (door + boxes + spots-tree) at 60 px next to bathroom's leaves at 48 px — Stefan flagged as inconsistent. r68 introduces sibling-coordination: each `<everyday-group-layout-expanded>` fires a `slider-width-need` CustomEvent (bubbles + composed) with `{ entity, width }` whenever its resolveOverflow runs. The event reports the SMALLEST width that fits THIS card's content (= the override if overflow, else the baseline 60). Every ancestor card listens for these events (in `connectedCallback`), tracks per-child needs in `_childSliderNeeds: Map<string, number>`, and computes a global min `_childMinSliderNeed`. The min is composed with the card's own `_computedSliderWidthOverride` at render time so the inline `--everyday-slider-width` written on `.layout` is `min(own, descendant-min)`. Cascade ensures all descendants see the unified min. Stable: bottleneck child's min always fits everywhere by definition; after one rAF the tree converges. Verified empirically — bathroom shrinks to 48 → fires event → back applies 48 globally → hall's sliders re-render at 48 → fires event 48 → no further changes (already converged).

- 🐛 fix(P15.6-r68 / v1.0.1) **R350 parallel-slider-row min-gap so 2-slider expansions don't visually butt together (PA-0019)**: Stefan-Quote PA-0019 (config6): "when expanded and when there is enough space the paralell sliders are 0px apart (no gap between them)". Pre-r68 `.parallel-slider-row` used `gap: 0; justify-content: space-around;` — the 0-px floor meant that in narrow containers (e.g., 2 sliders × 60 px in a 120-px col), space-around had no slack to distribute and the sliders touched edge-to-edge. r68 sets `gap: 14px` so the floor is always visible; space-around still adds extra distribution when the container is wider, so wide-container layouts naturally space the sliders out further. Mindmap-arms still anchor at slider-centers because space-around centers each item in its slot regardless of the gap.

- 🐛 fix(P15.6-r68 / v1.0.1) **R351 embedded parallel-inline padding dropped (PA-0019)**: Stefan-Quote PA-0019 (config6): "the boxes slider isnt quite right aligned with the others sliders. the boxes slider is slightly lower than the others". Pre-r68 `.parallel-mindmap-layout` had `padding: 24px` and `.parallel-compact-layout` had `padding: 8px`. These paddings are appropriate for STANDALONE single-light cards but push the embedded slider DOWN by 8-24 px relative to sibling sliders (regular leaf-tiles or compact-group tiles) which have no such padding. r68 adds `:host([embedded]) .parallel-mindmap-layout, :host([embedded]) .parallel-compact-layout { padding: 0 }` so embedded parallel-inline cards align top-to-top with their sibling cards. Mirror of the existing `:host([embedded]) .layout { padding: 0 }` rule for `<everyday-group-layout-expanded>` (group-layout-expanded.styles.ts:118-122) — same fix-pattern, same rationale.

- 📚 docs(v1.0.1) **VERSION bump 1.0.0 → 1.0.1**: package.json + src/everyday-light-card.ts VERSION constant. First post-1.0.0 patch release; bundles all r66/r67/r68 fixes (PA-0017/PA-0018/PA-0019). Public-repo release via `/publish` skill — separate sync pipeline.

Build: clean (rollup, TypeScript, css-comments). Tests: 156/156 green. Bundle path unchanged. v1.0.0 → v1.0.1: backwards compatible — no config changes required.

## 2026-05-12 - P15.6-r67 (PA-0018): R345 group-icon-offset reverted 41→46 (correct for actual .lbl line-height), R346 group-expanded slider default 220→270 (matches single-light), R347 expand_in_place wired into parallel-inline compact↔expanded toggle, R348 standalone GROUP_DOT_Y formula off-by-6 fixed via Chrome MCP empirical alignment

- 🐛 fix(P15.6-r67) **R348 standalone GROUP_DOT_Y formula was 6 px off — fixed via Chrome MCP empirical measurement (PA-0018)**: Stefan-Quote PA-0018: "Main icon and mindmap-dot are still not visually aligned. If anything it is worse now". /deep-dive via Chrome MCP getBoundingClientRect on /ed-slider/main: standalone group cards (non-nested, non-embedded) take the **hardcoded SVG-coords path** at `group-layout-expanded.ts:1644`: `GROUP_DOT_Y = isTop ? 30 : (288 + sliderDelta)` → `groupYOverride` is set on `<everyday-mindmap-path>`, which **overrides** the `group-icon-offset` attribute that PA-0017 R343 had been mucking with. So R343/R345's `group-icon-offset` edits had ZERO effect on /ed-slider/main — the responsive-coords path is only taken when `embedded || hasNestedMembers`. Empirical measurement: dot-center 6 px BELOW icon-center, constant across slider heights (icon and dot moved in lock-step but with 6 px gap). `.tile.group` actual height = 69 px (.ic 46 + gap 4 + .lbl ~19, NOT 14 as the original tuning assumed) → .ic-CENTER = 46 from .topology-bottom; old formula placed dot at 40 from bottom (288 + 100 sliderDelta = 388 → 428 - 388 = 40). Fix: change base 288 → 282 so dot lands at 46 from .topology-bottom = exact icon-center. Confirmed with Chrome MCP post-fix: iconCenterY = 471, dotCenterY = 471, **diff = 0 px** ✓.

- 🐛 fix(P15.6-r67) **R345 group-icon-offset reverted 41→46 (PA-0017 R343 was wrong)**: Stefan-Quote PA-0018: "if anything it is worse now". PA-0017's R343 changed the responsive-coords-path `group-icon-offset` from 46→41 based on the mindmap-path TSDoc's stale assumption that `.lbl` is 14 px. Empirical measurement on /ed-slider/main: `.lbl` is actually 19 px (Roboto/HA default font line-height), so `.tile.group` height is 69 (not 64), and `.ic-CENTER` from .tile.group-bottom is 46 (not 41). Reverted to 46 + updated mindmap-path TSDoc with the empirical numbers. Note: this only affects the responsive-coords path (embedded / nested-member group cards); standalone groups go through R348 above.

- ✨ feat(P15.6-r67) **R346 group-expanded slider default 220 → 270 (matches single-light) (PA-0018)**: Stefan-Quote PA-0018: "die slider hier müssen auch alle die volle länge haben wie es auch bei dieser config der fall ist [single-light]". The R184-era 220 px default for group-expanded sliders was a "shorter than host to leave room for mindmap" compromise that's no longer wanted — Stefan now wants visual parity with single-light cards (which have shipped at 270 since PA-0002). Bumped the fallback in `effectiveSliderHeight` resolution from 220 → 270. `full_length_sliders: true` flag still resolves to 270 (kept as backwards-compat no-op alias). `expand_in_place + compact + _compactExpanded` chain unchanged (still 150 px so the card stays at the pre-expand height). Verified live: 6 sliders on /ed-slider/main render at 270 px each (was 220 pre-r67).

- ✨ feat(P15.6-r67) **R347 `expand_in_place` now wired into the parallel-inline compact↔expanded toggle (PA-0018)**: Stefan-Quote PA-0018: "die option 'Fixed card size (stay same height when expanding)' gibt es zwar im visuellen editor, aber sie hat keinen effekt bei dieser config [parallel_sliders.layout: compact + group.expand_in_place: true] — es muss aber da auch ganz genau so funktoinieren, wie bei den anderen!". Pre-r67 the `expand_in_place` flag was only consumed by `everyday-group-layout-expanded` (non-parallel compact↔expanded transitions). The parallel-inline path in `everyday-light-card.ts` ignored it — toggling the mindmap picker slot (R338) flipped compact→expanded but the card grew taller. r67 wires it: when `cfg.group?.expand_in_place === true` AND the config opted into compact (`parallel_sliders.layout: compact`) AND the user has currently toggled to expanded (via mindmap pick), shrink the slider by 68 px to absorb the extra overhead from the multi-axis layout (icons + chrome diff between compact and expanded). Math: compact overhead ≈ 95 px (pad 8+8 + icon 46 + gap 8 + caption 25), expanded overhead ≈ 163 px (pad 24+24 + mindmap-area 90 + caption 25), diff = 68 px. Verified live on /ed-slider/teessst with config4-equivalent card: initial compact card 370 px (1 slider 270 px), toggle expanded card 368 px (2 sliders 202 px each — shrunk by 68), toggle compact again 370 px. **2 px height drift** (sub-pixel rounding) ✓.

Build: clean. Tests: 156/156 green. Bundle path unchanged. Verified end-to-end via Chrome MCP on /ed-slider/main + /ed-slider/teessst (deployed cache-buster `0.9.0-r348-1778620594077`).

## 2026-05-12 - P15.6-r66 (PA-0017): R340 visual editor unbroken (flatten expandables), R341 3-icon picker = equilateral triangle, R342 inverted-mindmap glyph for parallel-expanded, R343 parallel-icon Y-align robust, R344 parallel-compact icon mindmap-node chrome

- 🐛 fix(P15.6-r66) **R340 visual editor inputs no longer silently dropped (PA-0017)**: Stefan-Quote PA-0017: "der visuelle editor ist irgendwie broken. wenn ich da irgendetwas einstellen will, werde ich an der eingabe gehindert. ich kann keine werte eingeben, keine checkboxen checken, keinen text schreiben etc.. wenn ich eine option aus dem dropdown auswählen will wird diese nicht übernommen". Root cause: r65's PA-0002 R6 reorganized the visual editor into 6 expandable sections via HA's `type: 'expandable'` schema items but every section was missing the `flatten: true` flag. HA's `<ha-form-expandable>` defaults to NESTING child values under the section's `name` key (per the home-assistant/frontend source) — so typing "parallel" into `default_view_mode` emitted `{ display_section: { default_view_mode: 'parallel' } }` to `_valueChanged`. Our `_valueChanged` reads `flat.default_view_mode` (top level) → undefined → ELSE branch deletes the value. Result: every keystroke, every checkbox toggle, every dropdown pick was thrown away on the next render. r66 adds `flatten: true` to every expandable section (display_section, group_section, slider_section, parallel_section, picker_section, gestures_section) — now expandables spread their children's values at the parent level on read AND write, matching our `_flatData()` shape. Type definition in the editor file extended with optional `flatten?: boolean` field with TSDoc explaining the trap. No runtime behavior change for users with config that already roundtripped before r65 (those values were never bound to expandables).

- 🐛 fix(P15.6-r66) **R341 3-icon mode-picker now renders as equilateral triangle, not L-shape (PA-0017)**: Stefan-Quote PA-0017: "die icons des mode pickers müssen, wenn er 3 items enthält immer als gleichschenkliges dreieck um das icon verteilt sein. aktuell ist ein icon oben, eins unten und eins rechts. bei dieser config (config3.txt) ist die anordnung der mode picker icons korrekt". config3.txt is `parallel-inline` (3 slots: mindmap/saved/wheel) which falls through to the uniform-distribute branch in `getPickerAngleMap` → angles [270, 30, 150] = top, lower-right, lower-left = equilateral triangle. But on `/ed-slider/main` (light.main rendered as `group-expanded` with 3 slots: parallel/saved/wheel after r63i dropped `cycle`), the picker hit the `slots.length <= 4` fixed-cardinal branch → angles [270, 0, 90] = top, right, bottom = L-shape. r66 narrows the condition from `<= 4` to `=== 4` so any 3-slot picker falls through to the uniform-distribute path regardless of variant. The 4-slot diamond (group-expanded with effects, or sticky+effects) keeps its fixed-cardinal layout for muscle-memory continuity. Tests: `picker-geometry.test.ts` updated — old `group-expanded (modern, no omitTemp): same 4-diamond as member` test split into separate 3-slot triangle + 4-slot diamond cases.

- ✨ feat(P15.6-r66) **R342 mindmap glyph in parallel-inline picker swaps to inverted (COLLAPSE) when expanded (PA-0017)**: Stefan-Quote PA-0017: "bei R3 soll beim zusammenklappen von den paralellen slidern die umgekehrte mindmap im mode picker gezeigt werden (anstatt dem 'aufklapp'-icon (also die umgedrehte mindmap soll angezeigt werden)". The mindmap slot in the parallel-inline picker controls compact↔expanded toggle (added in r65 R338). Pre-r66 the icon always showed the upright MINDMAP glyph (3 children at top, host at bottom = "expand to multi-axis"). r66 adds a new `parallelExpanded` `@property` to `<everyday-mode-picker>` (boolean, attribute `parallel-expanded`); when true AND the slot is `mindmap` AND variant is `parallel-inline`, `_renderIcon` returns the COLLAPSE glyph (inverted mindmap-trizack: host at top, three children converging from the bottom — already defined for the `'collapse'` slot in r65 R2a). New helper `_renderRawIcon(iconPath)` extracted so the swap doesn't recurse through `_renderIcon`'s mode-aware dispatch. Plumbed via new `parallelExpandedProvider?: () => boolean` in `PickerControllerOptions`; everyday-light-card's host wires it to read the same effective-compact resolution chain (runtime override beats config) used by `onParallelMindmapPick`. Provider pattern (vs static prop) so reactive `_parallelInlineCompactRuntime` toggles update the picker glyph without re-instantiating the controller.

- 🐛 fix(P15.6-r66) **R343 .parallel-mindmap-icon + .parallel-picker pinned to top:60 + translate(-50%,-50%) for sub-pixel-stable centering (PA-0017)**: Stefan-Quote PA-0017: "der 'halo' um das main icon ist nicht ganz aligned mit dem icon selbst. mit halo meine ich das ding um das icon, wenn man die Press-drag-select gesture ausführt. aktuell ist der halo etwas höher als das icon. das icon von Main ist auch etwas zu hoch. also das icon ist nicht richtig mit dem mindmap dot aligned. daher kommt wahrscheinlich auch das misalignement des mode picker halos". Pre-r66 `.parallel-mindmap-icon` used `bottom: 17px; transform: translateX(-50%)` and `.parallel-picker` used `bottom: 30px; left: 50%` — both calculated to land at y=60 from the .parallel-mindmap-area top (matching the SVG `groupYOverride=60`). Math was correct on paper but bottom-anchored positioning is sensitive to sub-pixel rounding in some HA themes when `--mdc-icon-size` is overridden, causing the icon to drift 1-3 px in real renders. r66 switches both to `top: 60px; transform: translate(-50%, -50%)` so the visual centers land deterministically at the same Y as the SVG dot. Halo (= picker dots orbit) and icon now align tight on every theme.

- 🐛 fix(P15.6-r66) **R343 group-icon-offset corrected from 46 → 41 in topology-bg context (PA-0017)**: Same Stefan-Quote as above — Stefan reported the main icon misaligned with the mindmap dot on `/ed-slider/main` (group-expanded view). Math: `.tile.group` height ≈ 64 px (.ic 46 + gap 4 + .lbl 14), `.ic-CENTER` at 41 px from `.tile.group-bottom`. The `<everyday-mindmap-path>` was being passed `group-icon-offset="46"` so the SVG dot landed at 46 px from .topology-bottom = 5 px ABOVE the actual icon center. r66 changes the offset to 41 (matching the value already documented in the mindmap-path TSDoc as the correct topology-bg context value: "group-row sits at the topology bottom with its .ic (46 px) centered 41 px above the topology bottom (= half-icon 23 + 4 gap + 14 label)"). The earlier 46 was a typo / mis-edit that escaped review.

- 🎨 style(P15.6-r66) **R344 .parallel-compact-icon now wears the mindmap-node chrome (PA-0017)**: Stefan-Quote PA-0017: "bei R3 fehlt auch der mindmap dot und der runde hintergrund um das icon (damit der slider genau so aussieht wie die anderen)". The compact variant of `parallel_sliders.layout` (added in r63n R327) renders a bare `<ha-state-icon class="parallel-compact-icon">` without the mindmap-node visual identity (dark fill + gold border ring) that every other compact-collapsed group icon uses (`.tile.group.compact-target .ic` and `.caption .single-icon`). r66 adds `width: 46px; height: 46px; border-radius: 50%; background: var(--mindmap-dot-fill); border: 2.6px solid var(--mindmap-group-stroke); box-sizing: border-box; display: inline-flex; align-items: center; justify-content: center` so it matches the other group/single icons. Picker overlay positioning updated from `bottom: 47px` to `bottom: 56px` to track the new 46 px icon height (was 28 px before; half-icon 23 vs 14 → +9 px offset).

Build: clean (rollup, TypeScript, css-comments). Tests: 156/156 green (added 2 new tests for the 3-slot triangle + 4-slot diamond in `picker-geometry.test.ts`). Bundle path unchanged. R340/R341/R342/R343/R344 ship together under P15.6-r66.

## 2026-05-12 - P15.6-r65 (PA-0002): default slider 270, parallel-compact icon below slider, effects-list opt-in, sticky expansion + collapse picker slot, visual-editor refactor with "Fixed card size"

- ✨ feat(P15.6-r65) **PA-0002 R4 default slider height 220 → 270 px**: Stefan-Quote PA-0002: "the default slider length should be 270px". Bumped the universal default in five places that previously hard-coded 220 (single-light path, compact-host, parallel-inline default, expanded-popup `full_length:true` branch, expanded-popup `full_length_sliders:true` override in `_renderTopologyPopup`). Also fixed a latent bug surfaced by Stefan's `config2.txt` repro (`group.layout: compact, slider.height: 270`): the compact view never threaded `this.sliderHeight` into its own `--everyday-slider-height` CSS var — `_renderCompact()` now applies an inline style on `.layout.compact` so user `slider.height` config takes effect (previously the hard-coded 220 in `group-layout-expanded.styles.ts:.layout.compact .compact-slider-el` won). That same rule changed to `var(--everyday-slider-height, 270px)` so the cascade is now: explicit `slider.height` → 270 default. `full_length_sliders: true` / `parallel_sliders.full_length: true` (previously the 260 px opt-in) now resolve to 270 too — equal to the new default, kept as backwards-compat no-op aliases. Expanded member-sliders for `full_length_sliders: false` (the deliberate "shorter to leave room for mindmap" branch) keep 220 by design. `expand_in_place` base shifted 220 → 270 so the post-shrink children are 270-120=150 px (was 100 px) — more usable shortening.

- 🐛 fix(P15.6-r65) **PA-0002 R3 parallel-compact icon now renders BELOW the slider**: Stefan-Quote PA-0002: "the icon for hall boxes is shown on top of the slider. it needs to be below the slider". The `parallel_sliders.layout: compact` variant in `everyday-light-card.ts` rendered the icon as the FIRST child of `.parallel-compact-layout` (above the slider-row in column-flex). DOM order flipped: `.parallel-slider-row` first, then `<ha-state-icon>`, then `.single-picker` overlay, then `.caption`. `.single-picker` CSS moved from `top: 22px` (anchored at old icon position) to `bottom: 47px` (anchored at new icon position: caption ~25 + 8 gap + half-icon 14). Mirrors the `.parallel-mindmap-area` layout pattern where the icon sits below the sliders.

- ✨ feat(P15.6-r65) **PA-0002 R1 effects-list slot is now opt-in in the long-press mode-picker**: Stefan-Quote PA-0002: "by default the effects-list should be disabled in the mode-picker". Pre-r65: any entity with a non-empty `effect_list` attribute auto-got an effects slot in the picker (most modern color bulbs do, so every card showed it). Added `effects_picker.in_picker?: boolean` config flag (default `false`). New `effectsInPickerProvider?: () => boolean` in `PickerControllerOptions` and new `effectsInPicker` `@property` on `everyday-group-layout-expanded` carry the flag through the cascade. `hasEffects = effectList.length > 0 && opts.effectsInPickerProvider?.() === true` at all three call sites in `picker-controller.ts` (long-press-move hit-test, mode-pick dot anchor, picker render) plus the matching call in `group-layout-expanded.ts._applyPickerMode`. Existing `effects_picker.editable` / `effects_picker.source` knobs continue to work independently of the new `in_picker` flag.

- ✨ feat(P15.6-r65) **PA-0002 R2a sticky expansion + 'Collapse' picker slot**: Stefan-Quote PA-0002: "when this card is expanded then only tapping outside of the card should close the expansion (or lets make it so, that the expansion state is remembered. that is better. ... If it is not the mode picker needs to have an option to collapse the node)". New `group.expansion_sticky?: boolean` config flag (default `false`). When `true`: (a) `_compactExpanded` is mirrored to `localStorage` under the per-entity key `everyday-light-card:expanded:${groupEntityId}` (read on connectedCallback + first-updated, written on every state change), (b) outside-click NO LONGER collapses the inline-expanded view (handler short-circuits when `expansionSticky` is true), (c) a NEW `'collapse'` slot is added to the `group-expanded` picker variant — picking it sets `_compactExpanded = false`. New `'collapse'` member added to `PickerMode` type (mode-picker.ts + the local duplicate in group-layout-expanded.ts), new `hasCollapseProvider` in `PickerControllerOptions`, new `hasCollapse` `@property` on `<everyday-mode-picker>`, new `hasCollapse` opt in `PickerLayoutOpts`. New `COLLAPSE` glyph in mode-picker (inverted mindmap-trizack: host at top, three children converging from the bottom). picker-geometry's group-expanded fixed-cardinal-angle path now also tracks `collapse` at 180° (with effects/mindmap at the same slot — only one of them is active at a time in practice). Slot count > 4 falls through to pentagon distribution. Storage helpers `_expansionStorageKey()`, `_restoreExpansionFromStorage()`, `_persistExpansionToStorage()` are defensive: they swallow `localStorage` exceptions (private browsing / quota) and silently fall back to legacy session-only behaviour.

- ✨ feat(P15.6-r65) **PA-0002 R5 + R6 visual editor refactor with sections + new knobs**: Stefan-Quote PA-0002: "please implement the change and also add the option to the visual editor ... also please tidy up the visual editior a bit (make it better). also look in conductor.db and backlog if you find any of my previous comments regarding the visual editor and make it better". Editor schema (`src/editor/everyday-light-card-editor.ts`) reorganized into 6 expandable sections via HA's native `type: 'expandable'` schema items: **Display** (view mode), **Group** (layout, manual_members, expand_in_place "Fixed card size", expansion_sticky), **Slider** (orientation, style, height), **Parallel sliders** (modes, full_length), **Mode picker** (effects_in_picker), **Gestures** (double-tap action). New fields exposed: `expand_in_place` labeled "Fixed card size (stay same height when expanding)" — Stefan's R5 ask; `expansion_sticky` labeled "Remember expansion state (sticky, requires manual collapse)" — Stefan's R2a; `effects_in_picker` labeled "Show effects in mode-picker (long-press radial menu)" — Stefan's R1. BACKLOG carry-over: R283 'Remove effects-list view-mode from visual editor' done (option removed from `default_view_mode` dropdown; YAML key still supported in the runtime for back-compat); R282 'Add expand_group to double-tap-action picker' done (added to gestures dropdown); R285 'Pick which modes to cycle with double-tap' still backlogged. Added `color-wheel` and `saved-colors` as new selectable view-modes in the dropdown (they were already implemented in the runtime, hidden from the editor pre-r65). Existing top-level fields (entity, name, icon) kept above sections so first-paint stays immediately actionable.

Build: clean (rollup, TypeScript). Tests: 154/154 green. Bundle path unchanged. Backwards compatible: all new flags default to false; pre-r65 configs render identically. Stefan-Live-Verify-Pending — dashboard render-pipeline broken at `_curView` init (carried over from prior session), so no automated visual diff possible from the agent side.

## 2026-05-12 - P15.6-r64+ (PA-0014): color-wheel defaults 8x24, conditional white-center, standalone color-wheel + saved-colors display modes, smooth-wheel CSS fix

- ✨ feat(P15.6-r64+) **R335 color-wheel defaults bumped from 21 hues x 6 rings to 24 hues x 8 rings (PA-0014 R1)**: Stefan-Quote PA-0014: "default, with 8 steps and 24 hues, 12 und 5 steps". `EverydayColorWheel.hues` default 21 -> 24, `rings` default 6 -> 8. The new default reads cleaner on a 320 px wheel area (less segment crowding) while keeping more hue resolution. Host fallbacks in `group-layout-expanded.ts` and `picker-controller.ts` follow the same defaults via the existing `??` pattern, so explicit-undefined props land on 8x24 too.

- ✨ feat(P15.6-r64+) **R336 white-center disc auto-suppressed when `rings <= 5` (PA-0014 R1)**: Stefan-Quote PA-0014: "bei 5 oder weniger Steps soll der innere Kreis nicht weiss sein, sondern immer auch als optionen zum auswaehlen bestehen". For chunky variants (`saturation_rings: 5` or fewer), the inner ring is now a normal selectable arc segment at the natural low-saturation HSV value, not a white-only disc. Implementation: `render()` computes `effectiveWhiteCenter = this.whiteCenter && RINGS > 5`. Explicit `white-center` attribute still respected for `rings > 5`. Smooth variant unchanged (keeps its radial white-bias gradient).

- 🐛 fix(P15.6-r64+) **R337 smooth color wheel no longer distorted into a 320 x 360 rectangle (PA-0014 R6)**: Stefan-Quote PA-0014: "smooth color wheel ist irgendwie broken / distorted / gequetscht (also so schmaler als hoch)". `EverydayColorWheel` :host had `max-width: 320px` AND `aspect-ratio: 1 / 1`. When a consumer set explicit `width: 360px; height: 360px` (assets/test/wheel-variants.html), `max-width` clamped width to 320 but the explicit height stayed 360 -> 320x360. Removed the max-width clamp; aspect-ratio now derives height from the actual rendered width, no double-source conflict. Popup-host wrapper still controls absolute size.

- ✨ feat(P15.6-r64+) **R338 new `default_view_mode: 'color-wheel'` and `default_view_mode: 'saved-colors'` (PA-0014 R3)**: Stefan-Quote PA-0014: "do the implementation and reinstate the page". Two new card display modes turn the entire card body into a standalone color picker. No slider, no icon, no mindmap. `color-wheel` mounts `<everyday-color-wheel>` with the configured (or default 8x24) geometry; taps dispatch `light.turn_on rgb_color`. `saved-colors` mounts `<everyday-saved-colors-picker>` against the same `_savedColorsState` palette + persistence path (helper:input_text + HA user_data fallback) the popup picker uses. Edit mode (long-press swatch -> wiggle + -/+ checkmark) works via a new `@state _displayModeSavedEditing`. Pattern mirrors the existing `effects-picker` mode. Shared `.display-mode-card` CSS for chrome consistency across all three "no-slider" picker tiles.

- 📚 docs(P15.6-r64+) **`docs/howto/05-gradient-mood.md` reinstated with the new color-wheel + saved-colors recipes (PA-0014 R5)**: page was deleted in PA-0011 R2 because it referenced a non-existent effect-picker-mode. New content describes the wheel-only / saved-only display modes that actually shipped, including density tuning, custom palette config, edit-mode behaviour, and when to use each. Cross-linked from README.

- 📚 docs(P15.6-r64+) **README updated for new wheel defaults + display modes (PA-0014)**: Color wheel feature line now reads "8 saturation rings x 24 hues default" instead of "21 hues x 6 saturation rings default". Added a new "Dedicated color picker tiles" section above Install with the side-by-side overview screenshot. Wheel-variants screenshot caption updated for new variants (8x24, 12x24, 5x24, smooth).

- 🎨 assets(P15.6-r64+) **regenerated wheel variants screenshots + PNG + WebP (PA-0014 R1, R4, R7)**: Stefan-Quote PA-0014: "gibt mir das WebP (weiss nicht, ob das auf reddit hochladbar ist (auf github(?))" -- WebP works on both: Reddit Gallery accepts WebP, GitHub renders it in READMEs / issues / PR comments. Both formats committed: `assets/screenshots/wheel-variants-grid.{png,webp}` (4-up gallery), `wheel-8x24.{png,webp}`, `wheel-12x24.{png,webp}`, `wheel-5x24.{png,webp}`, `wheel-smooth.{png,webp}`, plus a new `display-modes-overview.{png,webp}` showing the three card layouts side-by-side. Removed stale `wheel-21x6-default.png`, `wheel-12x4-simpler.png`, `wheel-6x3-chunky.png`. `assets/test/wheel-variants.html` updated to render the new variants (8x24 default top-left, 12x24 top-right, 5x24 bottom-left with the no-white-center rule active, smooth bottom-right) and the explicit `height: 360px` dropped from the inline style so aspect-ratio drives the second dimension cleanly. New helper `assets/test/wheel-single.html` accepts URL params `?hues=&rings=&type=` for headless single-wheel screenshotting via `--screenshot`.

Build: clean (rollup, TypeScript). Tests: 154/154 green. No behaviour regression on existing single-light / parallel / group renders -- all wheel + saved-colors code paths landed additively beside the existing handlers.

## 2026-05-12 - P15.6-r64: PA-0012 — R331 temp→brightness auto-revert on color-mode flip, R332 parallel_sliders.layout compact = brightness-only, R333 popup tap-outside no longer collapses card-expansion

- 🐛 fix(P15.6-r64) **R331 slider auto-reverts temperature → brightness when light leaves `color_temp` mode (PA-0012)**: Stefan-Quote PA-0012: "When a light is in temp mode and the slider-cycle is in temp mode and then canges the color mode to color the slider should reset to brightness mode". Pre-r64: the cycle-mode auto-revert (Stefan-2026-05-08) flipped `_memberModes[id]` back to `brightness` only on on→off transitions. Picking a colour via the wheel/saved popup left the slider stuck on the temperature axis even though the light's `color_mode` had flipped from `color_temp` to `hs`/`rgb`/`rgbw`/`rgbww`/`xy` — visually a temp slider on a light that no longer has temp. r64 adds a parallel watcher `_lastColorModes: Record<string, string>` and, in the same `updated()` block that handles on→off, detects `color_temp` → colour transitions per entity. When the slider was `temperature` for that entity, snaps it to `brightness` on the same hass push that delivers the flipped `color_mode`. Reverse (colour → `color_temp`) is intentionally NOT auto-applied — the user controls when they want the temperature slider back via the mode-picker, so we don't yank them out of brightness unprompted.

- 🐛 fix(P15.6-r64) **R332 `parallel_sliders.layout: compact` now renders brightness-only (PA-0012)**: Stefan-Quote PA-0012: "`parallel_sliders: layout: compact` should mean that in compact view only the brightness slider is shown". Pre-r64: R327 (r63n) introduced `parallel_sliders.layout: 'expanded' | 'compact'` to drop the mindmap-bg SVG when embedded — but compact still iterated `parallel_sliders.modes` and rendered every configured slider. Stefan's intent for the `compact` flag is "give me ONE slider, the brightness one" — the surrounding context (nested parent's topology) already provides the spatial cue, and a single brightness slider keeps the embedded tile narrow. r64 collapses the modes array to `['brightness']` when `layout === 'compact'`, at TWO render sites: (a) inline parallel-mindmap-layout in `everyday-light-card.ts` (Card 6b → also the `light.hall_boxes_2` nested-member render path), (b) parallel-sliders popup in `group-layout-expanded.ts._renderParallelSlidersPopup` (member double-tap → `expand_inline_parallel` opens it). The `modes:` array configuration is intentionally ignored when layout=compact — code comment documents the override. Expanded layout keeps the full N-axis stack as before.

- 🐛 fix(P15.6-r64) **R333 popup tap-outside-popup no longer collapses card-expansion (PA-0012)**: Stefan-Quote PA-0012: "in expanded view, open any pop up (color wheel or paralell sliders pop up) and then tap in empfy space to close the pop up but also the expansion of the card closes (only the pop up should close, the expanded mindmap should only close when you press outside of the card)". Pre-r64: each `<everyday-group-layout-expanded>` instance installs its own `document` outside-click listener that only checks ITS OWN popup state (`_wheelTarget`, `_savedColorsTarget`, etc.) before falling through to compact-collapse. In Stefan's nested-card setup (`/ed-slider/nested`: Spots is compact + inline-expanded, Bathroom hosts a wheel popup), Spots' listener sees no popup on Spots and runs compact-collapse, collapsing the inline-expanded view on the same tap that closes Bathroom's wheel. r64 adds a global popup-presence check in the compact-collapse branch: if `document.querySelector('.inplace-popup, .parallel-popup, .topology-popup')` returns non-null AND the click landed on any `ha-card` in `composedPath`, treat the tap as popup-dismissal-inside-card and skip the collapse. Truly empty dashboard space (no `ha-card` in path) still triggers collapse — Stefan's "should only close when you press outside of the card" semantics are preserved. The popups mount via portal at `document.body` and the wrapper sets `pointer-events: none` so tap-coords pass through to the actual underlying card chrome, making this discriminator reliable.

Tests: 154/154 green (unchanged). TypeScript: clean. Build: clean. Bundle path unchanged. R331/R332/R333 ride together under P15.6-r64.

## 2026-05-12 - P15.6-r63n (REVISED): PA-0007 deep-dive + PA-0008 — R326 supersedes R323 (mobile press-drag), R325 popup-origin fix, R327 parallel_sliders.layout compact, R328 drop ha-card for embedded parallel, R329 mindmap arm alignment to embedded parallel icon, R330 mindmap always at top of mode-picker

- ✨ feat(P15.6-r63n) **R327 new `parallel_sliders.layout: 'expanded' | 'compact'` config field (PA-0008)**: Stefan-Quote PA-0008 desired config: `parallel_sliders: { layout: compact, modes: [brightness, temperature] }` for `light.hall_boxes_2`. `'expanded'` (default) keeps the full mindmap-area SVG (curve arms + groupDot + orbiting icon) above the sliders. `'compact'` drops the SVG and renders a minimal vertical stack: small icon → slider-row → caption. Use for nested embedded members where the parent's mindmap already provides the surrounding topology — the inner mindmap-bg would be a visual double-up.

- 🐛 fix(P15.6-r63n) **R328 drop inner `<ha-card>` wrapper when parallel-inline is embedded (PA-0008)**: Stefan-Quote PA-0008: "currently there is a card background around the parallel sliders (which should not be there)". The parallel-inline render path in `everyday-light-card.ts` wrapped output in `<ha-card>` unconditionally. When `cfg.embedded === true` (R208 nested-member path), the parent already provides the card surface; the nested embedded card now returns its `<parallel-mindmap-layout>` (or `<parallel-compact-layout>`) directly. Mirrors the group-render branch pattern at line ~1006 (`if (cfg.embedded) return groupContent`).

- 🐛 fix(P15.6-r63n) **R329 mindmap-arm anchors to embedded parallel-inline icons (PA-0008)**: Stefan-Quote PA-0008: "the main mindmap dot that leads to the hall_boxes_2 icon dot is not correctly aligned". Pre-r63n: `_measureNestedIconYs` looked for `.tile.group .ic` or `.tile.group.compact-target .ic` inside embedded `<everyday-group-layout-expanded>` children. Boxes-as-parallel-inline doesn't render a group-layout-expanded child — its icon is `.parallel-mindmap-icon` (or `.parallel-compact-icon`) sitting in the embedded card's own shadow root. r63n extends the measurement to also query `.parallel-compact-icon`, `.parallel-mindmap-icon`, and `.single-icon` as fallbacks. Direct `getBoundingClientRect` on whichever variant is rendered → the parent's `memberYs[]` lands exact, and the curve-arm terminates at the actual icon center.

- ✨ feat(P15.6-r63n) **R330 mindmap option pinned to top (270°) in mode-picker (PA-0008)**: Stefan-Quote PA-0008: "wenn im Press-drag-select menü / dem mode picker die mindmap-option verfügbar ist, muss diese immer oben angezeigt werden". Pre-r63n: when `additionalMindmap` was set (standalone compact-group cards with both parallel-popup AND mindmap-expand), `getPickerSlots` appended `'mindmap'` at the END of the slot list — so the angle distribution placed it at the lower-left (198° in pentagon, 210° in hexagon). r63n swaps: when `additionalMindmap` is true, `slots[0]` becomes `'mindmap'` (north pole 270°) and the displaced `'parallel'` moves to the end. Saved/effects/wheel/cycle keep their relative order; the pentagon/hexagon shape just rotates so mindmap occupies the top. `useMindmap` path was already at top (no change needed).

- ✨ feat(P15.6-r63n) **R326 mobile press-drag-select fix (PA-0007 deep-dive, supersedes R323)**

- ✨ feat(P15.6-r63n) **R326 mobile press-drag-select fix (PA-0007 deep-dive, supersedes R323)**: Stefan-Quote PA-0007: "scrolling is still possible and the previous issue persists". /deep-dive convened 4 agents in parallel (Frontend Developer, Mobile App Builder, Code Reviewer, general-purpose web-research). Convergent root cause: R323 had two structural flaws — (a) `documentElement.style.touchAction = 'none'` fired inside the long-press timer (t=200ms) but the browser's touch-action chain is read at FIRST pointerdown and FROZEN for the gesture's lifetime per W3C pointer-events-3 spec; iOS Safari + Android Chromium classify pan-vs-tap by the first pointermove past slop (~30-50ms), well before the lock could acquire; (b) the parallel-inline bound icon (`.parallel-mindmap-icon`), the `.tile.group` element (bound by `_bindExpandedGroupGestures` + `_bindCompactGestures`), and the picker-wrapper overlays (`.parallel-picker`, `.single-picker`, `.parallel-mindmap-area`) all had default `touch-action: auto` — only `.tile.member` + `.single-icon[data-interactive]` were covered by R323. Fix layered four sub-patches: **R326-A** static `touch-action: none` on bind-targets (`.tile.group` in `group-layout-expanded.styles.ts` + `popup-portal-styles.ts`; `.parallel-mindmap-icon` in `everyday-light-card.ts`). **R326-B** static `touch-action: none` on parent overlays (`.parallel-mindmap-area`, `.parallel-mindmap-area .parallel-picker`, `.single-icon-wrap .single-picker`). **R326-C** moved scroll-lock acquisition to pointerdown via new `onPointerDownLock` / `onPointerDownRelease` callbacks in `gesture-detector.ts`; `picker-controller.ts` wires `_acquireScrollLock` synchronously at pointerdown and releases on tap-completion/cancel paths (long-press route releases via existing `_setPickerOpen(false)`). Idempotent — `_setPickerOpen(true)` still calls acquire but the snapshot-guard makes it a no-op when already held. **R326-D** exposed `<PickerController>.origin` public getter; updated `_runDoubleTapAction(entityId, action, variant, iconOrigin)` to thread the captured origin to `_applyPickerMode` so wheel/saved popups opened via R325 double-tap bloom from the icon position (without this, `_popupOrigin` stayed null and the popup didn't render). Full agent findings + synthesis in `development/deep-dive-2026-05-12-mobile-gesture/`.

- ✨ feat(P15.6-r63n) **R325 expanded-group renderer now consumes `gestures.{group_icon,member_icon}.double_tap` config (PA-0006)**: Stefan-Quote PA-0006: "double tap on 'bathroom' shows color-wheel, double Tap on 'Kitchen' shows saved colors, double tap on 'spots' triggers 'expand', for boxes there should be an inline expand option on double tap, that shows the brightness and temp slider side by side". Pre-r63n: the `GestureMapping.double_tap` field existed in `types/config.ts` since Phase 2, but only `everyday-light-card.ts._handleDoubleTap` (parallel-inline path) consumed it. The expanded-group renderer (`components/group-layout-expanded.ts`) hardcoded all three picker controllers' `onDoubleTap`: `_expandedGroupPicker` had no callback (silent no-op), `_compactGroupPicker` and `_createMemberPicker` both did `cycle_mode`. r63n wires all three to consult config. New @property `groupDoubleTapAction?: GestureAction` carries the host card's `gestures.group_icon.double_tap` field; per-member actions are read directly from `memberConfigs.get(id)?.gestures?.member_icon?.double_tap` (no new property needed — `memberConfigs` was already plumbed since R208). New private dispatcher `_runDoubleTapAction(entityId, action, variant, iconOrigin)` covers the existing action vocabulary (`color_wheel`, `saved_colors`, `expand_inline`, `expand_inline_parallel`, `cycle_mode`, `toggle`, `toggle_with_restore`, `effects_list`, `classic_more_info`, `mode_picker`) and routes through the existing `_applyPickerMode` / `_setMemberMode` machinery so no separate state machines were introduced.

- ✨ feat(P15.6-r63n) **`expand_inline_parallel` action added to `GestureAction` (PA-0006)**: Stefan's Boxes case (`light.hall_boxes_2` as a leaf member) wants double-tap to open an inline parallel-sliders view with brightness + temperature side-by-side. MVP reuses the existing parallel-popup mechanism — popup mounts at the member's slider rect, so visually it sits where the slider was (close to "inline" but technically still a body-portal popup). A true tile-replace-with-N-sliders inline-expand is a follow-up if Stefan prefers it — backlogged in `docs/BACKLOG.md ## Tentative`.

- 🐛 fix(P15.6-r63n) **R324 drop redundant on/off state-line across all 3 caption variants (PA-0005)**

- ✨ feat(P15.6-r63n) **R325 expanded-group renderer now consumes `gestures.{group_icon,member_icon}.double_tap` config (PA-0006)**: Stefan-Quote PA-0006: "double tap on 'bathroom' shows color-wheel, double Tap on 'Kitchen' shows saved colors, double tap on 'spots' triggers 'expand', for boxes there should be an inline expand option on double tap, that shows the brightness and temp slider side by side". Pre-r63n: the `GestureMapping.double_tap` field existed in `types/config.ts` since Phase 2, but only `everyday-light-card.ts._handleDoubleTap` (parallel-inline path) consumed it. The expanded-group renderer (`components/group-layout-expanded.ts`) hardcoded all three picker controllers' `onDoubleTap`: `_expandedGroupPicker` had no callback (silent no-op), `_compactGroupPicker` and `_createMemberPicker` both did `cycle_mode`. r63n wires all three to consult config. New @property `groupDoubleTapAction?: GestureAction` carries the host card's `gestures.group_icon.double_tap` field; per-member actions are read directly from `memberConfigs.get(id)?.gestures?.member_icon?.double_tap` (no new property needed — `memberConfigs` was already plumbed since R208). New private dispatcher `_runDoubleTapAction(entityId, action, variant)` covers the existing action vocabulary (`color_wheel`, `saved_colors`, `expand_inline`, `expand_inline_parallel`, `cycle_mode`, `toggle`, `toggle_with_restore`, `effects_list`, `classic_more_info`, `mode_picker`) and routes through the existing `_applyPickerMode` / `_setMemberMode` machinery so no separate state machines were introduced.
- ✨ feat(P15.6-r63n) **`expand_inline_parallel` action added to `GestureAction` (PA-0006)**: Stefan's Boxes case (`light.hall_boxes_2` as a leaf member) wants double-tap to open an inline parallel-sliders view with brightness + temperature side-by-side. MVP reuses the existing parallel-popup mechanism — popup mounts at the member's slider rect, so visually it sits where the slider was (close to "inline" but technically still a body-portal popup). A true tile-replace-with-N-sliders inline-expand is a follow-up if Stefan prefers it.
- 🐛 fix(P15.6-r63n) **R324 drop redundant on/off state-line across all 3 caption variants (PA-0005)**: Stefan-Quote PA-0005: "they all show the Status below the name of the entity. The Status should never be shown (I mean 'on'/'off' ( this is redundant))". Affected: `parallel-mindmap-layout > .caption` (line 911, pure state — span dropped entirely, no mode-suffix to preserve), `.hpill-caption` (line 1175, state + optional mode-suffix — span dropped unless mode is temperature/volume, in which case render just "temp"/"vol"), `.caption` (line 1225, vertical single-light — same conditional pattern as hpill). Icon color already conveys on/off (active ⇒ entity-rgb stroke, inactive ⇒ secondary-grey), so the text was pure redundancy. Mode-suffix preserved for non-default-brightness modes since it conveys real info (which slider axis is active) — Stefan only called out the on/off text as redundant.

- 🐛 fix(P15.6-r63n) **R323 mobile press-drag-select gesture: scrolling no longer hijacks target acquisition (PA-0004)**: Stefan-Quote PA-0004 (initial): "there is still and issue with the Press-drag-select gesture on mobile where instead of our gesture horizontal and vertical scrolling is triggered instead". Stefan-Quote PA-0004 (clarification 1): "the menu pops up but the taget aquisition is not working properly". Stefan-Quote PA-0004 (clarification 2): "no the pop up menu appears but the moving of the finger to the mode picker item triggers scrolling instead. Scrolling should be locked when our menu is active (kinda?)". Root cause: the bound icon (`.tile.member`, `.single-icon[data-interactive]`) already declared `touch-action: none` so the long-press timer fired correctly and the picker overlay opened. But once the user's finger moved OFF the icon and ONTO a picker dot (`everyday-mode-picker > .dot`), those dot elements inherited default `touch-action: auto` — and mobile Safari/Chrome's touch-scroll engine reclaimed the touch for page-scrolling even though `setPointerCapture` was active on the icon (pointer-capture does not override the lower-level touch-scroll decision). Result: pointermove events stopped arriving at the gesture-detector, target-acquisition broke, and the page scrolled underneath the visible-but-frozen picker.
- ✨ feat(P15.6-r63n) **per-element `touch-action: none` on mode-picker dots (PA-0004)**: `src/components/mode-picker.ts` now declares `touch-action: none` on both the `.picker` container and each `.dot` button. First line of defense: when the finger is over any picker child, the browser knows not to claim the touch for scrolling.
- ✨ feat(P15.6-r63n) **global scroll-lock while picker is open (PA-0004)**: `src/helpers/picker-controller.ts` now sets `documentElement.style.touchAction = 'none'` when `pickerOpen` flips false→true and restores the previous value (snapshotted, not blindly cleared to `''`) when it flips back. Belt-and-suspenders catch for the rare case where the finger drifts off the picker bounds entirely (e.g. fast diagonal swipe past a dot onto raw page background). Implementation routes every `pickerOpen` mutation through a private `_setPickerOpen(value)` setter so the lock state can never desync from the picker state. `hostDisconnected` also releases the lock — HA view-switches that disconnect cards mid-gesture would otherwise leave `touch-action: none` frozen on the page root indefinitely (would break ALL page scrolling until reload). `_releaseScrollLock` reads from the per-controller snapshot, so a host-page-level rule that intentionally set touchAction before our picker opened is preserved (we don't blindly clobber to `''`).
- ✅ verify(P15.6-r63n) **all 154 tests still green, build clean, TypeScript clean (PA-0004)**: no behavior change on desktop (mouse + `touch-action: none` is moot — mouse never triggered scroll-claim). On mobile, both fixes compose: per-element prevents the typical case, global lock catches edge cases. Stefan must hand-verify on mobile (Chrome MCP can't simulate touch-scroll behavior accurately).

## 2026-05-12 - P15.6-r63m: PA-0044 — R319/R320/R321/R322 overflow-aware gap + slider-width (no overlap when Spots expands)

- ✨ feat(P15.6-r63m) **R319–R322 overflow-aware resolution layered on R299 (PA-0044)**: Stefan-Quote PA-0044: "the gaps are not recalculated when a node is expanded ... when i expand 'spots' here ... the sliders are overlapping. This should never happen. Rather the gaps should be readjusted first and if the space is still too tight the slider width should be adjusted there after. ... only the expanded sliders width is reduced (if possible)". Live-probe via Chrome MCP on `/ed-slider/nested` with force-expanded Spots: depth=3 card with hostWidth 103 px, inherited slider-width 40 px → 3 × 40 + 2 × 3 (gap floor) = 126 px content vs 103 px col = **23 px overflow → visible slider-on-slider overlap**. New pure helper `resolveOverflow({depth, containerWidth, childCount, baselineSliderWidth})` in `helpers/member-cols-gap.ts` implements Stefan's priority chain: (1) `computeMemberColsGap` R299 base, (2) if `n*baseline + (n-1)*baseGap > cw` → shrink gap toward effective floor first, (3) if gap-at-floor still insufficient → compute local `--everyday-slider-width` override `floor((cw - (n-1) * gapFloor) / n)` clamped at `SLIDER_WIDTH_HARD_FLOOR = 24 px`. The `Math.floor` rounds the override DOWN so CSS grid sub-pixel rounding (which drops fractional col widths) doesn't leak 1 px of content past col bounds — confirmed live: 3 × 32 + 2 × 3 = 102 ≤ 103, **0 px slider overflow, all slider-edge gaps positive (4 + 3 px), all member-icon edge gaps positive (2 + 1 px)**.

- 🐛 fix(P15.6-r63m) **R319 gap recompute now fires on child-expand (PA-0044)**: pre-r63m the R299 `_memberColsRO` observed only `.member-cols` WIDTH on the host card. Child compact↔expanded toggles change the child's HEIGHT (compact ~120 px → expanded ~380 px), not the parent's width, so the parent's RO didn't fire — its overflow assessment was stale. r63m extends the existing `_memberColRO` (per-col HEIGHT observer from R260) callback to ALSO call `_scheduleGapRecompute()` whenever any col-height changes — a col-height change is a reliable signal that the SHAPE of subtree content shifted (typically a nested child went compact→expanded), and the overflow check must be re-evaluated. Belt-and-suspenders with the newly-expanded child's own RO (which fires automatically on the first observation of its NEW `.member-cols`).

- ✨ feat(P15.6-r63m) **R320 strict overlap prevention via gap-first / slider-second cascade (PA-0044)**: Stefan-Spec "gaps should be readjusted first and if the space is still too tight the slider width should be adjusted there after". `resolveOverflow` returns `{gap, sliderOverride?}`:
  - **No overflow** (`needed ≤ cw`): returns R299 base gap, no override.
  - **Gap-shrink suffices** (`gapForFit ≥ effectiveFloor`): returns the shrunk gap, no override. Inherited slider widths preserved.
  - **Gap at floor + still overflowing**: returns `{gap: floor, sliderOverride}` where override is the largest INTEGER slider width that fits at floor gap. Clamped to `SLIDER_WIDTH_HARD_FLOOR = 24 px` — below that we accept overlap rather than render an unusable touch-target (rare; happens only when too-many-leaves are stuffed into a too-narrow card).

- ✨ feat(P15.6-r63m) **R321 slider-width adapts to available space, self-stabilizing (PA-0044)**: Stefan-Spec "the slider width to automatically adjust based on the available space. Priority: gaps first, Slider width second". Override cascade is purely re-derived each recompute: baseline read from `getComputedStyle(this).getPropertyValue('--everyday-slider-width')` on the HOST element (not on `.layout` — host's CSS-var reflects the PARENT's cascade, ignoring our own inline override on our `.layout`). If the container later grows wide enough that the natural baseline fits, `resolveOverflow` returns `sliderOverride: undefined`, the inline-style is dropped, and sliders relax back to the inherited width. No state tracking required — pure re-derivation each frame, no oscillation, no infinite loops (0.5 px dedup on both gap and override values).

- ✨ feat(P15.6-r63m) **R322 compact-cards keep their inherited slider width (PA-0044)**: Stefan-Spec "different widths in expanded mode vs collapsed though the order should be that only the expanded sliders width is reduced (if possible)". By construction: `sizeOverrides` (which carries `--everyday-slider-width`) is applied only to `<div class="layout">` (expanded) at line 1746, NOT to `<div class="layout compact">` at line 2776. Compact-mode never reaches `resolveOverflow` because compact-rendering has no `.member-cols` — the width-observer has nothing to attach to. State values `_computedSliderWidthOverride` and `_computedMemberColsGap` may persist when the card cycles expanded→compact, but they're inert until the expanded `.layout` reappears. Stefan-spec satisfied with zero conditional logic.

- 🧪 test(P15.6-r63m) **12 new unit tests in `test/member-cols-gap.test.ts` (PA-0044)**: `resolveOverflow` coverage for no-overflow (R299 base gap, no override), gap-shrink-first (overflow absorbed by reducing gap above floor), gap-at-floor + slider override (the LIVE Spots case `cw=103,n=3,baseline=40 → gap=3,slider=32`), hard-floor protection (`cw=50 → slider=24` accepted-overflow), post-fit invariants (total content ≤ container px for non-pathological inputs), relax-on-grow (override drops when container widens, no hysteresis), invalid-baseline robustness (NaN/0/negative no-throw). Full suite: 154/154 (was 142/142 at r63l).

Tests: 154/154 (142 r63l pre-existing + 12 R319-R322). TypeScript: clean. Build: clean. Bundle deployed to `/config/www/everyday-light-card.js` + lovelace resource URL bumped to `?v=0.9.0-p15-r63m-1778574000000`.

## 2026-05-12 - P15.6-r63l: PA-0043 — R313/R314/R315/R316/R317/R318 (padding parity + 1-mode parallel collapses to single-light + 3 hide flags + compound card with entity:none)

- 🐛 fix(P15.6-r63l) **R313 `.parallel-mindmap-layout` padding 24 px (PA-0043 Req A)**: Stefan-Quote PA-0043 Req A: "in this config the padding is wrong. ha-card div.parallel-mindmap-layout padding needs to be 24px (as it is for all the other cards) I dont understand how it can be different". Pre-r63l: `.parallel-mindmap-layout { padding: 14px 14px 16px }`, `.parallel-inline { padding: 16px 16px 20px }`. Bumped both to `padding: 24px` to match `.container`/`.layout`/`.layout.compact` from R303 parity.
- ✨ feat(P15.6-r63l) **R314 1-mode parallel-inline collapses to bare single-light layout (PA-0043 Req B)**: Stefan-Quote PA-0043 Req B: "this config should look exactly like this config (except that the slider is the temp slider of course". Best-guess interpretation: 1-mode parallel-inline should visually equal bare single-light (only slider mode differs). Pre-r63l: even 1-mode parallel rendered through `.parallel-mindmap-layout` shell (mindmap-bg SVG, parallel-mindmap-icon, caption-below) — visually different from bare single-light. r63l routes `default_view_mode: parallel + modes.length === 1` THROUGH the bare single-light render path with the configured mode override. Implementation: early branch in `render()` skips the parallel-inline shell; downstream `mode` resolution picks up `cfg.parallel_sliders.modes[0]` as the slider mode. Also extended slider-mode local type to include `'hue'` and `'saturation'`. Live-verified via Chrome MCP: 1-mode-parallel-temp card now renders `.container vertical` (NOT `.parallel-mindmap-layout`), 1 slider in temperature mode, 24 px container padding — visually identical to bare single-light.
- ✨ feat(P15.6-r63l) **R315 `parent_node: 'show' | 'hide'` config (PA-0043 Req C)**: Stefan-Quote PA-0043 Req C: "an option to ... disable the parent ( so that only the sliders show up besides each other". New top-level config field `parent_node?: 'show' | 'hide'` (default 'show'). When 'hide', `everyday-group-layout-expanded` skips the parent group-row in `_renderTopologyTree()`. Plumbed via new `hideParent` boolean property on the inner component.
- ✨ feat(P15.6-r63l) **R316 `show_icons` config (PA-0043 Req C)**: Stefan-Quote: "to disable the icons completely". New top-level config field `show_icons?: boolean` (default true). When false, the inner component's `.topology` gets a `.hide-icons` class which CSS-hides every `.ic` + `.compact-glyph` inside (member icons + parent icon). Slider labels (`.lbl`) stay so the user still sees what each slider controls.
- ✨ feat(P15.6-r63l) **R317 `show_mindmap` config (PA-0043 Req C)**: Stefan-Quote: "to disable the mindmap". New top-level config field `show_mindmap?: boolean` (default true). When false, `_renderTopologyTree` skips the `<everyday-mindmap-path class="topology-bg">` SVG entirely. Independent of `show_icons` — you can keep the icon tiles while removing the curve-arms + dots.
- ✨ feat(P15.6-r63l) **R318 compound card with `entity: 'none'` + per-member configs (PA-0043 Req D)**: Stefan-Quote PA-0043 Req D: "I want to have a slider config in 1 config, where the main lights are to the left and to the right there is one slider to adjust the color temp for all lights". `setConfig` now accepts `entity: 'none'` (or omitted) when `group.manual_members` is populated. New `_renderContainerCard()` method renders directly as `<everyday-group-layout-expanded>` with `hideParent: true` default. Each manual_member uses its OWN full card-config (already supported via R208 `ManualMember = string | (Partial<Config> & {entity})`). Inner `protected render()` guard relaxed to skip `groupEntityId` check when `memberIds.length > 0`. Live-verified via Chrome MCP: `{entity: 'none', group: {manual_members: [hall_spot_1, hall_spot_2-with-parallel-temp]}}` renders 2 sub-cards side-by-side, each with its own `default_view_mode` honored.

Tests: 142/142 green. TypeScript: clean. Build: clean. Bundle deployed + resource bumped to `?v=0.9.0-p15-r63k-1778572314900` (cb tag is "r63k" but the bundle is the r63l build).

## 2026-05-12 - P15.6-r63k: PA-0041 — R299 dynamic `--member-cols-gap` (container-width × depth-ratio)

- ✨ feat(P15.6-r63k) **R299 dynamic / relative `--member-cols-gap` (PA-0041, sketch from PA-0031 unparked)**: Stefan-Quote 2026-05-11 18:53 CEST (PA-0031): "wenn ich hier `http://192.168.2.231:8123/ed-slider/nested` zb spots aufklappe, dann sollen sich alle anderen gaps automatisch anpassen … aktuell sind alle abstände statisch … sie sollen aber relativ und dynamisch sein. du hast in r292 28/14/8 vorgeschlagen. Nimm diese werte doch als relationen. (und minimum abstand ist 3px oder so)". Plan reevaluation (`/.context/prompt-analysis-0041.md` Phase A) trimmed the sketch: dropped the cross-card event-bus + `gap_strategy` config-flag + LCA-boundary-depth distribution model. Replaced with a simpler per-card formula: `gap = clamp(floor=3, maxByDepth[d], ratios[d] * 8 * containerWidth / 600)`. Each `<everyday-group-layout-expanded>` self-measures its own `.member-cols.clientWidth` via a dedicated ResizeObserver (rAF-coalesced + 0.5 px dedup to avoid RO-loop), runs the pure helper `helpers/member-cols-gap.ts`, and writes the result as inline `--member-cols-gap` on `.layout`. The CSS `:host([depth='N'])` rules in `group-layout-expanded.styles.ts` stay as the pre-measurement initial-paint fallback (no FOUC). Mindmap-arm geometry (`<everyday-mindmap-path>.tileGap`) now reads the same computed value so arms and visible gaps stay locked together at every depth — removed the previously-duplicated ternary 28/14/8/4 in `_renderTopologyTree()`.
- ✨ feat(P15.6-r63k) **Pure helper `helpers/member-cols-gap.ts` (PA-0041)**: extracted the gap-resolution math from the component so it's unit-testable in isolation. Exports `computeMemberColsGap({depth, containerWidth})` plus the `GAP_RATIOS / GAP_MAX_BY_DEPTH / GAP_FLOOR / GAP_REFERENCE_WIDTH / GAP_RATIO_TO_PX_UNIT` constants. Depth-out-of-range clamps into the ratios array; container-width 0/NaN/Infinity returns the depth-static max (matches the CSS pre-measurement fallback so the inline-style + CSS-fallback always agree). Drop-in parity with R292's absolutes at the reference width (600 px): depth 0 → 28, depth 1 → 14, depth 2 → 8, depth 3 → 4, depth 4 → 2.
- 🧪 test(P15.6-r63k) **32 new unit tests in `test/member-cols-gap.test.ts` (PA-0041)**: reference-width parity (4 tests, depth 0..3 lands on absolutes), narrow-container shrink (5 tests, half-width → half-gap, floor-clamp at very narrow), wide-container max-clamp (3 tests, never overshoots max), floor + effectiveFloor at deep nests (5 tests, depth 4 returns 2 px even though < floor — Stefan's "oder so" treated as guideline), depth-out-of-range (4 tests, including negative + fractional + depth 99), invalid container width (4 tests, depth-static fallback), ratio preservation in mid-range (2 tests, d0/d1 ratio = 3.5/1.75), monotonicity (2 tests, wider container always means ≥ narrower gap at same depth). Total suite: 142/142 (was 110/110 pre-r63k).
- 🐛 fix(P15.6-r63k) **mindmap-arm geometry now reads the dynamic gap (PA-0041)**: pre-R299 the component had `TILE_GRID_GAP` as a hardcoded ternary `28/14/8/4` duplicated from the CSS, threaded into `<everyday-mindmap-path>.tileGap`. With dynamic CSS-gap, the duplicate would drift on narrow viewports (CSS gap = 16.33 px, arm-gap = 14 px → arms miss col-centers). Post-R299 both read from `_computedMemberColsGap`. Chrome-MCP probe on `/ed-slider/nested`: 6/6 cards have `cssGap === tileGap` (depth 0 at 1561 px → 28/28, depth 1 at 657 px → 13.6/13.6, depth 2 at 318 px → 4.01/4.01, depth 2 at 212 px → 3/3 floor-clamp).
- ♻️ refactor(P15.6-r63k) **simplified vs the R299 sketch (PA-0041)**: the sketched algorithm (LCA-boundary-depth + global gap-space-distribution + cross-card composed-event bus + `gap_strategy: 'static' | 'dynamic'` config-flag + per-adjacency-walking) was correct but over-engineered. The simpler per-card formula achieves the same visual effect: container-width is the only input that varies in practice, and each card's RO catches all the situations where the sketch needed an event bus (expand/collapse, sibling resize, viewport-drag). Removed ~30% of the sketched complexity. The deferred `gap_strategy` opt-out is unnecessary because the CSS-var override still works for users who explicitly set `--member-cols-gap` higher in the cascade (theme-level rules outside the component's shadow DOM). Reasoning + drop list in `/.context/prompt-analysis-0041.md`.

Tests: 142/142 green (110 pre-r63k + 32 new). TypeScript: clean. Build: clean. Bundle deployed to `/config/www/everyday-light-card.js` + lovelace resource URL bumped to `?v=0.9.0-p15-r63k-1778571500000`.

## 2026-05-12 - P15.6-r63j: PA-0040 — R312 bare single-light default gestures (gate dropped)

- ✨ feat(P15.6-r63j) **R312 bare single-light icon gesture-active by default (PA-0040)**: Stefan PA-0040 silently attached the bare config `entity: light.hall_spot_1` (nothing else — no `group`, no `default_view_mode`, no `gestures`). Combined with the verbatim PA-0039 Req 5 "The defaults must work! (on/off, Press-drag-select gesture, doppeltip cycle". Pre-r63j: `everyday-light-card.ts:564-572` gated the `.single-icon` bind behind `cfg.gestures?.member_icon` (R289 / PA-14). Without that explicit config, the icon was inert — no hand-cursor, no tap-toggle, no picker. r63j drops the gate: bind whichever icon exists in the rendered DOM (`.parallel-mindmap-icon` for parallel-inline, `.single-icon` for bare single-light). Group + horizontal-pill render paths don't expose either selector, so `bindIcon(null)` cleanly tears down. Defaults now wired through the PickerController's existing behavior: tap → `hass.callService('light', 'toggle', {entity_id})` (`picker-controller.ts:280-285`), long-press → picker ring, double-tap → `_handleDoubleTap` (cycle_mode default, no-op when only one slider mode visible). Live-verified via Chrome MCP probe: bare `light.hall_spot_1` card with NO gestures config gets `pickerBoundEl: HA-STATE-ICON`, `pickerHasGestures: true`, `iconCursor: pointer`, and synthetic tap fires `light.toggle` service call on the entity.
- ✅ verify(P15.6-r63j) **1-mode parallel-inline edge case (PA-0040 Implicit-1)**: Stefan also attached `default_view_mode: parallel, modes: [temperature], show_labels: false` (1-slider parallel). Live-verified via Chrome MCP — renders 1 slider at 220 px height (r63i default), 0 labels (r63i show_labels gate), `.parallel-mindmap-icon` present + picker-bound, popup width formula `Math.max(160, 1*82+20)` → 160 px (no degeneracy). No additional code change needed. Inherits r63i's gestural + visual fixes automatically.

Tests: 110/110 green. TypeScript: clean. Build: clean. Bundle deployed + resource bumped to `?v=0.9.0-p15-r63j-1778569999190`.

## 2026-05-12 - P15.6-r63i: PA-0039 batch — R307/R308/R309/R310/R311 (parallel labels off + popup slider length + drop cycle slot + compact state-line removed + compact ring color cascade)

- ✨ feat(P15.6-r63i) **R307 parallel-slider labels OFF by default (inline + popup, PA-0039)**: Stefan-Quote PA-0039 Req 2: "please disable the lables for the paralell sliders (pop up und inline) by default". Pre-r63i both call-sites used `!== false` (labels ON unless explicitly `false`) — flipped to `=== true` (labels OFF unless explicitly `true`). Two surfaces affected: (a) inline path at `everyday-light-card.ts:720` for `default_view_mode: parallel` cards (Card 6b), (b) popup path at `group-layout-expanded.ts:1578` for the parallel-sliders popup triggered from member-mode-picker → 'parallel' slot. Opt-in via `parallel_sliders.show_labels: true` still works. Live-verified via Chrome MCP probe: inline parallel-card now renders 0 labels (was 2 — "hue"/"saturation" — pre-r63i).
- ✨ feat(P15.6-r63i) **R308 parallel-popup slider length matches parent default (220 px, PA-0039)**: Stefan-Quote PA-0039 Req 3: "the paralell slider pop ups sliders should have the same slider length as its parent". Pre-r63i: popup defaulted to 170 px (the historic `_renderParallelSlidersPopup` choice), parent (compact-group + parallel-inline) defaults to 220 px (vertical-pill-slider :host fallback + `everyday-light-card.ts:729`). 50 px visual mismatch when the popup opened mid-card. Bumped popup `sliderHeightPx` from `fullLengthSliders ? 260 : 170` → `: 220` so the popup now matches the parent context's default. Explicit `fullLengthSliders: true` opt-in still yields 260 px.
- 🐛 fix(P15.6-r63i) **R309 cycle slot dropped from `group-expanded` picker (PA-0039)**: Stefan-Quote PA-0039 Req 4: "for this cards parent node it doesnt make sense for it to have the mode picker element to cycle the slider. because it is expanded and doesnt have a slider it can cycle". The expanded-group parent tile is icon-only (no slider) — `cycle` slot was a visual no-op that confused the picker UX. Two changes in `picker-geometry.ts`: (a) `getPickerSlots()` skips `cycle` for `variant === 'group-expanded'` → 3-slot triangle `[parallel, saved, wheel]` (or 4-slot diamond with effects). (b) `getPickerAngleMap()` special-cases `group-expanded` to use FIXED cardinal angles (parallel:270°, saved:0°, wheel:90°, effects/mindmap:180°) instead of uniform-distribute — keeps the user's muscle memory of "parallel up, saved right, wheel down" intact. Other variants (`member`, `group-compact`, `parallel-inline`) unchanged. `member` + `group-compact` keep `cycle` because they DO have a slider to cycle. Tests updated: 110/110 green.
- 🐛 fix(P15.6-r63i) **R310 compact-group state-line removed (PA-0039)**: Stefan-Quote PA-0039 Req 6a: "i dont understand why this config shows the label 'on'. it does not make any sense. Where does the 'on' come from? remove it". Pre-r63i: R274 (P15.6-r61) added a `<div class="compact-state-line">${state}</div>` sibling-of-`.lbl` inside the compact group-tile — meant for parity with `.caption .state` on single-light cards. Stefan finds it redundant noise (state already encoded via icon-color cascade per R305/R311). Removed (a) the `<div class="compact-state-line">…</div>` markup at `group-layout-expanded.ts:2522`, (b) its CSS rules at `group-layout-expanded.styles.ts:207-213`, (c) the R274 `gap: 2px` override on `.tile.group.compact-target` (reverted to standard `.tile { gap: 4px }`).
- 🐛 fix(P15.6-r63i) **R311 compact-group icon ring color mirrors expanded view state cascade (PA-0039)**: Stefan-Quote PA-0039 Req 6b: "the dot around the icon is not the same color as in the expanded view. it must be the same as if it were the extended view." Pre-r63i: `.layout.compact .tile.group.compact-target .ic` had hardcoded `border: 2.6px solid var(--mindmap-group-stroke, #f4b91d)` — always gold regardless of state. Expanded view's SVG groupDot (`mindmap-path.ts:579-584`) uses dynamic stroke (gray when off, RGB when on, themed gold otherwise). Fix: compact `.ic` now gets inline `border-color` computed in `_renderCompact()` mirroring the exact same branching: off → `var(--disabled-color)`, on+rgb → `rgba(R,G,B)`, otherwise → themed gold fallback. Live-verified via Chrome MCP probe (light off → computed `rgb(189, 189, 189)` = themed disabled-color, expected branch).

### Req 1 + Req 5 (PA-0039) — no code change, fixed-in-place by prior r62 + r63h work

Stefan-Quote Req 1: "the paralell slider pop up can currently only be closed when tapping outside the parent card. thouh it should close in the same manner as the color wheel, saved colors and effects list". Stefan-Quote Req 5: "i should be able to use the standard guestures but none work. The defaults must work! (on/off, Press-drag-select gesture, doppeltip cycle".

- **Req 1 outside-click close**: ALREADY FIXED by R287 (r62) which narrowed the popup wrapper from `Math.max(r.width, …)` (full card-width) to `Math.max(160, modes.length * 82 + 20)`. With the narrower popup, clicks inside the card but outside the popup wrapper now hit `HOME-ASSISTANT` rather than `.parallel-popup`, so the host's `_installOutsideClickListener` correctly closes. Live-verified in Chrome MCP this turn: clicks inside the popup (popup-body / popup-slider targets) keep it open ✓; clicks inside card + outside popup (any of 4 test points right/below/left/far) close it ✓. Stefan's complaint was against the stale pre-r62 cached bundle.
- **Req 5 default gestures on parallel-inline**: ALREADY FIXED by R306 (r63h) which adds `this._boundEl = null;` in `PickerController.hostDisconnected()` so the post-view-switch rebind actually re-attaches gesture listeners. Live-verified in Chrome MCP this turn: tap on `.parallel-mindmap-icon` of a 2-mode parallel-inline card toggled `light.hall_spot_1` from off → on ✓; long-press (350ms hold) opened the picker (pickerOpen=true) ✓. Stefan's complaint was against the stale pre-r63h cached bundle. Resource cache-bust to `?v=0.9.0-p15-r63i-<timestamp>` shipped this turn fixes the stale-cache on Stefan's Chrome.

Tests: 110/110 green. TypeScript: clean. Build: clean. Bundle deployed to `/config/www/everyday-light-card.js` + lovelace resource URL bumped to `?v=0.9.0-p15-r63i-1778542342835`.

## 2026-05-11 - P15.6-r63h: R306 root-cause fix (PA-0038 — PickerController._boundEl now cleared on hostDisconnected)

- 🐛 fix(P15.6-r63h) **R306 — `PickerController` retained `_boundEl` after `hostDisconnected`, blocking rebind on view-switch reconnect (PA-0038)**: Stefan PA-0038 visual confirmation matched the Chrome-MCP probe exactly: "on every parent icon the hand appears now - but no click action with the bug active". Root cause (FOUND LIVE): when HA dashboard view-switch fires `hostDisconnected` on every PickerController, the controller's gesture-detector listeners are disposed via `_gestureDispose?.()` — but `_boundEl` is NOT cleared. On view-switch-back HA reattaches the SAME element instance (verified `same_tile: true`, `same_picker: true`). Lit's `updated()` runs the host's rebind logic (r63g made it unconditional in `group-layout-expanded.ts`), which calls `picker.bindIcon(tile)`. The idempotency guard at `picker-controller.ts:224` (`if (el === this._boundEl) return;`) — designed to prevent mid-gesture rebind from killing the in-flight long-press detector (R145b Card 6b regression) — saw `tile === _boundEl` (still pointing at the now-disposed binding) and returned early WITHOUT re-attaching the gesture listeners. Visible symptom: cursor:pointer survives (inline style from initial bind sets `el.style.cursor='pointer'` on the element itself), `_boundEl` still points at the live element, but `_gestureDispose === null` → pointerdown listener gone → tap fires no handler. Fix: in `hostDisconnected()`, add `this._boundEl = null;` AFTER the `_gestureDispose?.()` call. Order matters — dispose first (lets the in-flight gesture-detector teardown run against the real element ref), THEN forget so the next `bindIcon(sameEl)` sees `null !== sameEl` and proceeds to re-attach listeners. Verified live via Chrome-MCP: after singles→groups→singles tab-switch, `pc_has_gestureDispose: true` (was false pre-fix), synthetic tap toggled `light.hall_spots` from "off" to "on". Combined with r63g (unconditional bind in `updated()`), the bug is FIXED. Both fixes needed together — r63g ensures the rebind ATTEMPT, r63h ensures the rebind ACTUALLY re-attaches listeners. Tests: 109/109 green.

## 2026-05-11 - P15.6-r63g: R306 (PA-0034 — group-tile gestures rebind unconditionally on every updated())

- 🐛 fix(P15.6-r63g) **R306 parent group-icon STILL unclickable after view-switch (R302 reopen, PA-0034)**: Stefan PA-0034: re-quoted PA-0032 R302 symptom verbatim — "as described many times". r63e's fix (clear `_lastBoundMemberIds` in `connectedCallback`/`disconnectedCallback`, widen rebind guards with `isFreshMount`) was necessary but not sufficient. Root cause analysis: HA dashboard view-switches may rebuild only the inner shadow-DOM (`.tile.group`, `.tile.group.compact-target`) without firing `connectedCallback` or `disconnectedCallback` on the `<everyday-group-layout-expanded>` web-component itself — the host `<everyday-light-card>` survives the switch, only its shadow-DOM subtree is rebuilt by Lit. In that path `_lastBoundMemberIds` is never cleared, `isFreshMount` stays false, and the previous gated rebind was skipped. The picker controller's `_boundEl` still pointed at the destroyed groupTile element; the newly-rendered groupTile had no handlers → Stefan's "first not hand on hover, other 2 hand but no action" smoking-gun. Fix: move `_bindCompactGestures()` + `_bindExpandedGroupGestures()` OUT of the gated block in `updated()`. They now fire unconditionally on every `updated()` pass — matching the pattern already in `everyday-light-card.ts:564-572` for the host card's `.single-icon` / `.parallel-mindmap-icon` (which is why single-light cards stayed clickable after view-switch while group cards didn't). Safety: `PickerController.bindIcon()` is idempotent (line 224: `if (el === this._boundEl) return;`) so calling it with the same element is a no-op (one identity check, ~zero cost). Mid-gesture rebind protection (R145b) preserved by the picker's own idempotency guard. The heavier `_rebindMemberGestures()` (which rebuilds the per-member picker Map) stays gated to avoid mid-gesture controller-teardown. Tests: 109/109. Build: clean.

## 2026-05-11 - P15.6-r63f: R305 + R303-followup (PA-0033 — icon_color cascades to all icons, single-light padding 24 px)

- ✨ feat(P15.6-r63f) **R305 icon_color cascades from card config to group + member icons (PA-0033)**: Stefan PA-0032 + PA-0033: "please can you make all icons behave the same? ... doesnt require many different implementaions to do one thing" + greenlight "of course, dann fix alles". r63d added `icon_color: 'on-state' | <CSS color>` to `EverydayLightCardConfig` but only wired it to `.single-icon` + `.parallel-mindmap-icon`. r63f completes the architectural unification: same field now drives the inline color on `.tile.group .ic` (compact + expanded group icons) AND `.tile.member .ic` (bare leaf member icons in expanded view). Implementation: extracted `computeIconStateColor()` from `everyday-light-card.ts` to new shared helper `src/helpers/icon-color.ts` (re-import in both files, no circular-dep risk). Added `@property iconColor` to `group-layout-expanded.ts` wired from host card via `.iconColor=${cfg.icon_color}`. Per-render-site: compact-glyph reads group entity state, expanded group-icon reads group state, each bare member reads ITS OWN state (5-member group with `icon_color: 'on-state'` → 5 different colored icons). For nested-embedded children (e.g. apartment view), `icon_color` propagates through the existing `childConfig` spread in `_renderTopologyTree` — nested embedded cards default to inheriting the parent's `iconColor` when their own config doesn't override. Explicit per-card override still wins. Result: ONE config, ONE behavior across ALL 5 icon render paths (`.single-icon` vertical, `.single-icon` horizontal/hpill, `.parallel-mindmap-icon`, `.tile.group .ic`, `.tile.member .ic`).
- 🐛 fix(P15.6-r63f) **R303-followup single-light .container + .hpill-row padding 24 px (PA-0033)**: Stefan PA-0033 "fix alles" — confirms my r63e follow-up note "single-light .container also at 24 px to keep all 3 surfaces matching". `.container { padding: 16 -> 24 }`, `.hpill-row { padding: 10px 16 -> 10px 24 }`. Vertical 10 px on hpill kept so horizontal-stacks of multiple pill cards stay reasonably narrow. All four surfaces — `.container` (single-light vertical), `.hpill-row` horizontal-axis (hpill), `.layout` (expanded), `.layout.compact` (compact-group) — now have uniform 24 px outer padding.

## 2026-05-11 - P15.6-r63e: R301 + R302 + R303 + R304 (PA-0032 follow-up — single-light long-press wired, parent-icon rebind on remount, padding parity 24 px, icon-color CSS unified)

- 🐛 fix(P15.6-r63e) **R301 single-light long-press now opens picker (PA-19 / PA-0032)**: Stefan PA-0032: "with this config [`light.hall_spot_1, gestures.member_icon.double_tap: color_wheel`] the default should be that 'long press' should work normally. currently it does not do anything. but interestingly with this config [parallel-mode + same gesture config] long press works". Root cause: r62 R289 wired `_picker.bindIcon(.single-icon)` so the gesture-detector DID fire on long-press, but `${this._picker.renderPicker()}` was only mounted in the parallel-mode render-path's `.parallel-mindmap-area`. Long-press → opened picker state → but no DOM container existed for the 4-option ring → silent no-op. Fix: wrapped the .single-icon in a new `.single-icon-wrap` positioning container in BOTH single-light render paths (vertical .caption AND horizontal .hpill-row) with `<div class="single-picker">${_picker.renderPicker()}</div>` as an absolutely-positioned overlay centered on the icon. Picker controller variant + bindings unchanged — only the missing render-target added. Tests: 109/109 still pass.
- 🐛 fix(P15.6-r63e) **R302 parent group-icon clickability survives dashboard view-switch (R294 reopen, PA-0032)**: Stefan PA-0032: "in chrome after switching views i cannot press the Parent icon ... here i can press the childeren no problem (spot 1 etc) but the parents not". Root cause: `_lastBoundMemberIds` guard in `updated()` (added in r62 to prevent rebind racing mid-gesture) skipped the rebind whenever `memberIds.join(',')` matched the cached value. HA dashboard view-switches that rebuild the card DOM left this cache stale — the new DOM elements had no gesture handlers attached, and the guard prevented the rebind from firing. PA-0031's "R294 closed" was premature — symptom only narrowed to parent-icon specifically. Fix: (1) clear `_lastBoundMemberIds = ''` in BOTH `connectedCallback` AND `disconnectedCallback`; (2) widen the member-bind guard in `updated()` to also rebind on `isFreshMount` (`_lastBoundMemberIds === '' && memberIds.length > 0`); (3) widen the compact/expanded group-bind guard to also rebind on `isFreshMount`. Mid-gesture rebind protection preserved by the existing string-equality check (`idsKey !== _lastBoundMemberIds`).
- 🐛 fix(P15.6-r63e) **R303 padding parity 24 px for `.layout` AND `.layout.compact` (PA-0032)**: Stefan PA-0032: "[div.layout.compact] and [div.layout] must have the same amount of padding (24px)!!". Pre-r63e: `.layout` = `24px 24px 20px` (asymmetric bottom for legacy mindmap-arm clearance, irrelevant since R237 dropped the arm), `.layout.compact` = 16 px (R274 single-light parity). r63e bumps both to uniform `24px`. Visual outcome: compact-mode card gains 8 px on each side. If Stefan wants single-light .container also at 24 px to keep all 3 surfaces matching, that's a one-line follow-up — flag if needed.
- 🐛 fix(P15.6-r63e) **R304 icon-color CSS unified across all paths (PA-0032)**: Stefan PA-0032: "when i use a theme with --active-color set currently the icon changes color to --active-color on firefox with [single-light config], but not for [nested apartment, parallel-mode configs] ... please can you make all icons behave the same? i dont understand why they dont". Root cause: `.single-icon.active` and `.parallel-mindmap-icon.active` in `everyday-light-card.ts` used `var(--paper-item-icon-active-color, var(--state-light-active-color, #f88d2a))` — a 3-level fallback chain that honors HA theme's `--paper-item-icon-active-color` first. But `.tile.on .ic` (group + member icons in `group-layout-expanded.styles.ts`) used `var(--state-light-active-color, #f88d2a)` directly, skipping the theme override layer. Themes that set `--paper-item-icon-active-color` applied to single-light icons but NOT group/member icons. Fix: standardize `.tile.on .ic` on the same 3-level chain. Cross-browser inconsistency Stefan observed (Chrome r63c1 ≠ Firefox) was the r63c entity-RGB-inline-style (cached on Chrome); r63d revert + hard-refresh to r63d1 resolves automatically. Cross-config inconsistency (same browser, different card types) is what r63e CSS unification addresses.
- ⏸️ **R305 deferred** — Stefan's architectural plea "1 implementation" would also need the `icon_color: 'on-state'` config (from r63d) to propagate to `.tile .ic` rendering in `group-layout-expanded.ts`. Not in r63e — needs design pass (per-entity vs per-card, member RGBs ≠ group RGB, embedded-card inheritance). Parked in BACKLOG PA-19 / PA-0032 section.

## 2026-05-11 - P15.6-r63d: R298 revert + opt-in / R300a + R300c / R299 sketch / R294 + R295 close (PA-0031 follow-up cluster)

- 🐛 fix(P15.6-r63d) **R298 icon-color default reverted to themed gold + new `icon_color` config (PA-19 / PA-0031)**: r63c shipped entity-RGB inline-style as the DEFAULT on `.single-icon` + `.parallel-mindmap-icon`, which made the card visually inconsistent with the rest of the dashboard (every other icon is gold). Stefan-Quote PA-0031: "R298 off: I dont understand: why is this icon a different icon than the others? it doesnt make sense (this should be 1 implementation, no?). What you implemented is icon_color_reflects_on_state=true ist geil, ja, aber per default soll es false sein". r63d flips to opt-in via a new config field `icon_color?: 'on-state' | string`. Default `undefined` → no inline style, themed gold CSS applies (matches every other icon). `'on-state'` → entity RGB + brightness-modulated opacity (the r63c behaviour, now opt-in). Any other string → literal CSS color override (`'#ff0000'`, `'rebeccapurple'`, `'var(--my-color)'`). Implementation: shared helper `computeIconStateColor(cfg, isOn, rgb, brightness)` consumed by all 3 render sites (parallel-mindmap-icon at `everyday-light-card.ts:735`, single-icon horizontal at line 1019, single-icon vertical at line 1060). Group-icon and member-icons in the expanded topology still rely on `.tile.on / .tile.off` CSS — the R298 regression was localized to single-light + parallel paths only; if Stefan flags the option should propagate to group/member icons too, that's a follow-up r.
- 🐛 fix(P15.6-r63d) **R300a expanded slider width parity at 60 px (PA-0031)**: Stefan PA-0031: "R300: B width should be 60px as well" (option B = expanded follows compact, locked in PA-0030 review). `group-layout-expanded.styles.ts:32` `--everyday-slider-width: 47px → 60px`. Responsive shrink slope updated in `group-layout-expanded.ts:1161`: start 60 px, -4 px/leaf beyond 5 leaves, floor 40 px. Small groups (Hall 5 leaves, Spots 3 leaves) now render at 60 px in BOTH compact and expanded views — compact↔expanded width parity achieved. Mid-sized groups (Spots-expanded with 7 visible leaves) at 52 px. Apartment view (14 leaves) hits the 40 px floor. The floor was R291's Stefan-tuned "regular width" minimum from PA-14.
- ✨ feat(P15.6-r63d) **R300c new `expand_in_place` GroupConfig option (PA-0031)**: Stefan-Quote PA-0031: "we need to add the option to 'card has the same height and the icon stays in the same place' instead of what i meant with since full_length_sliders=false. yes: '(1) the group icon should sit at the exact same vertical offset from card-top in both compact and expanded' is correct". New `group.expand_in_place?: boolean` field in `GroupConfig`. Default `false` (current behaviour — card grows on expansion). When `true` AND `compact:true` AND `_compactExpanded:true`, the expanded member-sliders shrink so the topology-tree fits within the same total card-height as the compact view → the group-icon Y-from-top stays constant ("expand in place"). Implementation: `EXPAND_IN_PLACE_TOPOLOGY_OVERHEAD = 120` px (empirically-estimated: mindmap-bg + member-tile + group-row + gaps), `effectiveSliderHeight = max(80, 220 - 120) = 100` px when active. Tunable via explicit `slider.height` override. Resolution priority: `slider.height` > `expand_in_place` derived > `full_length_sliders` > default 220. The 120 px overhead and 80 px floor will be tuned post-Stefan-verify.
- 📝 docs(P15.6-r63d) **R299 dynamic-gap sketch landed (PA-0031)**: Stefan PA-0031: "R299 sketching: nicht vergessen!!". New file `docs/notes/R299-dynamic-gaps-sketch.md` with algorithm pseudocode (gap-slot-depth-from-common-ancestor, ratio-weighted-distribution), CSS-var wiring plan (replace static `:host([depth=N])` with inline-style + JS-computed gaps), ResizeObserver lifecycle (`connectedCallback` + `disconnectedCallback` + RAF throttling + cross-card expand-event-bus), 6 test cases (Hall 5-leaf, apartment 14-leaf at various widths, post-Spots-expand 16-leaf), complexity estimate (150-250 LOC + tests), open questions (container-measurement source, event-bus naming, ratio extensibility past depth 3, min-clamp 3 px overflow behaviour). Implementation deferred to **r63e or later** — r63d is a polish round for already-shipped PA-19 items.
- 📚 docs(P15.6-r63d) **R294 + R295 close in BACKLOG (PA-0031)**: Stefan-greenlight: "lets say R294 / R295 are resolved for now". R294 (icon-tap broken on view-switch) — self-resolved after r63b; Stefan reports icon-click works currently. R295 (mindmap detached node) — unable to reproduce across multiple Chrome-MCP probe sessions. Both struck-through in `docs/BACKLOG.md ## PA-19` with notes documenting the close-out hypothesis (R294 = lifecycle race on rapid view-switch, R295 = ResizeObserver re-measurement race). Watching for reappearance.

## 2026-05-11 - P15.6-r63c: R298 (PA-19 icon state-color reflects entity RGB)

- 🐛 fix(P15.6-r63c) **R298 single-icon + parallel-mindmap-icon state-color reflects entity RGB (PA-19 / PA-0030)**: Stefan-Quote 2026-05-11 19:30 CEST: "icon clicking works but it is still not reflecting the state". Live Chrome-MCP measurement on `/ed-slider/singles?cb3=r63b2` showed the icons stuck at a single themed color (`rgb(88, 188, 180)` teal from Stefan's `--paper-item-icon-active-color`) regardless of the entity's actual `rgb_color: [255, 211, 176]`. Root cause: the CSS rules `.caption .single-icon.active`, `.parallel-inline .single-icon.active`, and `.parallel-mindmap-icon.active` all used `var(--paper-item-icon-active-color, var(--state-light-active-color, #f88d2a))` — a themed STATIC color, not the entity's per-state RGB. Mirrored the pattern from `mindmap-path._armStroke` which DOES read `m.rgb` + opacity-modulates by brightness. Computed inline `style="color: rgba(r,g,b,opacity)"` from `stateObj.attributes.rgb_color` + brightness in `everyday-light-card.ts`, wired into all 3 render sites: `.parallel-mindmap-icon` (line 783), `.single-icon` horizontal-row (line 1019), `.single-icon` vertical-default (line 1060). When light is off, no inline color → CSS theming applies (baseline gray dot, .active falls back to themed default). Re-measured: 6 cards across the singles dashboard all show `color: rgba(255, 211, 176, 0.443)` matching the entity's actual RGB + brightness-modulated opacity.

## 2026-05-11 - P15.6-r63b: R297 / R297a (PA-19 mindmap alignment — group-icon-offset + member-Y gate fix)

- 🐛 fix(P15.6-r63b) **R297 parent-group-dot Y alignment (PA-19 / PA-0026 / PA-0029)**: Chrome-MCP live measurement on `/ed-slider/nested` showed all 6 parent-group-dots (everyday_all / back / kitchen / bathroom / hall / everyday_main) sat **uniformly 5 px below their icon centers** — exactly as Stefan predicted in PA-0026 ("bei allen ist es die gleiche höhe"). Root cause: `group-icon-offset="41"` in `group-layout-expanded.ts:1395` was calibrated for the pre-Hue-Wallswitch design where the icon + label took 41 px from bottom; with the current `.tile.group { ic + lbl + 4 gap }` layout the icon-center actually sits 46 px from the bottom. Bumped to `group-icon-offset="46"`. Re-measured: all 6 parents at delta = 0 px. r62's claim that parent-dots were "already aligned" was based on incomplete measurement — only bare-member dots were measured in r62, and the parent issue had to wait for explicit Stefan-flagging in PA-19.
- 🐛 fix(P15.6-r63b) **R297a per-member-dot Y collapse (PA-19 / PA-0029)**: Stefan PA-0029: "jetzt gibt es einzig noch einen bug wo die all → back/Main dots detached sind" + "alle nodes ein bisschen detached bis auf die von Bathroom expanded und main expanded". Chrome-MCP measurement showed every mindmap's member-dots all collapsed to a SINGLE Y (uniform across all members of one mindmap), e.g. apartment's Back-dot at y=563 AND Main-dot at y=563 even though Main's icon is at y=427. Root cause: the gate `useResponsiveCoords && this._memberColHeights.size > 0` in `_renderTopologyTree` (line 1350) checked the ResizeObserver-driven col-height map — which was empty (the outer `.member-col` flex/grid auto-sizes from the child's intrinsic size, so its content-rect height stays at 0 when the embedded card owns its own layout). Meanwhile `_measuredMemberIconYs` was populated correctly with per-entity icon Ys (e.g. apartment: back=469, everyday_main=338) via the direct `getBoundingClientRect` walk in `_measureNestedIconYs`. Widened the gate to `_measuredMemberIconYs.size > 0 || _memberColHeights.size > 0`. The per-entity branch inside the map already prefers measured-Y over the height-formula, so the gate widening is safe. Re-measured: 19 member-dots across 6 mindmaps, all at delta = 0 px.
- ⏸️ **R294 / R298 still investigating.** Stefan reported live repro on `/ed-slider/singles?cb3=r63b1` (Spot 1 cards 1+3 icon unclickable + no state-color). After cache-bust + reload, Chrome-MCP probe shows both cards correctly bound (`boundElClass: 'single-icon active'`, `boundElIsSingleIcon: true`). Conclusion: R294 is a LIFECYCLE bug that triggers AFTER a view-switch, not an initial-render bug. Waiting for Stefan to repro: switch HA dashboard view → switch back → observe broken state with the page still in r63b2 cache.
- ⏸️ **R295 mindmap-detached** could not be reproduced after r63b2 hard-refresh (Chrome-MCP found 0 orphan dots — every member-dot has a corresponding arm). Likely an intermittent race-condition state. Watching for repro.
- ⏸️ **R299 dynamic/relative gaps** parked for r63c after R294/R295 land.

## 2026-05-11 - P15.6-r63a: R288 / R292 / R296-C (PA-19 cluster part 1 — effects-edit-default flip, depth-scaled member-cols gap, HA user_data fallback for saved-colors)

- 🐛 fix(P15.6-r63a) **R288 effects-list edit-mode default flipped to true (PA-19 / PA-0021)**: Stefan PA-19: "btw long press the effects list still doesnt work (it should, please fix)". Long-press → enter-edit-mode flow was already wired in `effects-list-picker.ts:100-117` but gated behind `EffectsPickerConfig.editable` which defaulted to `false` (R230's r49 opt-in decision). None of Stefan's cards had `editable: true` set explicitly, so long-press was a no-op everywhere by design. r63a flips the default: card-class default `@property editable = true`, both read-sites in `everyday-light-card.ts` (lines 642, 816) now use `!== false` so config-explicit `editable: false` still suppresses. Reverts R230 (r49) per Stefan-decision in PA-0021. Long-press on an active effects-list row now enters edit-mode out of the box; second long-press in edit-mode deletes the effect; tap on a grayed row restores it. Tests: 109/109 still passing — no tests asserted on `editable` default.
- 🐛 fix(P15.6-r63a) **R292 depth-scaled member-cols gap (PA-19 / PA-0019)**: Stefan PA-14: "the distance between hall_door and hall_boxes is the same as for hall_door and main_desk ... intra-group should be tighter than inter-group". Pre-r63a `.member-cols { gap: 18px }` was uniform across all nesting depths, so the apartment view's outer Back↔Main boundary (depth 0) had the SAME pixel gap as Hall's hall_door↔hall_boxes_2 siblings (depth 2) — visual hierarchy collapsed. r63a plumbs a new `depth: number` `@property` from `everyday-light-card` → `everyday-group-layout-expanded` (via `.depth=${this.depth + 1}` on every embedded child render). The expanded layout reflects `depth` to attribute; CSS attribute-selectors `:host([depth='0'..='5'])` set `--member-cols-gap` to **28 / 14 / 8 / 4 / 4 / 4 px**. `.member-cols { gap: var(--member-cols-gap, 18px) }` honors the var (18 px fallback for any depth not covered — preserves pre-r63a behavior in test contexts that don't set depth). `TILE_GRID_GAP` in group-layout-expanded.ts is now depth-derived too so the mindmap-arm geometry stays in sync with the CSS gap. Top-level apartment → 28 px outer column boundaries; Back-inner → 14 px between Kitchen/Bathroom/Hall; Kitchen-inner → 8 px between ceiling/counter. Visual hierarchy now matches Stefan's PA-14 spec.
- 🐛 fix(P15.6-r63a) **R296-C HA user_data fallback for saved-colors (PA-19 / PA-0024)**: Stefan PA-0020/PA-0024: "save as temp works now! but the save is not yet persistent ... cant we use the native way colors are saved for the default home assistant light card? how do they do it?". Investigation: existing `effects_picker.source: helper:input_text.<id>` / `saved_colors.source: helper:input_text.<id>` persistence path requires the user to manually create an `input_text` helper per entity — Stefan has none configured for his lights, so saved colors were in-memory only (hard-refresh wiped them). HA itself doesn't natively persist "recent colors" on its more-info-light card, but `frontend/get_user_data` / `frontend/set_user_data` WebSocket endpoints exist for exactly this kind of per-user, server-persisted, cross-device-synced state. Verified live: write `{key: 'everyday_light_card', value: {saved_colors: {...}}}` round-trips correctly with nested ColorEntry[] arrays including kelvin 4-tuples. r63a extends `src/helpers/saved-colors-persistence.ts` with `readSavedColorsFromUserData(hass, entityId)` and `persistSavedColorsToUserData(hass, entityId, colors)` async helpers. Both call sites (`everyday-light-card._onSavedAddCurrent` + `group-layout-expanded._onSavedAddCurrent` / `_syncSavedColorsFromSource`) now fall back to user_data when `cfg.saved_colors?.source` is unset — explicit `helper:input_text` source still wins. Scoping: per-entity for the single-light path; per-group-entity for the group-card path (one palette per group card, matches pre-r63a architecture). Read-on-mount + write-on-mutate, fire-and-forget; the local `_savedColorsState` / `_savedColors` is the immediate render source-of-truth. No HA helper required — saves persist automatically. Stefan can still opt into explicit `helper:input_text` config when he wants cross-user sharing (multi-user HA setups).
- ⏸️ **R294 + R298 + R295 + R297 deferred to r63b.** Stefan-decision in PA-0024: focused Chrome MCP session needed to reproduce R294 icon-tap regression live (PA-0019 hypothesis about `updated()` once-only was wrong — actually fires every render and bindIcon is idempotent), measure R297 K/B/H/M parent-dot vs icon-Y offset (initial Chrome walker hit shadow-DOM depth issues), and identify the orphan entity in R295 mindmap-detached symptom from the screenshot. Splitting these 4 items into r63b means r63a ships pure code-changes with no risky measurement-dependent fixes.

## 2026-05-11 - P15.6-r62: R281 / R287+R286 / R289 / R290 / R291 / R293 (PA-14 cluster — gestures binding, temp-save fallback, popover sizing, slider floor, compact-group mindmap slot, bare-member icon-Y direct-measure)

- 🐛 fix(P15.6-r62) **R289 single-light icon gesture-binding (PA-14)**: `gestures.member_icon` was effectively dead on a non-group, non-parallel single-light card. `updated()` at `src/everyday-light-card.ts:486` only ever wired `_picker.bindIcon(...)` when `default_view_mode === 'parallel'`, so a config like `{entity: light.hall_spot_1, gestures: {member_icon: {double_tap: color_wheel}}}` had no `cursor: pointer` and no double-tap reaction. Stefan PA-14: "in this config clicking the icon doesnt work. no hand appears on hover". Added a second branch: when `cfg.gestures?.member_icon` is truthy, bind the picker to `.single-icon`. Single-light render now also stamps `data-interactive` on the icon when gestures are configured, and CSS adds `cursor: pointer; touch-action: none` for that selector. Reuses the existing `parallel-inline`-variant picker (long-press → wheel/saved popup; double-tap → `_handleDoubleTap` routes to `_picker.openWheel()` etc.).
- 🐛 fix(P15.6-r62) **R281 save-color falls back to color_temp_kelvin attribute (PA-14)**: existing temp-save logic at `everyday-light-card.ts:297` + `group-layout-expanded.ts:1813` keyed solely on `color_mode === 'color_temp'`. Some Hue / Zigbee bulbs report `color_mode: unknown` (or stale non-CT values) immediately after a CT command; group entities aggregate to non-CT even when all members are in CT. Stefan PA-14: "saving a color while the lamp is in temp mode still doesnt save it as temp. it must be saved as temp please". Added a third signal: `color_temp_kelvin > 0` AND `color_mode` is NOT explicitly one of `{hs, rgb, rgbw, rgbww, xy}`. When the lamp definitively says "I'm in HS", we still save as rgb; otherwise (unknown / missing / legacy mode), kelvin presence is the ground-truth save-as-temp indicator.
- 🐛 fix(P15.6-r62) **R287 + R286 parallel-slider popover width (PA-14)**: `group-layout-expanded.ts:1374` used `popupWidth = Math.max(r.width, modes*82+20)`. When the anchor rect's width was the full card width (nested contexts where the captured slider rect spans wide), the popover stretched edge-to-edge — every click inside the card landed inside `.parallel-popup`, defeating the document-level outside-click handler. Stefan PA-14: "to close the parallel slider pop up you have to click outside of the card" + "way too wide (it fits the width of the card)". Dropped the `Math.max(r.width, …)` clamp; popup is now `Math.max(160, modes*82+20)` only (floor for the degenerate 1-mode case). Side-benefit: R286 auto-resolves because the popup is now narrower than the card → outside-the-popup-but-inside-the-card clicks are correctly "outside" again.
- 🐛 fix(P15.6-r62) **R291 slider-width floor 24→40 (PA-14)**: `responsiveSliderWidth` formula at `group-layout-expanded.ts:1114` (introduced in R276 for the apartment view's 14-leaf overflow) was settling at the 24 px floor. Stefan PA-14: "the sliders in this view are way to skinny now ... make them the regular width. it will probably be fine". Bumped floor from 24 → 40 px. New formula: `Math.max(40, 47 - (totalLeafCount - 5) * 3)`. Apartment view now renders leaf-sliders at 40 px; mid-sized groups stay at 41-44 px; small groups still at 47 px. Verified live: `--everyday-slider-width: 40px`, leaf slider rects all 40 px.
- 🐛 fix(P15.6-r62) **R290 compact-group picker mindmap as additional slot (PA-14)**: standalone-compact group config (`{entity: light.hall_spots, name: "Hall (compact)", group: {layout: compact}}`) previously had no mindmap option in its picker — only parallel/saved/wheel/cycle (4 slots), with the embedded-only `useMindmapProvider: () => this.embedded` REPLACING parallel with mindmap. Stefan PA-14: "there should always be the mindmap option in the mode picker (maybe you need to extend the mode picker to be able to handle 6 items now)". Added `additionalMindmap` option to `picker-geometry.ts` PickerLayoutOpts; when set, `mindmap` is APPENDED (not replaced) — 5 slots default, 6 with effects. `getPickerAngleMap` already uses `360/n` so pentagon/hexagon distribution is automatic. Picker-controller forwards `additionalMindmapProvider`; mode-picker passes the flag to `getPickerSlots`/`getPickerAngleMap` AND adds `nth-child(5)/(6)` animation-delay rules for the bloom stagger. Wired via `additionalMindmapProvider: () => this.compact && !this.embedded` in `_compactGroupPicker`. Embedded path is untouched (useMindmap-replacement-of-parallel still wins). `_applyPickerMode` propagates `additionalMindmap` to `pickerDotPosition` so wheel/saved popups bloom from their actual rendered pentagon/hexagon dot positions.
- 🐛 fix(P15.6-r62) **R293 bare-member icon-Y direct-measurement (PA-14)**: Chrome-MCP measurement on the live nested apartment view confirmed that **parent-dots already align perfectly** with their parent icons (Back/Main top-level, Kitchen/Bathroom/Hall sub-level) — Stefan's "Spots icon is bigger may mess up the rest" hypothesis was off. The real bug: **bare leaf-member dots sat 5 px below their icon centers** (hall_door, hall_boxes_2, all bathroom leaves). Root cause: the colHeight-`BARE_ICON_BOTTOM_OFFSET`(=35) formula has ~5 px drift from label line-height + flex gap precision. Stefan PA-14: "The dots for hall door for instance need to sit a bit higher". Extended R277's nested-member direct-measurement pattern to bare members: `_measureNestedIconYs` now also walks `.tile.member .ic` via `getBoundingClientRect` for non-nested cols. `_renderTopologyTree` consumes the measured value for BOTH nested and bare members, falling back to the formula only when DOM isn't ready. Verified live: bare member icons + dots both at y=245 (was: icons 245, dots 250) → perfect alignment, 0 px drift.
- ⏸️ **R292 (intra<inter spacing) deferred to r63.** Chrome-MCP measurement confirmed all hierarchical levels (All-outer, Back-mid, Hall-inner) use the same 18 px col-gap; Stefan's "intra-group should be tighter than inter-group" wants depth-dependent gap scaling AND/OR uniform-pitch-across-depths leaf distribution. Bigger architectural change than the rest of r62 — parked for a dedicated round so it gets the design pass it needs.

## 2026-05-11 - P15.6-r61: R273-R277 (PA-13 — single-light icon + compact-group parity + name/icon honor + uniform leaf-cols + measured icon-Y)

- 🐛 fix(P15.6-r61) **R273 single-light vertical icon (PA-13 Issue 4)**: vertical-orientation single-light render at `src/everyday-light-card.ts:941+` was missing the icon-circle promised by the P47-fix comment at line 899. Only the horizontal-orientation branch (line 919) wired up `<ha-icon class="single-icon">`. Stefan PA-13: "bei `light.hall_spot_1` fehlt das icon". Added `<ha-state-icon class="single-icon ${active}" .stateObj .icon=${iconName}>` inside the `.caption` block, above name and state. Reuses existing `.caption .single-icon` CSS (46×46 gold-stroke disc, mdc-icon-size 28). Icon-precedence chain `config.icon ?? state.attr.icon ?? domain-fallback` was already computed at line 905-908 — wiring the result into the template was the missing step.
- 🐛 fix(P15.6-r61) **R274 compact-group renders like single-light (PA-13 Issue 5)**: Stefan-Quote: "diese config (`hall_spots, group.layout:compact`) muss genauso aussehen wie diese (`hall_spot_1`)". Standalone non-embedded compact-group now matches single-light visually 1:1. Changes:
  - `_renderCompact()` drops the decorative `<compact-mindmap-arm>` SVG (Stefan-confirmed: arm was redundant chrome).
  - State-line moved INSIDE `.tile.group.compact-target` (sibling of `.lbl`) so it inherits the tile's 2 px gap instead of `.layout`'s 12 px gap.
  - `.layout.compact` overrides: `min-height: 0`, `justify-content: flex-start`, `padding: 16px` (matches `.container { padding: 16px }`), `gap: 12px` (matches single-light slider-to-caption spacing).
  - `.tile.group.compact-target .ic` restyled: 46×46 disc, dark-fill `#3a3a52` + 2.6 px gold border (matches `.caption .single-icon` exactly). Drop-shadow on inner glyph cleared.
  - `.compact-slider-el { --everyday-slider-width: 60px; --everyday-slider-height: 220px }` matches single-light defaults (previously 47×260 to fit the compact-tall design-mock).
  - Cross-mode (compact ↔ expanded) icon-Y stability from P42 R16 is intentionally given up — compact-mode now standalone-tile-sized regardless of the expanded view's needs.
- 🐛 fix(P15.6-r61) **R275 honor config.name + config.icon in compact-group (PA-13 Issue 6)**: `light.everyday_all` with top-level `name: Apartment` and `icon: mdi:home-lightbulb` plus `group.layout: compact` rendered the entity friendly_name + registry-icon instead. Root cause: `everyday-light-card.ts` never passed these props to `<everyday-group-layout-expanded>`. Fix wires `.groupName=${cfg.name}` and `.groupIconName=${cfg.icon}` through the template. New `groupName` property on the expanded component. `_groupName()` short-circuits on `this.groupName` before falling back to `deriveGroupName()`. New `_groupIcon(fallback)` method uses `override > state.attr.icon > fallback` precedence — inverse of R150's per-member chain, intentional: user-config-icon for the parent card must win, while per-entity registry icons still take precedence for member tiles (preserved via `resolveEntityIcon`).
- 🐛 fix(P15.6-r61) **R276 uniform leaf-col widths via totalLeafCount-aware slider shrinkage (PA-13 Issues 1+2)**: Stefan PA-13: "die Main group ist immer noch so weit außen, die steht vollkommen alleine da" + "die Gaps zwischen den Hall slidern ist immer noch größer wie die gaps zwischen den slidern der anderen groups". Root cause: pre-R276 `responsiveSliderWidth` shrank based on DIRECT member count `n` (2 for outer All, 3 for Hall, 6 for Main, etc). At the outermost All card, n=2 → slider stayed 47 px. With 14 total leaves at 47 px each + 13 × 18 px gaps = 892 px required, narrower outer cards forced cols into `min-content` mode, breaking fr-distribution (Hall, Main hit min-content overflow at different points → uneven pitches). Fix:
  - Slider shrinkage formula now keyed off `totalLeafCount` (recursive leaf count) not direct `n`: `totalLeafCount <= 5 ? 47 : max(24, 47 - (totalLeafCount - 5) * 3)`. For the apartment view (14 leaves), slider settles at 24 px so 14 × 24 + 13 × 18 = 570 px content fits typical 600 px+ outer widths.
  - Slope softened to `-3 px / leaf` (was `-6 px`) so mid-sized cards (8-10 leaves) shrink gracefully; floor lowered to 24 px (was 28).
  - `.member-col { min-width: 0 }` added so grid items can fall below their content min-width when fr-shares are tighter than min-content — previously the slider's `width: 47px` on `:host` forced each col to be ≥ 47 px wide, defeating the fr-distribution Stefan needs for even pitches.
- 🐛 fix(P15.6-r61) **R278 leaf-count uses HA state for auto-resolved groups**: pre-R278 `countManualMembersLeaves` only descended through the config tree. `light.everyday_main` has `group: { layout: expanded }` and NO `manual_members` (auto-resolves via HA group state at runtime) → counted as 1 even though it renders 6 sliders. Side-effect: outer 8fr/1fr split collapsed Main to ~12% of card width. New `_countLeavesForEntity(id, manualMembers, level)` walks the manual_members config tree AND falls back to `hass.states[id].attributes.entity_id` at every depth. Apartment view now correctly counts Back=8 + Main=6 = 14 leaves; outer template settles at 8fr/6fr; Main and Back stretch proportionally.
- 🐛 fix(P15.6-r61) **R279 embedded property wired through Lit binding (PA-13 follow-up)**: parent's `<everyday-light-card .config=${childConfig}>` direct property assignment does NOT call the card's `setConfig()` method (which is what normally syncs `this.embedded = config.embedded === true`). Without an explicit `.embedded=${true}` binding the embedded card's `:host([embedded]) { width: 100% }` rule never activated — embedded cards rendered at content-width (138 px) instead of stretching to their fr-share (216 px), and Bathroom/Hall/Kitchen visibly diverged from their grid allocations. Fix adds `.embedded=${true}` to the template at the nested-card-render site.
- 🐛 fix(P15.6-r61) **R280 embedded compact slider sizing inherits from parent**: my R274 standalone-compact override `--everyday-slider-width: 60px` leaked through to embedded compact members (`light.kitchen_ceiling`, `light.hall_spots` in the apartment view) → those rendered at 60 px while sibling expanded-leaf sliders were 24 px. Added `--everyday-slider-width: unset` to `:host([embedded]) .layout.compact .compact-slider-el` so embedded compact inherits the parent layout's slider sizing.
- 🐛 fix(P15.6-r61) **R277 direct-measured nested icon-Y (PA-13 Issue 3)**: Stefan PA-13: "die Back bathroom node ist nicht aligned mit dem bathroom icon (hat schonmal funktoiniert, jetzt wieder nicht mehr)". R260's `memberY = colHeight - 41` formula assumes a fixed 14 px label height. R271's `width: 100%` on `:host([embedded])` made embedded cards wider → label-wrap behavior shifted → 41 px offset became wrong for any embedded card whose label wrapped to 2 lines. New `_measureNestedIconYs()` runs in `updated()` after each render, traverses each `.member-col[data-entity]` → embedded `<everyday-light-card>` shadowRoot → `<everyday-group-layout-expanded>` shadowRoot → `.tile.group .ic` (or `.compact-target .ic`), and stores the icon-center-Y relative to `.member-cols` top via `getBoundingClientRect`. Stored map preferred over the offset formula in `_renderTopologyTree`. Bare-string members keep the offset formula (no embedded icon element to query). Self-throttling: only `requestUpdate()` when measured values actually change.
- Resource bumped to `?v=0.9.0-p15-r61`. 109/109 tests passing.

## 2026-05-11 - Untrack zzz-session-logs (dev-repo policy correction)

Stefan-Korrektur 2026-05-11: superpro-light-card-dev ist ein PRIVATER dev-Repo. Release-Public-Repo wird via /publish skill erzeugt (siehe ~/.claude/commands/publish.md). Daher ist es irrelevant ob session-logs in dem dev-repo on-main liegen — sie gehören dazu (analog placemybooking + productivity-v1). Vorher: `.gitignore` hatte `zzz-session-logs/` Zeile → session-logs nie auf main. Diese Korrektur entfernt die Zeile, künftige session-logs werden via /ship oder manuell `git add zzz-session-logs/` tracked.

- Removed `zzz-session-logs/` from `.gitignore` so future session-logs land on main alongside code.
- Existing local zzz-session-logs/ dirs (siehe cairo-v1/lima-v2 workspaces) bleiben workspace-local bis explicit committed.
- /publish skill ignoriert zzz-session-logs/ automatisch (via .releaseignore) → public repo bleibt clean.

## 2026-05-10 - P15: group-layout-expanded code-split (start)

Phase 15 begins the long-deferred code-split of `group-layout-expanded.ts` (2548 LOC monster file). Strategy: extract pure-helper modules first (lowest-risk wins), then migrate expanded-group-tile picker to PickerController (= P14c.2 absorption), park member + compact picker migration for P15.5+ pending multi-target controller decision.

- 🐛 fix(P15.6-r60) **R269 + R270 + R271 + R272 — stretch embedded cards + revert R256/R265 (2026-05-11)**: Stefan's r59 review surfaced 3 issues with strong corrections. (1) **R269 revert R256 horizontal shift** — both the `.group-row { transform: translateX }` HTML shift AND the mindmap-path `_groupX` weighted-midpoint logic reverted. Stefan PA-12: "Die mindmap für Al -> Back / main ist zu weit rechts (immer noch)". All-icon now centered at W/2 unconditionally; asymmetric arms to weighted children dots (Back 0.286W, Main 0.786W) are acceptable. (2) **R270 revert R265 gap** — `.member-cols { gap }` 8 → 18 px (back to default). Stefan PA-12: "R265 — tighter horizontal gap war glaube ich ein fehler". (3) **R271 stretch cards to fill cols** — root-cause fix for the huge horizontal gap between Back's cluster and Main's cluster. Was `.member-cols { justify-items: center }` which kept cols at their content-width, centered in cells, leaving big empty channels between sibling cards. Changed to `justify-items: stretch` so cols fill the grid container width. Combined with new `:host([embedded]) { width: 100% }` on everyday-light-card, every embedded card spans its parent's col fully. Recursive effect: outer apartment's 8fr/6fr cols fill the card, Back's 2fr/3fr/3fr cols fill Back, Hall's 3 equal cols fill Hall, etc. Net result: all 14 leaf-cols in the apartment view settle at ~1/14 of outer card width — sliders evenly distributed with dynamic per-col empty space + the explicit 18 px col-gap. Stefan PA-12: "die slider sollen sich über die ganze breite der Karte verteilen und die Gap dynamisch berechnet wird basierend auf dem verfügbaren Platz". (4) **R272 TILE_GRID_GAP sync** — `const TILE_GRID_GAP = 18` (was 8) to keep the mindmap-path's gap-aware member-X formula matching the CSS gap. 109/109 tests still passing. Resource: `?v=0.9.0-p15-r60`.
- 🐛 fix(P15.6-r59) **R263 + R264 + R265 — group-dot at icon-center + uniform slider widths + tighter col gap (2026-05-11)**: 3 polish items from Stefan's r58 review. (1) **R263 group-dot Y at icon-center** — parent group-dots (Hall/Kitchen/Bathroom/Main, plus the outer All) were rendering at `_H - 10` (responsive default for compact-mindmap-arm geometry) while their group-icons sit at `_H - 41` (= half-icon 23 + 4 gap + 14 label, above the topology bottom). Dots were 31 px BELOW their icons — Stefan PA-11: "der Dot ist zu weit unten. er muss etwas höher damit er mit dem Icon übereinstimmt". Added `groupIconOffset` reactive prop (default 10 for back-compat with compact-arm) and exposed it as `group-icon-offset` HTML attribute on `<everyday-mindmap-path>`. `group-layout-expanded.ts` passes `group-icon-offset="41"` to the topology-bg, so the responsive `_groupY` becomes `_H - 41` = icon-center. Dot circumference (r=23) now perfectly overlaps the .ic circle (46 px diameter). Compact-mindmap-arm keeps offset 10 unchanged (icon-tile's margin-bottom: -33 already aligns there). (2) **R264 uniform slider widths** — embedded inner cards each computed their own `responsiveSliderWidth` based on their member count (Main with 6 members shrank to 41 px; Bathroom/Hall/Kitchen with 2-3 members stayed at 47 px). Stefan PA-11: "die slider von Main sind schmaler als die anderen. Das macht keinen sinn ... per default sollen alle die gleiche breite haben". Fix: when `this.embedded`, skip the override entirely — `--everyday-slider-width` inherits from the outer parent (which uses its own non-embedded slope, or in the apartment view's 2-member-cols case stays at the 47 default). User's explicit `slider.width` config still wins. (3) **R265 tighter col gap** — `.member-cols { gap }` reduced 18 → 8 px so the empty channel between Back's rightmost slider (hall_door) and Main's leftmost slider (main_*) shrinks. Stefan PA-11: "die horizontale lücke zwischen hall door und main desk ist viel zu groß die muss kleiner sein". `TILE_GRID_GAP` const in group-layout-expanded.ts updated to 8 to keep the mindmap-path's gap-aware member-X formula in sync. 109/109 tests still passing. Resource: `?v=0.9.0-p15-r59`.
- 🐛 fix(P15.6-r58) **R259 + R260 + R261 + R262 — revert col bottom-pack + per-member-Y + depth-aware mindmap dots (2026-05-11)**: Stefan's r57 review surfaced 2 regressions + 1 new wish. (1) **R259 revert R258** — `.member-col { justify-content: flex-end; height: 100% }` had packed bare-string sliders (hall_boxes_2, hall_door, kitchen_counter) to col-bottom, breaking top-alignment with sibling sliders. Stefan PA-09: "hall boxes, door und kitchen counter sind nicht mehr top aligned". Removed the flex-end + height:100% rule. (2) **R261 revert host flex-end** — `:host([embedded]) { display: flex; flex-direction: column; justify-content: flex-end }` on `<everyday-light-card>` had packed embedded content to host-bottom (= col-bottom when grid stretched), uniformly flattening all sibling group-icons to the same Y. Stefan PA-09 now wants depth visible: "die höhe der dots für main und back muss unterschiedlich sein, weil Main viel höher ist als Back". Kept the R248 `position:relative + z-index:3` stacking-context fix for long-press hit-testing. (3) **R262 disable grid stretch** — added `align-items: start` to `.member-cols` so cols size to their content-height instead of stretching to row-max. Each card now occupies its intrinsic height at row-top; shallow members (Main, 1 level deep) end higher than deep members (Back, 2-3 levels deep). All sliders top-aligned across siblings (row-top). (4) **R260 per-member-Y mindmap** — new `memberYs?: number[]` prop on `EverydayMindmapPath`; when set, dots and arm end-points for member i use `memberYs[i]` instead of the single `_memberY`. Updated `_bezierD` to accept optional Y-override, `_renderBezierArms` + `_renderCombArms` + member-dots render to read per-index. In `group-layout-expanded.ts`: new ResizeObserver tracks `.member-col` rendered heights (added `data-entity` attr to bare-member cols so the observer can map back to entity_id). On each render, compute `memberYs[i] = colHeight - offset` where offset is 35 for bare (label 14 + half-icon 17 + gap 4) and 41 for nested (label 14 + half-group-icon 23 + gap 4). Auto-reflows when nested children inline-expand (col grows → RO fires → re-render with new memberYs). Standalone non-nested path unaffected (useResponsiveCoords false → memberYs undefined → mindmap-path uses its single _memberY). New private fields `_memberColHeights: Map<string, number>` + `_memberColRO?: ResizeObserver` + `_observedMemberCols: Set<Element>` + `_reobserveMemberCols()` helper called from `updated()`. 109/109 tests still passing. Resource: `?v=0.9.0-p15-r58`.
- 🐛 fix(P15.6-r57) **R255 + R256 + R257 + R258 — weighted mindmap dots + group-icon shift + dots higher + col bottom-pack (2026-05-11)**: Stefan's r56 nested-view re-test: 4 alignment bugs remained. (1) **R255** — outer mindmap dots stayed at equal `(i+0.5)/n × W` distribution while the grid had switched to weighted cols (Back 8fr / Main 6fr), so Back-dot was at 0.25W but Back-icon at 0.286W (col center) — both dots "zu weit links". Fix: new `colWeights?: number[]` prop on `EverydayMindmapPath`; when set, `_memberXs(n)` computes weighted col centers matching the parent's `grid-template-columns`. Wired from `group-layout-expanded.ts`: `.colWeights=${useWeightedCols ? memberWeights : undefined}`. Equal-weight case stays at the legacy `(i+0.5)/n` formula so non-nested standalone views are pixel-identical. (2) **R256** — All-node (group dot + HTML icon) sat at W/2 instead of the visual midpoint between the weighted children. Two-part fix: (a) `EverydayMindmapPath._groupX` responsive default now returns `(memberX[0] + memberX[n-1])/2` when `colWeights` shifts the distribution off-center (otherwise stays at W/2 — back-compat preserved); (b) the `.group-row` in `group-layout-expanded.ts` receives an inline `transform: translateX(midpointPct - 50% %)` so the HTML icon physically shifts to match the SVG dot. With Back 8 / Main 6: midpoint = 0.536W → group-row shifts ~3.6% right of center, icon visibly nudged toward Main. (3) **R257** — member dots vertically "zu tief" relative to the embedded children's group-icons. Geometry walk: in the nested-outer context, _H ≈ topology total height; member-cols-bottom ≈ _H - 94 (after 30px gap + ~64px group-row); embedded sub-card's group-icon center ≈ col-bottom - 41 (≈ _H - 135). Old `_memberY = _H - 110` put dots ~25px BELOW the actual icons. Bumped offset to 140 → `_memberY = _H - 140`, dots now ~5 px above icons (visually merging with the icon circles via stroke-radius overlap). (4) **R258** — vertical alignment broke when one sibling col was taller than another (e.g., Hall with hall_spots expanded inline vs Kitchen with kitchen_ceiling collapsed). The taller col stretched the grid row, but the shorter col's content sat at the TOP of the stretched cell with empty space below → icons at different Y. Root cause: `.member-col` was `display: flex; flex-direction: column;` with no `justify-content`, so flex-children default to start (top). Fix: `.member-col { justify-content: flex-end; height: 100%; }` packs content to col-bottom regardless of stretch. Combined with the existing `:host([embedded]) { justify-content: flex-end }` on the embedded everyday-light-card host, the chain now reliably places ALL sibling group-icons at the same row-bottom = same Y for the outer mindmap dots. New helper `_isWeighted()` in `mindmap-path.ts` skips the responsive-midpoint logic when all weights are equal so standalone equal-cols paths stay at W/2. 109/109 tests still passing. Resource: `?v=0.9.0-p15-r57`.
- 🐛 fix(P15.6-r56) **R253 + R254 — weighted grid columns + responsive mindmap for nested-outer (2026-05-11)**: Stefan's r55 nested-view re-test: long-press + inline-expand now work, but: (1) **R253** layout splits the card 50/50 between Back (8 leaves) and Main (6 leaves) regardless of subtree depth, giving uneven entity density. Fix: new `_memberLeafWeight(id)` recursive counter walks the `manual_members` tree via `memberConfigs`, and the `.member-cols` grid now uses `--member-cols-template` set to `Wfr Wfr ...` per-member weights when nested members are present. Back gets `8fr`, Main gets `6fr` → 57/43 split. Standalone path (all bare members) keeps `repeat(N, 1fr)` so non-nested views are unchanged. New module-scope `countManualMembersLeaves()` helper, `ManualMember` type imported. (2) **R254** — the OUTER card (everyday_all) isn't `embedded` itself but it HAS nested members; r54's "use hardcoded SVG coords for non-embedded path" meant the outer was using calibrated-for-380 coords in a 600+ px topology, so the All-mindmap dot floated at 40% from top while the All-icon sat at 95% from top. New condition `useResponsiveCoords = embedded || hasNestedMembers` switches to responsive defaults for both cases. Combined with R254's revised responsive `_memberY = _H - 110` (compromise between bare-tile 115 and nested-sub-icon 105 cases), the All→Back/Main arms now anchor on the actual icon Y. 109/109 tests passing. Resource: `?v=0.9.0-p15-r56`.
- 🐛 fix(P15.6-r55) **R248 + R249 + R250 + R251 + R252 — embedded card bottom-alignment + long-press z-index (2026-05-11)**: Stefan's r54 nested-view re-test surfaced two related sets of issues, both rooted in CSS layout: (1) **R248** — long-press only fires on bare-string endnodes (boxes, tv, ...), not on embedded parent group-icons (spots, main, ...). Cause: the embedded `<everyday-light-card>` host didn't establish its own stacking context, so the outer's mindmap-path SVG (with its enlarged r51 nested member-radii) intercepted hit-testing for the embedded group-tile area even with `pointer-events: none` on the SVG. (2) **R249-R252** — vertical misalignment of mindmap arms vs actual icon positions. Cause: the embedded `<everyday-light-card>` host has `height: 100%` and stretches to the CSS grid cell, but its INNER content (the `<everyday-group-layout-expanded>` element) has `height: auto` and sits at the TOP of the host with empty space below. Result: each embedded card's group-icon sits at `top + content-height`, NOT at cell-bottom. Cards with less internal content (Bathroom — all bare-string members) have icons HIGHER than cards with more internal content (Hall — has nested hall_spots) — Stefan's "Bathroom-node ist höher als Hall/Kitchen" complaint. Combined fix: `:host([embedded])` on `<everyday-light-card>` now uses `display: flex; flex-direction: column; justify-content: flex-end; position: relative; z-index: 3;` so (a) inner content packs to the BOTTOM of the grid cell (uniform group-icon Y across all siblings in a row, mindmap arms terminate consistently) AND (b) the host element creates its own stacking context above the outer's mindmap SVG (long-press hit-testing wins). New reflected `embedded` boolean property on `EverydayLightCard`, synced in `setConfig` from `config.embedded`. 109/109 tests still passing. Resource: `?v=0.9.0-p15-r55`. Note: the responsive mindmap-path defaults still use `_H - 80` for memberY (member-cols-bottom approx). Per-member DOM measurement deferred to post-launch — current fix puts most icons within ~5-10 px of the dots, which is good enough for the visual demo.
- 🐛 fix(P15.6-r54) **R245 + R246 + R247 — revert top-down + kitchen_ceiling inline + standalone mindmap restored (2026-05-11)**: Stefan rejected r53's top-down conversion ("Whole tree muss bottom-up sein!!") and flagged kitchen_ceiling popup clipping at the left edge with sliders rendering under kitchen_counter. (1) **R245** — removed `icon_position: top` from outer Apartment card in `assets/demo/dashboard.yaml`, removed the forced `icon_position: 'top'` override for embedded children in `group-layout-expanded.ts` nested-render fork. Whole tree now flows bottom-up consistently: each level's group-icon at its own bottom, members above, arms going up. (2) **R246** — `kitchen_ceiling` switched from `expansion_mode: popup` to `expansion_mode: inline` (matches `hall_spots`). Popup mode was clipping at the grid-cell left edge and rendering its sliders under `kitchen_counter`; inline expansion reflows siblings cleanly. (3) **R247** — restored hardcoded `GROUP_DOT_Y = 288 + sliderDelta` and `MEMBER_ICON_CENTER_Y = 195 + sliderDelta` for the STANDALONE non-embedded path (so non-nested views — groups / master / etc — stay pixel-perfect to Stefan's empirical calibration). For embedded cards specifically, the overrides are skipped (`this.embedded ? undefined : ...`) and mindmap-path uses its iconPosition-aware ResizeObserver responsive defaults — embedded cards have variable height and absolute coords drift. Responsive defaults updated: `_memberY` for `iconPosition='bottom'` now uses `_H - GAP_PLUS_GROUP_ROW (80)` so it points at member-cols-bottom (where tiles or embedded group-icons actually live), not the SVG top edge. 109/109 tests passing. Resource: `?v=0.9.0-p15-r54`. Dashboard redeployed.
- 🐛 fix(P15.6-r53) **R243 + R244 — outer iconPosition=top + responsive mindmap coords (2026-05-11)**: Stefan's r52 nested-view re-test: alignment improved BUT outer mindmap still flows bottom-up (All-icon at bottom, arms up) while embedded children flow top-down (Back/Main/Hall/Kitchen/Bathroom at top, arms down). Visual inversion. Plus: when hall_spots / kitchen_ceiling expand inline, the parent's mindmap arms don't reflow to the new child positions. (1) **R243** — outer card in `assets/demo/dashboard.yaml` (Nested view) now sets `group.icon_position: 'top'`. The whole tree reads top-down consistently: Everyday All at top of layout, arms going DOWN to Back + Everyday Main, each of those a top-down sub-tree. (2) **R244** — removed hardcoded `GROUP_DOT_Y = 288 + sliderDelta` / `MEMBER_ICON_CENTER_Y = 195 + sliderDelta` from `group-layout-expanded.ts:872-876`. These were calibrated for ~380 px standalone min-height layouts; embedded children + inline expansion pushed the topology way past those bounds and the dots drifted off the icons. New approach: drop the static overrides and let `mindmap-path` use its ResizeObserver-driven responsive defaults — which now also flip based on a new `icon-position` prop (group near top OR bottom depending on layout). The dots auto-reflow whenever the topology size changes (e.g., when kitchen_ceiling expands inline AND grows its containing Hall card AND grows the outer Back card — the entire mindmap stack updates). New `iconPosition` prop on `EverydayMindmapPath`; `_groupY` + `_memberY` getters now use `RESPONSIVE_PAD + DEFAULT_GROUP_R` (top) / `_H - RESPONSIVE_PAD` (bottom) and mirror image for member-Y. 109/109 tests still passing. Resource: `?v=0.9.0-p15-r53`. Dashboard redeployed to `ed-slider` via `ha_config_set_dashboard`.
- 🐛 fix(P15.6-r52) **R240 + R241 + R242 — embedded iconPosition=top + parent/child Y separation + long-press hit area (2026-05-11)**: Stefan's r51 nested-view re-test surfaced 3 follow-ups all rooted in the OUTER mindmap's hardcoded SVG Y coords (`288`, `195`) being calibrated for ~380 px standalone tile heights while embedded children blow the layout up to 600+ px. (1) **R241/R242** — forced `icon_position: 'top'` for all embedded children in the nested-render fork (`group-layout-expanded.ts:967-988`). Each embedded card now renders its group-icon at the TOP of its own layout. The top of the embedded card is also the top of the outer's member-col (= where the outer's mindmap arm terminates at memberY). Without this, embedded group-icons landed at member-col-bottom AND the outer's group-row sat right after with no visual separation, so All-icon ≈ Main-icon ≈ Back-icon all collapsed to roughly the same screen-Y. The user can still override explicitly via `nestedConfig.group.icon_position` if a card config wants the legacy bottom layout. Topology gap reduced to 12 px for embedded `[icon-position='top']` so the group-icon doesn't float far from its members below. (2) **R240** — defensive CSS: `:host([embedded]) .tile.group { pointer-events: auto; position: relative; z-index: 5; }`. Stefan couldn't trigger long-press on the parent group-icons (all / main / back / etc) in r51. r51's new bigger member-radii (GROUP_R = 23 instead of MEMBER_R = 17 for nested members) made the outer mindmap dots visually overlap the embedded card's group-tile area; even with `pointer-events: none` on the SVG, the raised dots can intercept hit-testing in some browsers. The explicit `pointer-events: auto` + z-index 5 on the embedded group-tile re-anchors hit detection. Should also surface the R238 picker-swap (mindmap glyph instead of parallel) which Stefan couldn't test in r51. 109/109 tests still passing. Resource: `?v=0.9.0-p15-r52`.
- 🐛 fix(P15.6-r51) **R234b + R235 + R236 + R237 + R238 + R239 — nested-view alignment + size + picker overhaul (2026-05-11)**: 6-item bundle from Stefan's testing of the new ed-slider/nested view post-r50. (1) **R235/237** embedded group-cards (hall_spots, kitchen_ceiling, main, etc) used to inherit standalone slider defaults — 47×170 host vars + 260 px compact-slider override + 380 px min-height + 24 px padding — making them taller and wider than sibling member-cols in the parent's grid. New `:host([embedded])` CSS block in `group-layout-expanded.styles.ts` resets all of these to `unset` (slider sizes), `0` (min-height + padding) and reduced gap so the embedded card matches sibling vertical rhythm. New `embedded` reactive property on `EverydayGroupLayoutExpanded` is wired from `everyday-light-card.ts` (passes `.embedded=${cfg.embedded === true}` alongside the existing nested-render fork). Reflected as attribute for CSS targeting. (2) **R237** decorative `compact-mindmap-arm` (the short SVG hint below the compact-slider) now hidden when embedded — the parent layout already draws a mindmap arm leading to this tile, the inner one was visual noise + extra vertical box. (3) **R236** `full_length_sliders` default stays `false` for embedded children — no explicit override needed because the dashboard config doesn't set it true on the nested members; the natural inheritance now produces the expected slider-height (220 px or whatever the parent's effective height resolves to). (4) **R234b** outer-mindmap member-dots now sized GROUP_R (23) instead of MEMBER_R (17) for members that themselves carry nested-group config (have an entry in `memberConfigs` map). New `memberRadii?: number[]` reactive prop on `EverydayMindmapPath` allows per-member-dot radius override. `group-layout-expanded.ts` computes the array at render time: `memberConfigs.has(id) ? 23 : 17`. Result: Stefan's "main is small as child of all but big in its own group" inconsistency resolved — sub-groups now look like groups everywhere they appear. Bare-string members keep MEMBER_R. (5) **R238** new picker slot 'mindmap' for embedded compact-group cards — replaces 'parallel' as the top picker option. Implementation: extended `PickerLayoutOpts` with `useMindmap?: boolean` flag; when set, `getPickerSlots` returns `['mindmap', 'saved', ...wheel + cycle ± effects]` instead of starting with `'parallel'`. New `useMindmapProvider?: () => boolean` option on `PickerController` (provider-pattern, not static boolean, because the embedded state can change after controller construction). New `use-mindmap` boolean attribute on `<everyday-mode-picker>` flows the flag into `getPickerSlots/getPickerAngleMap` calls. The `'mindmap'` PickerMode + icon already existed (P43 R21 / R131 days) — only the picker-slot wiring needed to come online. `_compactGroupPicker` in `EverydayGroupLayoutExpanded` now reads `useMindmapProvider: () => this.embedded` so the swap happens automatically for nested-group cards. (6) **R239** mindmap node alignment fix — consequence of (1): with the embedded slider-heights now matching siblings, the parent's GROUP_DOT_Y derivation (`288 + sliderDelta`) lands the SVG dots on the actual icon positions instead of floating high above. No code change beyond r51's slider-height fix; the mindmap geometry was always correct math, just fed wrong values. 109/109 tests still passing. Resource: `?v=0.9.0-p15-r51`.
- 🐛 fix(P15.6-r50) **R234 + helper-groups + dashboard trim + howto docs (HACS-launch final-polish, 2026-05-11)**: pre-launch tidy across code + HA + docs + demo. (1) **R234 single-light bug** (the load-bearing fix): pointing the card at a member-entity (`entity: light.hall_spot_1`) used to auto-detect the parent zigbee group via the reverse-scan in `resolveGroup` and render the WHOLE group instead of the single light. Stefan-Quote: "the problem currently with `- type: custom:everyday-light-card entity: light.hall_spot_1` is, that it does not show the single light, rather it shows the whole group (which is incorrect) ... if the light is a group (not part of a group) only then it makes sense to show it as a group". Fix: removed behavior 3 (parent-walk) from `src/helpers/group-detection.ts:resolveGroup`. Single members now render as single-light cards. Test in `test/group-detection.test.ts` flipped from "finds the parent group" to "returns null (single-light view)". 109/109 tests still pass. (2) **everyday_* helper groups** added to live HA via new package `assets/demo/ha-config/everyday_groups.yaml` → scp'd to `hassio:/config/packages/everyday_groups.yaml`. Provides `light.everyday_all` (back+main), `everyday_main`, `everyday_hall`, `everyday_kitchen`, `everyday_bathroom`. Reason: the auto-created zigbee `light.all` is a FLAT list of every bulb — useless for topology. The hand-built helpers preserve the apartment-tree hierarchy the nested demo needs. `ha_check_config` passed, `ha_restart` initiated via MCP, all 5 helpers materialised on restart. (3) **Dashboard trim** — `assets/demo/dashboard.yaml` reduced from 5×(2-3 cards) packed-with-overrides to 5×(1-2 cards) using true defaults. View 4 (Nested) is now the centerpiece: full apartment tree rooted at `light.everyday_all` with `manual_members` mirroring the helper structure (kitchen→ceiling popup + counter; bathroom→mirror+wall_1+wall_2; hall→hall_spots inline + boxes_2 + door; main→6 main_* lights). Deployed to `http://192.168.2.231:8123/ed-slider/*` via `ha_config_set_dashboard`. (4) **Howto docs aligned** — `docs/howto/06-house-off-shortcut.md` updated to use `light.everyday_all` (was `light.all`) + tip on `- entity:` vs bare-string member syntax. `docs/howto/09-nested-groups.md` extended with the full helper-groups yaml + real apartment tree. `docs/howto/03-hall-group-tile.md` gained a tip about `everyday_*` naming convention for apartment-wide setups. (5) **Roadmap entries** added to `docs/BACKLOG.md` for `slider.orientation: horizontal` in group view (works on singles, not in expanded-group), `slider.style: mixer` as a 1st-class style across all render paths, effects-list full-expansion, and speaker-mode as a generalised non-light card mode — all parked post-launch per Stefan-Decisions 2026-05-11. (6) **Cache-bumped** resource URL `?v=0.9.0-p15-r50` via `ha_config_set_dashboard_resource(resource_id: ff1c8fcbccb04acbb85ace77296b377f)`. (7) **Session-log structure** — `palmyra/HANDOFF.md` (legacy P0 bootstrap) archived to `zzz-session-logs/2026-05-11-palmyra-launch-prep/_archive/`. RECOVERY.md from cairo-v1 workspace copied into the same session-log dir. Resource: `?v=0.9.0-p15-r50`.
- ✨ feat(P15.6-r49) **R230 + R231 + R232 + R233 — effects-edit disable-default + grid-layout dashboard + real back-lights nested demo + self-deploy via MCP**: 4 items from Round 12 + self-deploy. (1) **R230** effects-list edit-mode is now opt-in (default false). New `effects_picker.editable: boolean` config flag — when unset or false, the popup is a clean pick-only surface (long-press on an active row is a no-op). Gated in `effects-list-picker.ts:_onLongPress` (the only emitter of `enter-edit`). `editable` prop plumbed through both host mount points: `everyday-light-card.ts` (parallel-inline effects popup) reads `this.config.effects_picker?.editable`, and `everyday-light-card.ts` → `<everyday-group-layout-expanded>` passes the same via new `.effectsEditable` reactive property. Stefan-Quote: "lets save the effects list for after launch for now - disable it by default for now". Cross-session storage per-light (4.6 Future Work item) deferred to post-launch — Stefan: "the changed effects list isnt permanent. for the same light it should be stored somewhere permanently so that when you remove the card, the perviously saved effects list is still there". (2) **R231** demo dashboard rewritten to use `type: custom:grid-layout` for every view (HACS card `lovelace-layout-card`, already installed on Stefan's hassio). All 5 views (Singles / Groups / Master / Nested / Speaker) carry the type at view-level so cards reflow into a single-screen grid instead of single-column stack. Stefan-Quote: "okay, das nested ist ein bisschen crazy jetzt. bitte erstelle dashboard immer als type: custom:grid-layout (dann passen auch alle auf eine Karte)". (3) **R232** nested-demo Card 11 rebuilt with Stefan's REAL apartment topology — verified live entity IDs via HA MCP: `light.back → [light.kitchen, light.bathroom, light.hall]`. `light.kitchen` has children `[light.kitchen_counter, light.kitchen_ceiling]` where `light.kitchen_ceiling` is itself a 2-LED group rendered with `expansion_mode: popup`. `light.bathroom` is flat: `[mirror, wall_1, wall_2]`. `light.hall` has `[light.hall_door, light.hall_spots, light.hall_boxes_2]` where `light.hall_spots` is a 3-spot group rendered with `expansion_mode: inline` (collapsed). Per-level `name:` + `icon:` overrides for human-readable labels (Kitchen / Bathroom / Hall / Ceiling / Spots) instead of entity-id slugs. Stefan-Quote: "back -> [kitchen, bathroom, hall] -> [counter, ceiling (expandable with pop up)], [mirror, wall_1, wall_2], [door, spots (inline expandable, collapsed), boxes]". (4) **R233** self-deploy via HA MCP: rebuilt dashboard pushed live to `http://192.168.2.231:8123/ed-slider/*` via `ha_config_set_dashboard(url_path: ed-slider)`. Resource cache-bumped to `?v=0.9.0-p15-r49` (was r7 — long-overdue refresh). Stefan-Quote: "bitte das demo dashboard updaten und bitte per mcp selbst deployen auf http://192.168.2.231:8123/ed-slider/*". 109/109 tests passing. Resource: `?v=0.9.0-p15-r49`.
- ✨ feat(P15.6-r48) **R208 + R227 + R228 + R229 — nested groups MVP + wheel defaults + slider-width fix + cycle config**: 4 items from Round 11. (1) **R208 nested groups MVP** (the big one — Stefan moved from post-launch P21+ to launch-blocker): implemented Approach A (recursive embed) per ADR 0005. New `ManualMember` type extends `manual_members` from `string[]` to `(string | { entity, group, parallel_sliders, ... })[]` — bare strings keep legacy flat-tile behaviour, object-form entries trigger embedded `<everyday-light-card>` rendering with their own per-level config. New `embedded` config flag (auto-set by parent during recursive render) suppresses the outer `<ha-card>` chrome so nested instances blend as member tiles inside the parent grid. `resolveGroup` now returns a `memberConfigs: Map<entity, partial-config>` payload alongside `memberIds`. `group-layout-expanded.ts` member-cols loop checks the map: if a member has nested-config, render `<everyday-light-card embedded>` instead of the slider tile. r48 ships an MVP — outer-mindmap arms still anchor to the member-col edge (not the inner card's group-icon) and per-level pickers can overlap; both polished post-launch (P21.4 + P21.5). Demo Card 11 + howto 09-nested-groups.md walk through Stefan's apartment-master example. ADR 0005 status flipped from Proposed → MVP shipped. Stefan-Quote: "we need to do the docs/adr/0005-nested-groups-architecture.md before launch! we still have time today, tonight and whole tomorrow!". (2) **R227** color-wheel defaults bumped from `12 hues × 4 rings` to `21 × 6` across all 3 read-paths (everyday-light-card.ts host fallback, group-layout-expanded.ts component default, color-wheel.ts component default). Smoother gradient + more saturation steps. Stefan-Quote: "this should be color wheel default: hue_segments: 21, saturation_rings: 6". (3) **R228** slider-width override didn't apply: vertical-pill-slider.ts `:host { --everyday-slider-width: 60px; }` declaration had higher specificity than parent's inherited inline-style override. Replaced with `var(--everyday-slider-width, 60px)` fallback pattern in all derived calcs (--pill-width, --thumb-size, --color-thumb-border, width, horizontal height). Now parent's inline `--everyday-slider-width` cascades through. Stefan-Quote: "i tried this config but the slider width doesnt change". (4) **R229** cycle-mode config: new top-level `cycle: { modes?: SliderMode[] }` config. `_cycleNextMode` in group-layout-expanded.ts checks the user list first; falls back to color_mode-driven heuristic when unset. New wiki section in `docs/wiki/02-config-reference.md` documenting the option + behaviour. Stefan-Quote: "needs a section for 'cycle mode' as well (where you can select which sliders to cycle (brightness, hue, temp, saturation)". 109/109 tests passing. Resource: `?v=0.9.0-p15-r48`. Closed BACKLOG: R210 (state-icon defekt) + R213a (mindmap arms wide groups) — both resolved by Stefan switching to grid-layout-card dashboard type.
- 🐛 fix(P15.6-r46) **R220 + R221 + R222 + R223 + R224 + R225 + R226 — Round 10 parallel-inline picker overhaul**: 7-item bundle from Stefan's testing of `default_view_mode: parallel` Card 2 plus visual editor + effects-popup polish. (1) **R220** effects popup outside-click: `_outsideClickListener` in `group-layout-expanded.ts` now also clears `_effectsTarget`/`_effectsEditMode`/`_effectsPopupActiveOrder` when the click lands outside any popup. Was missing → effects popup stayed open until you re-tapped the icon. Stefan-Quote: "effects list dosent close when you click/ tap somwhere else". (2) **R221 + R222** parallel-inline picker scope: new `'parallel-inline'` PickerVariant with slot list `['saved', 'wheel'] + (hasEffects ? ['effects'] : [])`. Drops the 'cycle' slot (every mode is already visible side-by-side, nothing to cycle) and the 'parallel' slot (already in parallel mode, popup of same modes redundant). everyday-light-card.ts `_picker` switched from `variant: 'group-expanded'` to `variant: 'parallel-inline'`. Stefan-Quotes: "main cozy (4 axis) mode picker contains the 'cycle' icon which makes no sense (and it of course doesnt do anything)" + "main cozy (4 axis) mode picker contains the 'pop up slider' icon / option which also makes no sense". (3) **R223** saved-colors edit-mode in parallel-inline: PickerController now supports edit-mode via `savedEditMode` state + `@enter-edit`/`@add-current`/`@remove-color` handlers in `renderSaved()`. Host (everyday-light-card.ts) wires `onSavedAddCurrent`/`onSavedRemove` callbacks: `_savedColorsState` mutable palette seeded from `cfg.saved_colors.static` or `helper:input_text` source on first edit, mutations persist via `persistSavedColorsToSource` when a helper source is configured. Long-press on a swatch → flips edit-mode → wiggle + delete-X buttons appear; tap + → saves current rgb (or kelvin 4-tuple if entity in color_temp mode, R201 logic). Stefan-Quote: "main cozy (4 axis) edit saved colors doesnt work". (4) **R224** effects popup wiring in parallel-inline: PickerController gained `onEffectsPick?: () => void` option. When defined and user picks 'effects' from the picker, host's handler fires (everyday-light-card.ts: flips `_effectsPopupOpen = true`, reusing the body-portal popup that the `effects_list` double-tap action already opened). Without this option the controller no-ops 'effects' (visible failure preferable to wrong default). Stefan-Quote: "main cozy (4 axis) effects pop up doesnt appear". (5) **R225** visual editor extension: 6 → 12 fields. Added `parallel_modes` (multi-select), `parallel_full_length`, `slider_orientation`, `slider_style`, `slider_height`, `double_tap_action` (single-select with all GestureAction options). `_valueChanged` un-flatten extended for parallel_sliders, slider, and gestures sub-configs. Empty values clean up entire sub-objects so YAML stays terse. Stefan-Quote: "the visual config editor needs more options". (6) **R226** name override on horizontal-pill: code already correct (`label = cfg.name ?? friendly_name ?? entity_id` at line 781, rendered in `.hpill-caption .name`). Likely Stefan saw stale cache OR was reading the slider's value-label. Verify with hard-refresh on `?v=0.9.0-p15-r46`. (No code change beyond cache-bump.) Stefan-Quote: "name override for this item doesnt do anything". 109/109 tests (added 2 new picker-geometry cases for `parallel-inline`). Resource: `?v=0.9.0-p15-r46`.
- 🐛 fix(P15.6-r45) **R201 + R215 + R216 + R217 + R218 + R219 — Round 8/9 testing fixes + full demo dashboard**: 6 items across two testing rounds. (1) **R201-regression** kelvin-save broken for `light.main`: in expanded view the picker has NO 'temp' slot (only `parallel/saved/wheel/cycle`), so `_memberModes['light.main']` defaults to `'brightness'` even when the lamp is in `color_temp` mode (verified `light.main` reports `color_mode: 'color_temp'` + `color_temp_kelvin: 2583`). Save check `_memberModes[target] === 'temperature'` failed → 3-tuple. Fix in `_onSavedAddCurrent`: also accept `state.attributes.color_mode === 'color_temp'` as a temp-mode signal. Stefan-Quote: "R201 funktioniert nicht für 'type: custom:everyday-light-card entity: light.main'". (2) **R215** complete demo dashboard: `assets/demo/dashboard.yaml` rewritten as 4-view multi-view dashboard mirroring all 14 demo cards from `assets/demo/cards/*.yaml` — Singles / Groups / Master+Routine+Effects / Speaker. Card 9 migrated off the deprecated `default_view_mode: effects-picker` to use `gestures.member_icon.double_tap: effects_list` (the post-r43 way to access effects). Cards 13 + 14 (saved-colors / color-wheel standalone) deferred — they require the still-unimplemented `view:` render-mode (R63 backlog). Stefan-Quote: "bitte gib mal ein komplettes dashboard, wo du die ganzen assets/demo/cards/*.yaml mir vorführst (gerne auch mehrere Views)". (3) **R216** 2-item mode-picker swap: `mode-picker.ts` `_renderedOptions()` had a hardcoded LEGACY 2-option early-return for `v === 'group-expanded'` that rendered wheel at 0° and saved at 180°, but the modern hit-detection (`pickerHoverFromPointer`) used the 4-diamond angle-map (saved=0°, wheel=90°). RIGHT pointer (dx>0) visually highlighted wheel-icon while the selected mode came back as saved → swap. Fix: drop the legacy early-return so render and hit-detection share `getPickerAngleMap` source-of-truth. Group-expanded now shows the same modern 4-5 slot picker as member/group-compact. Stefan-Quote: "wo im mode picker nur 2 items sind, verwechselt er rechts und links". (4) **R217** effects-list edit-mode in popup: portal popup in `group-layout-expanded.ts` was missing `@enter-edit` / `@delete-effect` / `@restore-effect` / `@exit-edit` handlers AND had `editMode={false}` hardcoded. Long-press a row emitted `enter-edit` but nobody caught it → "nothing happens". Fix: added `_effectsEditMode` + `_effectsPopupActiveOrder` state, full edit-mode handler set, popup chrome with title + " · edit" suffix + Done button as visual indicator. In edit-mode tap-to-pick keeps the popup OPEN so the user can continue trimming. Outside-click resets edit-mode + active-order back to defaults. (5) **R218** visual-editor manual-members drag-reorder: HA's `entity` selector with `multiple: true` ships add/remove only — no drag affordance. Label promised "drag to reorder", reality didn't match. Fix: changed label to "(order = order-of-addition)". Custom dnd-list widget tracked in BACKLOG R218-followup (post-launch ~2-3h work). Stefan-Quote: "'Manual member entities' in visual editor: 'drag to reorder' doesnt work". (6) **R219** stroke-dasharray DevTools warning: `mindmap-path.ts` `_armDash` returned `''` (empty string, invalid CSS) for the default-arm case; Firefox/Chrome dropped the declaration with "Error in parsing value for 'stroke-dasharray'. Declaration dropped." Fix: return `'none'` instead. Behavior identical (no dash applied), console stays clean. Stefan-Quote: "habe jetzt die devtools da. es wird kein fehler angegeben Error in parsing value for 'stroke-dasharray'. Declaration dropped." Resource: `?v=0.9.0-p15-r45`.
- 🐛 fix(P15.6-r43) **R209 + R214 + R213b — effects popup + steeper slider slope**: 3 launch-blocker fixes shipped together. (1) **R209**: effects-icon in mode-picker now opens a real effects-list portal popup instead of dispatching `hass-more-info`. New `_effectsTarget` state mirrors `_savedColorsTarget` / `_wheelTarget`. Render path adds `.inplace-popup.effects` containing `<everyday-effects-list-picker>` at the picker-dot anchor. `_onEffectPick` fires `light.turn_on effect: <name>` then closes the popup. Outside-click handler also clears `_effectsTarget`. Stefan-Quote: "anstatt die effect liste öffnet sich das standard HA light-card pop up". (2) **R214** (architectural correction): effects-list is now ALWAYS a popup reachable from the mode-picker, never a view-mode. The `default_view_mode: effects-picker` path stays in `everyday-light-card.ts` for back-compat (Card 9 demo) but is deprecated — Stefan-Quote: "effects list ist kein view mode!!! effects list ist ein pop up, dass beim vom mode picker aus aufgerufen wird (analog zu saved colors und color wheel)". (3) **R213b** steeper slider-width slope: r41's `-4 px per added member` was too subtle for Stefan to notice. Bumped to `-6 px` so n=6→41, n=7→35, n=8→29, n=9+→28 (touch-target floor). Visible diff at n=6+. Resource: `?v=0.9.0-p15-r43`.
- ✨ feat(P15.6-r42) **R206 — visual editor minimum-viable**: replaced 51-LOC stub at `src/editor/everyday-light-card-editor.ts` with a functional ha-form-driven editor. Schema-driven 6-field form: entity (light.* or media_player.*), name override, icon override, default_view_mode (slider / parallel / effects-picker), group.layout (auto / compact / expanded), and group.manual_members (multi-entity picker with drag-reorder). Out-of-scope power-user fields (parallel_sliders, gestures, color_wheel, saved_colors, slider styling) stay YAML-only — HA's "Show code editor" toggle round-trips cleanly because the editor preserves unknown config keys via spread on every value-change. Hint banner in the editor explains. Stefan-Quote: "We need an option there to order and or select the lights which are controlled by the light." Closed by the multi-entity ha-form selector. Resource: `?v=0.9.0-p15-r42`.
- 📚 docs(P15.6-r42) **ADR 0005 — nested-in-nested groups (post-launch P21+)**: design proposal for Stefan's R208 ask ("1 everyday-light-card for `light.back` with 3 always-expanded sub-groups, kitchen offers popup for kitchen_ceiling, hall offers popup or inline for hall_spots"). Approach A — recursive `<everyday-light-card>` instantiation with new `embedded` mode + extended `manual_members: Member[]` schema where each Member can be `string` (legacy) OR `{ entity, group, parallel_sliders, ... }` (new). Approach B (single render-tree) considered + rejected. 7 implementation phases sketched (P21.1-P21.7), ~6-8 hours total. Defers post-launch — Tuesday ships flat-groups + visual editor, nested arrives in a follow-up release.
- 🐛 fix(P15.6-r41) **R205 + R207 — off-state thumb hidden + responsive slider-width**: 2 quick polish items. (1) **R205** off-state thumb: when light is `state !== 'on'` AND slider is hue/temp/saturation, thumb hidden entirely. Track still renders so user can drag-to-commit. Brightness/volume thumbs stay visible (the empty fill bar IS the data at fraction=0). Stefan-Quote: "when a light is off the hue, temp and saturation sliders should show NO thumb! brightness slider in 0 position is ok." (2) **R207** wide-group responsive sizing: `--everyday-slider-width` shrinks as member count grows (47 px for N≤5, then -4 px per added member down to a 28 px floor). Mindmap-arms scale automatically. User's explicit `slider.width` config wins. Stefan-Quote: "we restrict the width of the card too much but the slider width tries to stay the same (makes no sense)". Resource: `?v=0.9.0-p15-r41`.
- ✨ feat(P15.6-r39) **R201 — save kelvin in temp mode**: saved-colors palette now supports kelvin entries (4-tuple `[r, g, b, k]` where k is the actual kelvin value 1000..10000). When user is in temperature mode and taps "save current color", the system stores the 4-tuple — rgb is HA's swatch render hint, k is the kelvin to apply on pick. RGB entries (3-tuple) stay legacy-shaped, fully back-compat. On pick: kelvin entries fire `light.turn_on color_temp_kelvin: k` (slider auto-switches to temperature mode); rgb entries fire `rgb_color: [r,g,b]` as before. Stefan-Quote: "saving a color does not work in temp mode. When i save a color in temp mode he saves it as color. he should save it as temp." Closes the kelvin round-trip loss problem (HA's hs→rgb→hs lost ~1% sat per cycle for color-mode saves; kelvin saves now bypass the conversion entirely). New exported type `ColorEntry = ColorTuple | [r,g,b,k]` from `saved-colors-picker.ts`. `saved-colors-persistence` validates `k ∈ [1000, 10000]` on read; out-of-range kelvin falls back to 3-tuple. `savedColorsDiffer` extended to detect kelvin-vs-rgb-with-same-rgb diffs. 6 new tests (107/107 total). Resource: `?v=0.9.0-p15-r39`.
- ✨ feat(P15.6-r38) **R202 + R203 + R204 — picker layout unified + dynamic distribution**: refactored `picker-geometry.ts` from per-variant fixed-angle bands to dynamic-count circular distribution. (1) **R202** all 3 variants (member / group-compact / group-expanded) now share the same 4-diamond default: parallel(top/270°), saved(right/0°), wheel(bottom/90°), cycle(left/180°). Stefan-Quote: "per default the main node's mode picker should contain the full pop up sliders". Group-compact's old mindmap slot dropped — long-press on the group-icon already triggers inline-expand (R131 default). (2) **R203** hit-detection now uses NEAREST-DOT instead of hardcoded angle bands. Pre-r38 the left band returned 'temp' but the picker rendered 'cycle' there → no `.sel` highlight on hover. New approach reads the rendered slot list + angle map (same source as the picker) and picks the nearest by angular distance. Cycle + effects buttons now light up correctly. (3) **R204** 5-item layout uses pentagon angles (270°/342°/54°/126°/198° — 72° intervals from top) instead of cardinals + 315°. Stefan-Quote: "the new effect mode button sits akwardly northeast while all other buttons are straight north, east, south and west" — fixed by uniform redistribution. Backwards-compat: `omitTemp` legacy path on group-expanded keeps the 2-option horizontal (wheel right, saved left). New API: `getPickerSlots(variant, opts)` + `getPickerAngleMap(variant, opts)` exported from picker-geometry. Tests extended: 16 → 25 cases (101/101 total). Resource: `?v=0.9.0-p15-r38`.
- 📚 docs(P17-launch) **demo dashboard + reddit teaser + Tuesday launch checklist**: `assets/demo/dashboard.yaml` is a paste-and-go 8-card view (cards 1, 2, 3, 4, 6, 7, 9, 10) for screenshot capture. `assets/demo/reddit-teaser.md` carries a Stefan-voice draft for r/homeassistant with title + body + voice-check pre-flight. `assets/demo/launch-checklist.md` works backwards from Tuesday morning launch: Sunday verify → Monday screenshots + repo creation + v0.9.0-rc1 tag → Tuesday HACS test-install + reddit post + v1.0.0 tag + HACS-Default PR. Stefan-Decisions §8 status documented inline. No code change.
- 🎨 polish(P15.6-r36) **R193 — mindmap arms style for N>3 (release-blocker)**: `COMB_FALLBACK_THRESHOLD` bumped from 6 → 100 so groups with 6+ members continue rendering the bezier S-curve style (was switching to a vertical-drops + horizontal-trunk "comb" fallback). Stefan tested with `light.main` (a Hue group with 6+ members) and flagged the comb-style as visually inconsistent with the 2/3/4-member look. Bezier scales to higher N fine; only past ~10 members do the arms get visually noisy. The threshold remains a knob via the `fallback-at` attribute for users who want comb on very-large groups (wall-tablet 20-light scenes etc.). Resource: `?v=0.9.0-p15-r36`.
- ✨ feat(P15.6-r35) **R195 + R196 + R197 — UX polish for launch**: 3 mode-picker UX changes shipped together. (1) **R195**: default double-tap action across all variants is now `'cycle_mode'` (was `'color_wheel'`). Member-tile in `group-layout-expanded.ts` calls `_setMemberMode + _cycleNextMode` on double-tap; parallel-inline in `everyday-light-card.ts` reads `cfg.gestures...double_tap ?? 'cycle_mode'`. (2) **R196**: the picker's left-slot is now the dynamic 'cycle' slot — icon shows the NEXT slider mode in the cycle (color_temp light: brightness ↔ temperature; color light: brightness → hue → saturation → brightness). New `currentMode` + `colorMode` props on `<everyday-mode-picker>`; `currentSliderModeProvider` option on PickerController. New `_renderModeIcon` helper renders sun/thermometer/spectrum/saturation glyphs per the resolved next-mode. Picker-pick now dispatches `'cycle'`; host's `_applyPickerMode` advances `_setMemberMode` by one cycle step. (3) **R197**: 5th picker slot for `'effects'` at 315° (top-right between saved and parallel). Conditional on entity having a non-empty `effect_list`. New `hasEffects` prop on `<everyday-mode-picker>` set by PickerController from the entity's state attributes. Picking fires `hass-more-info` (initial impl); future improvement: portal-popup mirroring parallel-inline effects_list. PickerMode union extended in BOTH `mode-picker.ts` AND `group-layout-expanded.ts` (the duplicate type literal that drifted in r33b sync). Resource: `?v=0.9.0-p15-r35`.
- 🐛 fix(P15.6-r34) **R199 + R200a + R200b + R192 — slider polish for launch**: 4 launch-blocker bugs in `vertical-pill-slider.ts`. (1) **R199** color-thumb gap top + right: switched from `box-sizing: content-box + width/height: thumb-size + border` to `box-sizing: border-box + left:0 right:0 + height: pill-width`. With fractional `--everyday-slider-width` (e.g. 47 → thumb-size 37.6 + border 4.7), browsers rounded each component independently giving 1-2 px gap on top + right. New layout pins the outer box to the pill exactly; border thickness consumed from within. (2) **R200a** hue 360° sticky-at-top: removed `Math.min(359, ...)` cap, added `_pinnedAtTop` state flag set on commit-at-top (fraction ≥ 0.999) and consumed by `_currentFraction()` to override hass-derived fraction back to 1.0. Cleared the moment user drags below 0.999. Result: slider stays at top with "360°" label across hass-pushes, even though HA stores hue=0 internally. Stefan-Quote: "auch wenn er auf 359 Grad ist soll er trotzdem 360 Grad anzeigen, weil das einfach Professioneller aussieht". (3) **R200b** saturation drift: `Math.round(cur?.[1] ?? 100)` when reading sat in hue-mode service-call. Pre-r34 used the raw `cur?.[1]` which carried sub-percent precision (e.g. 99.6 from HA's hs→rgb→hs round-trip), and each subsequent hue-drag re-sent the slightly-lower sat → cumulative drift down to ~35% over many cycles. Integer round breaks the cycle. (4) **R192** hue/saturation sliders blank when `color_mode=color_temp`: extended the existing `hideTempThumb` pattern with `hideHueSatThumb` for the inverse case. Track still renders; thumb hidden so the slider doesn't pretend to know a hue/sat value when the light is in kelvin mode (where hs_color is stale or invented). Resource: `?v=0.9.0-p15-r34`.
- 🧪 test(P16-extra) **popup-portal-styles smoke + coverage tooling (8 tests)**: `@vitest/coverage-v8` installed as devDep, `npm run test:coverage` script available. Smoke-tests for `POPUP_PORTAL_STYLES` (non-empty, contains key selectors `.everyday-popup-portal`, `.inplace-popup{,.wheel,.saved}`, `.topology-popup`, `.parallel-popup`, `everyday-inplace-bloom` keyframes, `var(--card-background-color)` for theming, `pointer-events: none` on portal-scope). Covers the load-bearing CSS contract so accidental rule deletion fails CI. **89/89 tests across 7 files. helpers/ at 68.51% line coverage / 93.82% branch coverage.** Remaining 31% is `picker-controller.ts` (Lit ReactiveController, harder to test without Lit-element infra).
- 🧪 test(P16-extra) **gesture-detector coverage (16 tests)**: covers `attachGestures` for tap (quick down+up), tap-cancellation (movement / timeout), long-press (timer-based fire / movement-cancel / lift-before-threshold), press-drag-select (onLongPressMove + onLongPressEnd), long-press-suppresses-tap, double-tap (within doubleTapMs / late falls back to single-tap / no-doubleTap-config), pointer-cancel (clears timer / fires LongPressEnd if active), dispose (removes listeners + clears pending timers), edge-cases (non-primary-button ignored). Uses `vi.useFakeTimers()` for deterministic timing in happy-dom. **Total tests: 81/81 across 6 files.** Core P6 gesture state-machine now property-tested — past-Stefan reported recurring subtle bugs in this layer (tap-vs-long-press race, phantom-clicks); regressions are now caught at test time.
- 📚 docs(P18-cleanup) **legacy doc deprecation headers**: PHASE-PLAN.md, CONCEPT.md, HANDOFF.md now carry deprecation banners pointing to `docs/AUDIT-REFACTOR-PLAN.md` (roadmap), `docs/wiki/` (user-facing concepts), and `zzz-session-logs/` (session handoffs). Files stay as historical records — the headers tell new readers which doc is now authoritative for their question. Rebrand-substitution-note included so legacy `superpro-*` references read as `everyday-*` mentally without rewriting the entire file. No functional change.
- 🧪 test(P16-extra) **saved-colors-persistence coverage (16 tests)**: covers `readSavedColorsFromSource` (static, helper:input_text, missing-helper, invalid-JSON, malformed-tuple-filter, ha_favorites no-op), `persistSavedColorsToSource` (helper write, overflow signal with default max=100 fallback, static no-op), `savedColorsDiffer` (length mismatch, per-channel diff, identity). Total tests now 65/65 across 5 files. P15.4-extracted module now has property-based test coverage.
- ♻️ refactor(P15-Phase-2 r33b) **derive helpers extracted to `helpers/group-derive.ts`**: 5 pure functions lifted out of `group-layout-expanded.ts` — `groupName`, `memberLabel`, `toMindmapMembers`, `isLightOn` (replaces `_isMemberOn` + `_isGroupOn`), `resolveEntityIcon`. Class methods kept as thin wrappers (one-liner delegates) so callsites stay terse. Adds `test/group-derive.test.ts` (15 tests) for property-based coverage of friendly-name fallback, entity_id humanising, RGB+brightness extraction, on-state detection, and icon-resolution chain. group-layout-expanded.ts: 1612 → 1609 LOC. Total tests: 49/49 (was 34). Bundle unchanged. Resource: `?v=0.9.0-p15-r33b`.
- ♻️ refactor(P15-Phase-2 r33a) **static styles extracted to `group-layout-expanded.styles.ts`**: 361 LOC of CSS lifted out of `group-layout-expanded.ts` into a dedicated `.styles.ts` module. Pure `css\`...\`` template, no class-state references, lowest-risk first-extraction in P15-Phase-2. group-layout-expanded.ts: 1970 → 1612 LOC (-358, -18%). Cumulative P15: 2548 (start of phase) → 1612 (-936, -37%). Tests still 34/34, bundle unchanged. Establishes the co-location pattern for future component-level extractions (member-tile, group-tile) — each can ship its template + matching styles together. Resource: `?v=0.9.0-p15-r33a`.
- 🚀 chore(P13-r32) **rebrand `superpro-*` → `everyday-*` (residual cleanup)**: by P15.6 the custom-elements, package.json, manifest.json, README, src/ were already `everyday-*`-branded — but several legacy artifacts still referenced the old name. Cleaned: (1) `hacs.json` "Superpro Light Card" → "Everyday Light Card", filename → `everyday-light-card.js` (HACS reads this for listing). (2) `scripts/sync-to-public.sh` paths + comments + `gh repo create` command updated to `everyday-light-card` repo. (3) `assets/test/mindmap-test.html` was BROKEN — referenced `superpro-mindmap-path` element + `dist/superpro-light-card.js` import that doesn't exist. Now imports the renamed bundle and uses the registered `everyday-mindmap-path` element. (4) `assets/design-mocks/{index,mindmap-geometries-n2-n8}.html` titles + headers. (5) `assets/icons/favourite-7-dot.svg` SVG comment header. (6) `test-fixtures/superpro_test_lights.yaml` → `everyday_test_lights.yaml` (`git mv`) with header rewritten; internal `unique_id: superpro_*` keys intentionally kept (HA-registry persistence keys; renaming would dupe entities in Stefan's running install). (7) `.releaseignore` now ships `docs/wiki/`, `docs/howto/`, `docs/adr/` to public; only `docs/AUDIT-REFACTOR-PLAN.md` + `docs/BACKLOG.md` stay private. Hard-cut rename — no alias for the old element name (Stefan-Decision §8.2 implicit-greenlit, v0.9.0 is pre-public). GitHub repos still need creation (§8.3) before first `npm run release:sync`. Resource: `?v=0.9.0-p13-r32` (no source-code change in src/, bundle unchanged).
- 🧪 test(P16-coverage) **group-toggle (8 tests) + picker-geometry (19 tests)**: second + third test files. `group-toggle.test.ts` covers `groupToggleWithRestore` for: bare-no-entity short-circuit, on→off scene-create+turn-off, on→off failure-tolerance (scene.create throw still turns off), off→on with existing scene, off→on fallback to light.turn_on when scene missing, off→on fallback when scene.turn_on throws. `picker-geometry.test.ts` covers PICKER_ANGLES_BY_VARIANT constants for all 3 variants, pickerDotPosition for member/group-compact/group-expanded, pickerHoverFromPointer dead-zone + reach + 4 cardinal directions per variant + group-expanded half-plane. 34/34 tests across 3 files now passing.
- 🧪 test(P16-bootstrap) **vitest framework + first test file**: vitest 1.6 + happy-dom installed as devDeps. `vitest.config.ts` configured for happy-dom env, `test/**/*.test.ts` glob, v8 coverage. First test: `test/group-detection.test.ts` covers `resolveGroup` for 7 cases — bare entity, single light, HA-native group via `attributes.entity_id`, manual_members override, single-member-rejection (>= 2 invariant), parent-group lookup from member, alternative `group_entities` attribute. 7/7 pass. `npm test` runs the suite. P16 ships unit-test infrastructure; further coverage (gesture-detector, mindmap-path, mode-picker, effects-list-picker, saved-colors-picker, render-router) accretes over future commits.
- 📚 docs(P18-bootstrap) **wiki + howto + ADR documentation suite**: 19 new docs files under `docs/wiki/`, `docs/howto/`, and `docs/adr/`. Wiki: 01-quick-start, 02-config-reference, 03-view-modes, 04-group-layouts, 05-pickers, 06-gestures, 07-theming, 08-troubleshooting. Howto: 01-install-via-hacs through 08-night-mode-routine (skipping 03/04/06 explanations covered in wiki, focusing on apartment-scenario recipes mapped from AUDIT-REFACTOR-PLAN §4.2). ADR: 0001-popup-portal-pattern (body-portal lesson from P41/P46/P47), 0002-mindmap-svg-component (P4 + P15.6 alignment lessons), 0003-gesture-detector-design (PickerController architecture). Plus `INSTALLATION.md` (HACS + manual), `CONTRIBUTING.md` (bug reports, PRs, dev setup), and `docs/README.md` (index). 0004-everyday-rebrand ADR deferred until Stefan-Decision §8.2/§8.3 lands. No code change — pure docs ship.
- 📚 docs(P17-bootstrap) **demo card matrix scaffolded under `assets/demo/`**: README.md indexes all 15 reference scenarios from AUDIT-REFACTOR-PLAN §4.1 (apartment-realistic configs mapping to Stefan's Munich Luisenstraße setup). 12 paste-and-go YAML files in `assets/demo/cards/` covering shipped cards (1-9, 6b) + NEW cards 10 (all-house master) and 15 (morning routine), plus stubs 13/14 (R63 standalone wheel/saved — deferred to P21). Each YAML carries an inline-comment block explaining the real-life situation it answers and the gesture vocabulary. Mock-hass demo site (P17 deliverable) parked until repo-rename + GH Pages decisions land.
- ✨ feat(P15.6-r31) **R163-step2 — Bubble-Card-inspired speaker row v2**: speaker-row layout grew from `[name | slider | − | +]` to `[icon | name+state | slider | − | + | ▶/⏸]`. Adds: (1) `<ha-state-icon>` on the left — entity-registry-resolved icon, falls back to `mdi:speaker`, color/bg shift to active-orange when state=playing. (2) state sub-line under the name — "playing" / "paused" / "idle" / "buffering" / "offline"; hidden when the player is off so off-state cards stay quiet. (3) ▶/⏸ play-pause toggle — same white-pill thumb style as the ± buttons so the button cluster reads as a uniform row of controls. Fires `media_play` or `media_pause` based on current state. Card height bumped 56 → 60 px to accommodate the name+state stack. step-3 deferrals (BACKLOG): source picker (long-press → mode-picker variant), per-speaker theme tinting from artwork, queue / shuffle indicators. Resource: `?v=0.9.0-p15-r31`.
- 🔒 chore(P15.6-r30) **R111 — pre-build lint guard against backtick-in-CSS-comment**: new `scripts/check-css-comments.js` heuristic-scans every `.ts` file under `src/` for `/* ... */` comments INSIDE `css\`...\`` template literals containing a backtick. Wired into `package.json` as `prebuild` so `npm run build` fails fast with a clear error instead of the cryptic `TS1005: ';' expected.`. Recurring TS1442 surface that bit me in r9, r12, r15 — now structurally prevented. Zero false positives on current src/ (17 files scanned). Resource: `?v=0.9.0-p15-r30` (build-tooling-only — no bundle change).
- ✨ feat(P15.6-r29) **R180 — parallel-inline slider sizing matches compact-group semantics**: default parallel-inline slider height bumped 170 → 220 px (matches `full_length_sliders: false` compact-group post-r25). New `parallel_sliders.full_length: true` opts into 260 px (matches compact + full_length:true). Explicit `slider.height` still wins. Stefan-Quote: "die paralell slider sollen die gleichen Maße haben, wie die normalen slider (also full_length_slider). aktuell sind sie zu kurz". Now Cards 6b (parallel-inline) and Card 4-6a (compact-then-expanded) line up in side-by-side dashboard rows. Resource: `?v=0.9.0-p15-r29`.
- 🔥 cleanup(P15.6-r28) **R154 debug-log removal + effects_list double-tap + classic_more_info**: Three small Phase-12-closing items shipped as one atomic commit. (1) **R154**: removed `console.info('[everyday-light-card] _onGroupTap → ...)` debug log from `group-layout-expanded.ts:_onGroupTap`. The log proved the gate-logic worked when Stefan flagged tap-to-on broken on OFF groups; bug was confirmed fixed mid-2026-05-10 (Stefan: "tap-to-on when off geht wieder"). Keeping the log would only ship console noise to end-users. (2) **effects_list double-tap action** for parallel-inline picker: new `_renderEffectsPopup()` renders the effects-list-picker as a centered modal in the body-portal when `gestures.member_icon.double_tap = 'effects_list'` and the entity has a non-empty `effect_list` attribute. Backdrop tap closes (300 ms phantom-click suppression mirrors PickerController). Effect-pick fires `light.turn_on` with `effect=...` and auto-closes. Edit-mode disabled (this is a quick-pick surface, not a list-management surface). (3) **classic_more_info action**: dispatches HA's standard `hass-more-info` event for users who want full entity control on double-tap. Resource: `?v=0.9.0-p15-r28`.

- ♻️ refactor(P15.1) **PORTAL_STYLES extraction**: 297 LOC moved out of `group-layout-expanded.ts` into new `helpers/popup-portal-styles.ts`. Pure CSS literal injected once into `document.head` for body-portal popups (topology, parallel, wheel, saved). Zero behavior change. group-layout-expanded.ts: 2548 → 2251 LOC.
- ♻️ refactor(P15.3) **picker geometry extraction**: `PICKER_ORBIT`, `PICKER_ANGLES_BY_VARIANT`, and `_pickerDotPosition` moved from `group-layout-expanded.ts` static class members into `helpers/picker-geometry.ts` as exported pure constants/function. Co-located with `pickerHoverFromPointer` (P14a). group-layout-expanded.ts: 2251 → 2204 LOC.
- ♻️ refactor(P15.4) **saved-colors persistence helpers**: `_syncSavedColorsFromSource`, `_persistSavedColorsToSource`, `_savedColorsDiffersFrom` extracted into new `helpers/saved-colors-persistence.ts` as pure functions (`readSavedColorsFromSource`, `persistSavedColorsToSource`, `savedColorsDiffer`). Future-reusable when single-entity card grows saved-colors support. group-layout-expanded.ts: 2204 → 2144 LOC. Total P15 reduction: 2548 → 2144 (404 LOC, -16%).
- ✨ refactor(P15.5) **PickerController-Migration: expanded-group-tile (= P14c.2 Absorption)**: Extended `PickerController` with two override hooks: `onTap` (replaces default `light.toggle`) and `onModePicked` (replaces internal wheel/saved popup state with host-controlled callback). Migrated expanded-group-tile picker in `group-layout-expanded.ts` to use the controller — gesture binding, picker overlay rendering, and pickerHover state machine now shared with `everyday-light-card.ts`'s parallel-inline picker. `_bindExpandedGroupGestures()` body shrunk from ~50 LOC of attachGestures setup to 7 LOC of `bindIcon()` calls. Member + compact-group pickers still native (multi-state coordination needs separate plan). group-layout-expanded.ts: 2144 → 2105 LOC. Resource: `?v=0.9.0-p15-r2`.
- 🔥 cleanup(P15.5) **dead code removal**: deleted `_legacyInlineExpandRender()` from group-layout-expanded.ts. 156 LOC of pre-P44 fallback code with no callers since the topology-popup-shipped (Stefan-2026-05-09 P44 R23). Cumulative P15: 2548 → 1939 LOC (-609, -24%).
- ✨ feat(P15.6-r6) **R131 inline-expand mode for compact-group**: NEW `group.expansion_mode: 'inline' | 'popup'` config (default `'inline'`). When the user picks Mindmap from the compact-group picker, the card now transforms IN-PLACE into the N-slider topology view instead of opening a body-portal popup. Sibling dashboard cards reflow, the expanded state is persistable, animations are smoother. Stefan-2026-05-10: "in line expand ist doch besser für dynamisches anpassen der siblings, animationen und so. mit dem popup expand gibt es nur ärger". Popup-expand stays as opt-in (`expansion_mode: 'popup'`). Resource: `?v=0.9.0-p15-r6`.
- 🐛 fix(P15.6-r7) **R133 compact mindmap-node alignment**: groupDot circle now centers ON the icon (was: rendered as a stacked half-circle ABOVE the icon). Fix: extend `.compact-mindmap-arm` height 28→80 px + `margin-bottom: -33` so the SVG groupDot lands exactly at the icon-tile's center, with `overflow: visible` so the circle isn't clipped at SVG bottom. Math: `groupY (responsive) = H - 10`, `icon-center = arm-top + (H + margin) + 23` ⇒ `margin = -33` (independent of H). Result: state-reactive colored stroke acts as the halo around the dark icon-tile.
- ✨ feat(P15.6-r7) **R134 configurable double-tap (Bug 1, parallel-inline)**: parallel-inline picker now reads `gestures.member_icon.double_tap` (or `gestures.group_icon.double_tap`) from card config. Default `'color_wheel'` — opens the wheel popup directly. Supported actions: `'none'`, `'toggle'`, `'toggle_with_restore'`, `'color_wheel'`, `'saved_colors'`. NEW GestureAction value: `'cycle_mode'` (no-op in parallel-inline; reserved for future group-layout-expanded.ts wiring). Stefan-2026-05-10: "soll man wählen können: doppel tip -> color wheel | safed colors | cycle slider | effect list...". Resource: `?v=0.9.0-p15-r7`.
- 🐛 fix(P15.6-r8) **R138 + R139 + mindmap-overflow + Bug 6b**: three related fixes. (1) Compact mindmap-arm: `dotsEnabled=false` removes the small member-dot at slider-bottom (Stefan: "child dot der zum Slider führt wollten wir ja nicht"). (2) `<everyday-mindmap-path>` SVG element now has `overflow="visible"` attribute so the groupDot circle (radius 23) isn't clipped at SVG viewBox-bottom — circle now extends naturally into the icon area for the halo effect. (3) Bug 6b root cause: synthetic-click after double-tap closes the just-opened wheel popup. Fix: PickerController's outside-click handler now suppresses clicks within 300 ms of any popup open (timestamp recorded on every transition to open). New public methods `openWheel()` / `openSaved()` ensure the timestamp is set when the host triggers a popup programmatically. Resource: `?v=0.9.0-p15-r8`.
- 🐛 fix(P15.6-r9) **R142 light-theme icon-bg + R143 wheel-anchored-at-icon**: (1) Compact group `.ic` background changed from `var(--card-background-color, ...)` to `transparent` — the variable resolved to WHITE in light theme and made the icon look like a bright ring; transparent lets the SVG groupDot fill (dark #3a3a52) show through, consistent in any theme. (2) PickerController now captures icon-center on double-tap (was: only on long-press). renderWheel + renderSaved use the captured `_origin` for inline-style fixed-position popup at the icon (transform-centred bloom-from-origin), matching the press-drag-select wheel-popup pattern Stefan wants ("popup over selected item with icon as center"). (3) Recurring TS1442 backtick-in-CSS-comment bug fixed inline. Resource: `?v=0.9.0-p15-r9`.
- 🎨 polish(P15.6-r27) **R186 — Stefan-tuned 8 px margin on compact-mindmap-arm**: Stefan tested manually and found `margin-top: 8px` makes collapsed view match expanded-false. R181's 14 px was too much, r26's 0 was too tight. 8 px = sweet spot, matches default member-col gap. Resource: `?v=0.9.0-p15-r27`.
- 🐛 revert(P15.6-r26) **R185 — walk back R181, restore tight collapsed mindmap**: Stefan: only remaining issue is the COLLAPSED view. Mindmap-arm sat 14 px below slider-bottom (R181 added `margin-top: 14px` to lift the slider higher) — Stefan-Quote: "zu viel abstand zwischen mindmap und dem slider". Priority shifted to group-icon parity between collapsed and expanded-false: removed `margin-top: 14px` so arm sits flush below the slider; collapsed group-icon now lands within ~3 px of expanded-false group-icon position. Tradeoff: slider no longer pinned to card-top (R181's original fix walked back). Resource: `?v=0.9.0-p15-r26`.
- 🐛 fix(P15.6-r25) **R183 + R184 — hue slider 360° wrap + color mapping + false-mode slider 220 px**: 3 issues. (1) **R184 false-mode slider too short post-r24**: `effectiveSliderHeight` default for full_length_sliders=false now resolves to 220 px (was undefined → CSS default 170). Stefan: "false sliders nicht mehr at the top aligned und kürzer". Both compact-expanded AND always-expanded (false) render with 220 px sliders → identical AND closer to compact-reference look. (2) **R183a hue slider 360° wraps to 0°**: when fraction = 1.0 (slider at top), `Math.round(fraction * 360)` produced 360 — HA normalised to 0 — slider snapped from top to bottom on the next hass push. Fix: cap outgoing hue at 359 via `Math.min(359, ...)`. Visually identical to red 0/360. (3) **R183b hue colors mismatched what user clicks**: gradient on `.track.hue` spanned the FULL track (0% bottom = red 0°, 100% top = red 360°), but `fraction` was clamped to the thumb-valid range `[pill-half, height - pill-half]`. Clicking on visual green at gradient-33% produced fraction ≈ 0.27 → 97° (yellow-green). Fix: gradient stops now use calc-based positions inset by `--pill-half` so the rainbow zone exactly matches the thumb-valid Y range. Bottom + top `pill-half` zones show solid red. Click-on-green now maps to ~120° as expected. Stefan-Quote: "klicke auf grün, die farbe wird gelb 60 grad". Resource: `?v=0.9.0-p15-r25`.
- 🐛 fix(P15.6-r24) **R182 — compact-expanded == always-expanded visual parity**: Stefan: "diese beiden konfigurationen müssen genau gleich aussehen" — `group: layout: compact + full_length_sliders: true` (compact-then-expanded) MUST look identical to `group: full_length_sliders: true` (always-expanded). Walks back r17–r23 attempts to align compact-expanded slider top with the compact slider top (R171 / R173 / R177-1). Removed: `cameFromCompact ? ANCHOR_SLIDER_HEIGHT` fallback, `compactExpandedActive` flag, `memberGapDelta`/`groupGapDelta` SVG-Y compensation, `.layout.compact-expanded` CSS rules, `compactExpandedClass` interpolation in template. Both configs now flow through `_renderTopologyTree` with the same gap defaults (topo 30, mc 8) and the same SVG-Y math. R181 (margin-top: 14 on compact-mindmap-arm) stays — only affects compact-collapsed view, not compared by Stefan. Resource: `?v=0.9.0-p15-r24`.
- 🐛 fix(P15.6-r23) **R181 — compact slider too far from top**: Stefan flagged after r22 that ANCHOR fix didn't help, the actual problem was in the COLLAPSED view. The compact slider sat too low from card-top because `.layout.compact` used `gap: 0` between flex children — with flex-end + min-height baseline, no inter-child spacing meant content was bottom-aligned and slack accumulated at the TOP, pushing the slider DOWN visibly. Stefan's experiment: `gap: unset` (= 14 px default) made the slider align right but broke the arm→groupTile margin overlap. Fix: add `margin-top: 14px` to `.layout.compact .compact-mindmap-arm` — adds breathing only between slider and arm, preserves arm's `margin-bottom: -33` overlap with groupTile. Slider top moves UP 14 px without disturbing icon-mindmap geometry. Resource: `?v=0.9.0-p15-r23`.
- 🐛 fix(P15.6-r22) **R176 — false-mode 3 px nudge (single-variable change)**: post-r19 false sliders sat ~3 px above the compact slider top. ANCHOR_SLIDER_HEIGHT 223 → 220 to nudge slider tops 3 px down. Isolated change after r20 revert (lesson: ship one alignment variable at a time). R177-2 (mindmap breathing) + R180 (parallel slider sizing) parked in `docs/BACKLOG.md`. Resource: `?v=0.9.0-p15-r22`.
- 🐛 revert(P15.6-r21) **R178 — revert r20, ship only R177-1 group-Y split delta**: Stefan reported r20 broke layout completely ("nichts stimmt mehr"). r20 stacked 3 simultaneous changes (ANCHOR 223→220 + topology-gap 3→0 + member-col gap 0→6 + 4-way delta change). Reverted r20 in full and re-applied ONLY the safe R177-1 split delta: `memberGapDelta=-8` (mc_gap collapse 8→0) and `groupGapDelta=-35` (mc_gap 8→0 + topo_gap 30→3). r19's CSS gaps unchanged. Group icon now visually centered on SVG groupDot in compact-expanded fullLength=true. Other r19 issues (R176 false 3px overshoot, R177-2 mindmap breathing) deferred. Resource: `?v=0.9.0-p15-r21`.
- 🐛 fix(P15.6-r19) **R173 + R174 + R175 — full_length=false top-align, true 3px nudge, mindmap-icon alignment**: Stefan's r18 side-by-side caught 3 micro-issues. (1) **R173 false-mode child sliders too short**: post-r18 false used 170 px default — child sliders ended far below where compact slider top was. Stefan-Spec: false should keep group-icon anchored AND slider top aligned with compact slider top. With same card height, expanded below-slider must equal compact's (= 117 px). expanded default below-slider = 154 px → slider must shrink by 37 → 260 - 37 = 223 px. New `ANCHOR_SLIDER_HEIGHT = 223` constant used as 3rd fallback in `effectiveSliderHeight` resolution (after `sliderHeight` prop and `fullLengthSliders=true`). (2) **R174 true-mode 3px overshoot**: r18 collapsed `.topology` gap from 30→0 — too aggressive. Sliders sat 3 px above the target. Fix: `.compact-expanded .topology { gap: 3px }` instead of 0, restoring ~3 px of below-slider content. (3) **R175 member-icon mindmap misalignment in true-mode**: R171 collapsed `.member-col` gap from 8→0, pushing the HTML member-tile UP by 8 px relative to its slider. SVG `MEMBER_ICON_CENTER_Y` was still computed assuming gap=8 → mindmap arm-tip landed 8 px below the actual icon. Fix: `gapCollapseDelta = -8` applied to both `MEMBER_ICON_CENTER_Y` and `GROUP_DOT_Y` when in compact-expanded fullLength=true mode, so the SVG dot positions match the gap-collapsed HTML layout. Resource: `?v=0.9.0-p15-r19`.
- 🐛 fix(P15.6-r18) **R171 + R172 — top-aligned expanded sliders + full_length_sliders explicit semantics**: Stefan caught two issues with r17. (1) **R171 expanded sliders slightly higher than compact reference** when full_length_sliders=true: expanded below-slider content (member-tile + topology-gap + group-row) is ~38 px taller than compact's (mindmap-arm + group-tile). With flex-end + same card height, expanded slider tops shifted UP by 38 px. Fix: new `.compact-expanded` modifier on `.layout` collapses topology-gap (30→0) and member-col-gap (8→0) so the below-slider heights match. Only applied when fullLengthSliders=true since that's the path that wants top-alignment. (2) **R172 full_length_sliders had no observable effect** post-r17: my r17 cameFromCompact fallback always set 260 px in compact-expanded mode regardless of the flag. Stefan-Quote: "die Option full_length_sliders: false/true macht keinen Unterschied". Fix: removed the cameFromCompact fallback. Now: `true` → 260 px (match compact slider, group-icon SHIFTS), `false` (default) → 170 px (default, group-icon stays anchored). This walks back r17's R170 default-to-260; users who want long sliders must set the option explicitly. Stefan-Quote: "full_length_slider=false heißt übergeordnetes Icon bleibt auch nach der expansion an der selben stelle". Resource: `?v=0.9.0-p15-r18`.
- 🐛 fix(P15.6-r17) **R170 — compact-expand child sliders too short**: when the user expands a compact-group inline (long-press compact group icon → Mindmap option), the child sliders should land at the SAME top-Y as the compact main slider so they sit "oben bündig". Pre-r17 child sliders defaulted to 170 px, the compact main slider is 260 px → child slider tops were 90 px below the compact slider's top after expand. Fix: `effectiveSliderHeight` resolution adds a 4th fallback step — when `compact && _compactExpanded` and no explicit override, default to 260 px (match compact slider's height). The R169 mindmap-Y compensation logic flows automatically since it reads `effectiveSliderHeight`. User can still set `slider.height` or `full_length_sliders: false` to override. Resource: `?v=0.9.0-p15-r17`.
- 🐛 fix(P15.6-r16) **R169 — mindmap groupDot off-center when full_length_sliders=true**: Stefan's r15 verify caught a regression from the R166 fix. The `_renderTopologyTree` Y constants (`GROUP_DOT_Y = 288`, `MEMBER_ICON_CENTER_Y = 195` for icon-position 'bottom') were empirically calibrated for slider-height = 170 px. When `fullLengthSliders` (or an explicit `slider.height`) pushes sliders to e.g. 260 px, the actual icon positions in the HTML layout shift downward by `delta = effectiveSliderHeight - 170` pixels but the SVG dot coords stayed at 288/195 — mindmap groupDot rendered too high above the group icon. Fixed by computing `sliderDelta = (effectiveSliderHeight ?? 170) - 170` and shifting `GROUP_DOT_Y` and `MEMBER_ICON_CENTER_Y` accordingly. Top-icon-position keeps `GROUP_DOT_Y = 30` (group-row is at the top, doesn't shift) but `MEMBER_ICON_CENTER_Y` still shifts since member-tiles sit below the (taller) sliders. Resource: `?v=0.9.0-p15-r16`.
- 🎨 polish(P15.6-r15) **R163-step1 + R164 + R165 + R166 + R167 + R168 — speaker-card redesign step-1 + theme polish + demo dashboard fill**: post-bug-bundle polish round. (1) **R164 mindmap arms theme-aware**: `FAINT_GRAY` constant in `mindmap-path.ts` was a fixed `rgba(255, 255, 255, 0.18)` — invisible on light themes (white-on-white). Now uses `var(--disabled-color, rgba(150, 150, 150, 0.55))` so arms stay visible across themes. (2) **R165 outer slider drop-shadow removed**: the heavy 4px/14px outer shadows looked wrong on the default HA theme's light surface. The vars `--everyday-slider-shadow-y1/y2` are deprecated; the new `--everyday-slider-outer-shadow` var lets paris-theme (or any theme) opt back in by setting a full box-shadow value. Default = `none`. (3) **R166 `full_length_sliders` now applies to inline-expand**: pre-r15 the prop only affected the topology-popup. `_renderTopologyTree` now uses an `effectiveSliderHeight` resolved as `sliderHeight ?? (fullLengthSliders ? 260 : undefined)` so Card 6a (`group.layout: expanded`) finally honours the flag. (4) **R167 speaker-row card height**: when the speaker card sat in a `horizontal-stack` row alongside the tall effects-list, it inherited the row-max height through the global `ha-card { height: 100% }` rule. Added `height: auto; flex: 0 0 auto;` to `.speaker-row-card` so the card hugs its 56 px content. (5) **R163-step1 +/- buttons as brightness-thumb pills**: the −/+ buttons now match the brightness-slider thumb visual — white pill (32×22 px, 11 px radius), dark text, drop-shadow. Stefan: "aussehen wie der Thumb des brightness sliders". Hover/active scale-transitions added for tactile feedback. (6) **R168 demo dashboard speaker row**: `dashboard_testbraoard` view 13 ('sp slider') now has a horizontal-stack with all 5 of Stefan's apartment speakers (sofa, regal, hall_center, kitchen, bathroom) using the mixer-fader speaker-row layout. Card 6 also got `full_length_sliders: true` so Stefan can verify R166 visually. Resource: `?v=0.9.0-p15-r15`.
- 🐛 fix(P15.6-r14) **R159 — saved-color tap silently dropped on parallel-inline + group-tile pickers**: PickerController._onSavedPick read `ev.detail.rgb` (an `[r,g,b]` array shape), but `saved-colors-picker.ts` dispatches `color-pick` with `{r, g, b}` (matching the wheel's event shape). Result: every tap on a saved color in the controller-managed popup (Cards 2/3/6b) silently no-op'd. Host body-portal popup (Card 6a) was unaffected because group-layout-expanded.ts's `_onSavedColorPick` correctly destructures `{r, g, b}`. Fixed by reading `{r, g, b}` from detail with type-guard. Resource: `?v=0.9.0-p15-r14`.
- 🐛 fix(P15.6-r13) **R155 + R156 + R157 + R158 — popup unification + entity-icon resolution**: Stefan's r12 verify exposed 4 deeper issues. (1) **R157 unified popup styling**: PickerController.renderWheel/renderSaved now use the SAME `.inplace-popup wheel` / `.inplace-popup saved` CSS classes from `popup-portal-styles.ts` that the host body-portal popups in `group-layout-expanded.ts` use. Inline styles reduced to position only — visuals (transparent wheel background, max-width 280 saved, bloom animation) come from the shared CSS. parallel-inline (Card 6b) now mounts into the SAME `everyday-popup-portal` class so styles apply uniformly. The r12-injected per-card `PARALLEL_PICKER_PORTAL_STYLES` deleted; both card types inject `POPUP_PORTAL_STYLES` once via id-based dedup. Stefan's "1 implementation für alle" directive now covers popup-styling too. (2) **R155 saved-colors data**: PickerController gains `savedColorsProvider` option. parallel-inline reads from `cfg.saved_colors.static` when set; falls back to `DEFAULT_PARALLEL_SAVED_COLORS` (8-entry palette mirroring `group-layout-expanded.ts`). The popup is no longer empty after long-press → drag-to-saved or double-tap → saved. (3) **R156 dot-anchor for press-drag-pick**: when a mode-pick comes from `onLongPressEnd` (drag-select), `_applyMode` offsets `_origin` via `pickerDotPosition(mode, _origin, variant)` BEFORE setting wheel/saved open. Popup blooms from the dot the user released on, matching Stefan's preference: "die mitte des color wheels über dem mode picker item erscheint (so wie bei 6a)". Double-tap path keeps icon-center anchor (no `_applyMode` invocation). (4) **R158 entity-registry icon resolution**: `<ha-icon icon=...>` only reads `state.attributes.icon` (integration-set), missing user-customised icons set via HA's Customize UI (stored in entity-registry). Replaced with `<ha-state-icon .stateObj=... .icon=fallback>` which does HA's full icon-resolution chain — entity-registry first, then state attribute, then domain default. Applied to compact + expanded group-tile, member-tile, AND parallel-mindmap-icon. hall_spots etc. with custom HA icons now show correctly across every card type. Resource: `?v=0.9.0-p15-r13`.
- 🐛 fix(P15.6-r12) **R148 body-portal for parallel-inline + R152 wheel-on-icon for member double-tap + R154 tap-to-on debug**: (1) **R148 root cause = transform-ancestor clipping**. Diagnostic logs confirmed: PickerController state was correct (`savedOpen: true, origin: {x: 1215.98, y: 534.4}`) but the popup div mounted in `everyday-light-card.ts`'s shadow root was clipped or repositioned by HA's transform on a wrapping ancestor. Fix: parallel-inline now mounts wheel + saved popups in a body-portal (same pattern `group-layout-expanded.ts` uses for its popups). New `_pickerPortal` div appended to `document.body` in `connectedCallback`, removed in `disconnectedCallback`, and Lit's imperative `render()` is called on it from `updated()` so the popups land outside any transformed ancestor. Inline `${this._picker.renderWheel()}${this._picker.renderSaved()}` removed from `<ha-card>` template. (2) **R152 — member-tile (Card 6a) double-tap wheel anchor at icon-center**, not at the wheel-dot's orbit position. Stefan: "soll das wheel mittig zentrieren auf dem child icon (aktuell ist es ein bisschen darunter)". The dot-anchor pattern only makes sense when the user explicitly picked the wheel option from the picker overlay (long-press → drag-to-wheel-dot); double-tap skips the picker so anchoring at the dot looks visually wrong. Member-onDoubleTap now sets `_wheelTarget` + `_popupOrigin` directly to the icon-center, bypassing `_applyPickerMode` → `pickerDotPosition`. (3) **R154 diagnostic** for tap-to-on broken when group is OFF (Cards 1, 4, 5, 6a). console.info added to `_onGroupTap` showing groupState, _wheelTarget, _savedColorsTarget so Stefan can verify the gate isn't short-circuiting. R148 debug logs removed (now resolved). Resource: `?v=0.9.0-p15-r12`.
- 🐛 fix(P15.6-r11) **R150 entity icons + R148 popup-debug logs**: (1) Group-layout-expanded.ts now reads each entity's `attributes.icon` via new `_entityIcon()` helper. Group-tile (compact + expanded) replaced hardcoded `mdi:ceiling-light` SVG path with `<ha-icon icon=...>` reading from group entity. Member-tile uses per-entity icon (each member's `attributes.icon`) instead of the shared `memberIconName` override. The `memberIconName`/`groupIconName` props now act as fallbacks when an entity has no registered icon. Stefan: "korrekte icon für hall_spots wird nur auf karte 6b angezeigt, nicht aber auf 1, 4, 5 und 6a". Cards 1 and 6b were already entity-icon-aware (everyday-light-card.ts); Cards 4/5/6a now follow suit. (2) Console.info logs added to PickerController._applyMode and host._applyPickerMode + _renderPopupPortal so Stefan can share the trace if the wheel/saved popup still fails to appear after long-press → mode-pick. The logs surface origin coords, mode, variant, popupOrigin, and portal connection status. Will be removed once R148 root cause is identified. Resource: `?v=0.9.0-p15-r11`.
- 🐛 fix(P15.6-r10) **R145 + R147 — gesture rebind, origin element, cursor, member double-tap**: four issues from Stefan's r9 verification round. (1) **R145b root cause — `bindIcon` is now idempotent**. Lit's `updated()` fires on every render, including the re-render triggered by the controller's own `host.requestUpdate()` inside `onLongPress`. Without a guard the gesture-detector listeners were torn down MID-gesture and press-drag-select silently broke for parallel-inline (Card 6b) — the user could long-press to open the picker but pointermove/pointerup events fired on freshly-rebound listeners that didn't know about the in-flight gesture. New `_boundEl` field skips the dispose+rebind cycle when called with the same element. (2) **R145b origin element fix** for Cards 2/3 (expanded-group, compact-group). `_origin` for press-drag-select is now captured from the `.ic` child element when present (the actual icon circle) instead of the bound tile. Tiles include `.ic` + `.lbl` underneath, so tile-center sat ~10 px below icon-center — enough offset to skew angle bands in the 4-option group-compact and member pickers. Helper `captureOrigin()` consolidates origin capture across long-press AND double-tap paths. Fallback to bound element when no `.ic` child (parallel-mindmap-icon = ha-icon is already the icon itself). (3) **R145a cursor**: `bindIcon` imperatively pins `el.style.cursor = 'pointer'` — some shadow-DOM trees (ha-icon, SVG glyphs) don't reliably inherit cursor from CSS-defined ancestors. (4) **R147 — member-tile (Card 6a) double-tap default = color-wheel**. Previously hardcoded to cycle slider-mode (R134-era); now opens the wheel popup in the body-portal anchored at the icon's `.ic` rect, matching parallel-inline (Card 6b) default. Routed through `_applyPickerMode('wheel', ...)` so the popup uses the same body-portal machinery as long-press → wheel-pick. Bonus: `onLongPressEnd` no longer clears `_origin` — the wheel/saved popup that opens after a press-drag-select pick now has the icon-center available for inline anchoring. Resource: `?v=0.9.0-p15-r10`.
- ✨ refactor(P15.6) **PickerController-Migration: member + compact-group + portal-tile pickers**: completes Stefan's "1 implementation für alle" directive — every picker site (parallel-inline, expanded-group, compact-group, member-tiles, body-portal-topology member-tiles) now uses `PickerController`. Extended controller with `onPickerOpen` (cross-controller coordination, single-active-picker invariant) and `onDoubleTap` (opt-in slider-mode-cycle). Per-member controllers live in `Map<entityId, PickerController>` rebuilt on memberIds change via `removeController()` + `addController()`. Portal-tile binding swaps each member-controller's bound element shadow → portal on popup-open and back on popup-close. Removed obsolete host state (`_activePicker`, `_pickerHover`, `_pickerOrigin`, `_gestureCleanups`, `_compactGestureDispose`) and dead method `_onModePick` (2-step click flow handled internally by controllers). group-layout-expanded.ts: 1939 → 1825 LOC. Cumulative P15: 2548 → 1825 LOC (-723, -28%). Resource: `?v=0.9.0-p15-r4`.

## 2026-05-09 - P12: phantom-phase closing + bug fixes + version 0.9.0

Closes the unplanned r5-r15 polish iterations + P38.1 effects-list-picker MVP under an explicit phase identity. Includes the comprehensive audit/refactor/documentation plan for the v1.0.0 launch (`docs/AUDIT-REFACTOR-PLAN.md`).

- 🔧 config: version 0.0.1 → 0.9.0 in `manifest.json` + `package.json`. Reconciles HACS-listed version with source `VERSION` constant. v1.0.0 reserved for the post-rebrand HACS launch.
- 📚 docs: `docs/AUDIT-REFACTOR-PLAN.md` — IST/SOLL gap analysis, P12-P20 phase plan, demo card matrix (15 apartment-realistic configs), test plan, documentation structure, mindmap reference.
- ✨ feat(P38.1) **effects-list-picker MVP**: new `everyday-effects-list-picker` component with default-effects-population, long-press-to-remove (in edit-mode), tap-to-restore from grayed-out section, no wiggle, tap-outside-row exits edit-mode. Wired through `default_view_mode: 'effects-picker'` on the host card. State stays in-memory for MVP — helper:input_text persistence lands in P12 follow-up.
- 🎨 ui(post-P47 r5-r15) **slider-geometry polish**: color-thumb (hue/temp/sat) bottom now flush with pill-bottom at fill-frac=0 (no gap). Saturation min-clamp = 1 in service-call (slider DISPLAY still shows 0). Temperature thumb hidden when light is in non-color-temp mode. Reverts: Card 1 single-entity back to simple P3 layout (mindmap-arms reserved for parallel-inline N≥2).
- 🐛 fix(P8) **portal-gesture data-portal-bound flag**: `_bindPortalTopologyGestures` now skips already-bound tiles via `[data-portal-bound]` attribute, so live re-renders during press-drag-select don't reset gesture state. Cleared on teardown.
- ✨ feat(post-P47) **horizontal pill row layout**: `slider.orientation: horizontal` on light entity now renders `[icon | slider | name+state]` row layout (mirrors speaker-row mixer card without the buttons). New `.hpill-card` class.
- 🎨 ui **mindmap-path groupDot split**: new `groupDotEnabled` prop on `everyday-mindmap-path` decoupling group-halo from member-dots. Group-layout-expanded passes `groupDotEnabled=true` always; `dotsEnabled` continues to control member-dots (renamed conceptually).
- 🎨 ui **default-parallel priority hoist**: `default_view_mode: parallel` now takes priority over group-detection. Works for group entities (service calls propagate to members) — see Card 6b in demo dashboard.
- 🐛 fix(post-P47) **outside-click guards**: topology-popup outside-click now respects `inPopup` (wheel/saved popups) AND `inParallelPopup` so opening a sub-popup from inside the topology popup doesn't close the topology underneath.
- 🎨 ui **topology-popup transparent background**: Stefan-Decision: popup-card chrome is invisible; sliders + tiles render solid over the underlying card/wallpaper.
- 🐛 fix **build warnings cleared**: removed backticks from CSS comments in `vertical-pill-slider.ts` (TS1005/TS1442).
- 🎨 ui(post-P47) **wheel + saved popup z-index**: bumped `.inplace-popup` z-index 99 → 200 so wheel/saved always stack above topology + parallel popups.
- 🎨 ui(P38.1) **effects-list-picker R65/R66/R68/R75**: hidden effects only render in edit-mode (R65); no wiggle (R66); no Done button (R68); long-press to remove only in edit-mode (R75).
- 🚀 deploy: cache-bumped to `?v=0.8.5-p38_1-r4` on test instance. Demo dashboard updated with 9 cards (`light.hall_spot_1`, `light.hall_spots`, `media_player.sofa`, `light.main_heater_2`).

## 2026-05-09 - P47: portal gesture-binding + popup padding + parallel-popup labels above + default-parallel render-mode

- 🐛 fix(P6/P8) **R29**: press-drag-select gesture now works on member tiles inside the topology-popup. Stefan-2026-05-09: "in dem pop up muss auch die Press-drag-select gesture funktionieren". Root cause: `_rebindMemberGestures` queries `.tile.member` via shadow-root @queryAll — tiles in the body-portal aren't reachable. New `_bindPortalTopologyGestures()` runs after every `_renderPopupPortal()` and attaches the same onTap/onDoubleTap/onLongPress handlers to portal-tiles. Cleanup keys are prefixed `portal:` so they don't collide with shadow-tile cleanups.
- 🎨 ui(P8) **R30**: `.topology-popup-card .layout` padding now `111px 24px 24px` (was 24/24/20). Stefan's measured value to align the popup's group-icon-y with the original card's group-icon-y. Inline-expand `.layout` keeps the original 24/24/20 — the popup variant compensates for the `translateY(-100%)` bottom-anchor model with extra top-padding.
- 🎨 ui(P6) **R31a**: parallel-popup labels now render ABOVE each slider (was: below). Stefan-2026-05-09: "es soll aber auf dem unteren Ende von dem slider sein (popup slider und default slider aligned)". Slider-bottom now sits at popup-bottom = anchor-y; labels float above each slider in the column. Without labels, the column is just a slider stack.
- ✨ feat(P6) **R31b**: NEW `parallel_sliders.show_labels: boolean` config (default true). Hide per-axis labels for a minimal aesthetic.
- 🎨 ui(P6) **R31c**: parallel-popup sliders now inherit the same height as the topology member-sliders. `full_length_sliders: false` (default) → 170 px (matches inline-expand member sliders); `full_length_sliders: true` → 260 px (matches host compact slider). Stefan: "Das pop up soll aussehen als hätten sie den default slider ersetzt". Implementation: dynamic inline `--everyday-light-card-height` based on the same flag.
- ✨ feat **R31d**: Q4 decision implemented — render-mode-system for default-parallel. NEW config `default_view_mode: 'collapsed' | 'parallel'` (default 'collapsed'). When 'parallel' AND domain is light, the single-entity card renders N parallel sliders inline (no popup). Modes + labels driven by the same `parallel_sliders` config as the popup. Stefan: "Es soll auch eine 'per default paralell' sliders geben (das ist dann wieder ohne popup gelöst)". Group-context default-parallel comes in P48.
- 🔧 config: VERSION 0.8.4 → 0.8.5, resource `?v=0.8.5-p47`.

## 2026-05-09 - P46: scroll-aware popups + bottom-anchor (main-node-icon)

- 🐛 fix(P8) **R27**: anchored popups now scroll with the page. Stefan-2026-05-09: "wenn man scrollt muss es sich mitbewegen (so wie beim Press-drag-select menü)". Root cause: P45 used `position: fixed` with viewport-coords, so the popup stayed at a fixed viewport pixel while the card scrolled away. Fix: switch to `position: absolute` + document-coords (rect + `window.scrollX/Y`). Portal element no longer `position: fixed` so absolute children use the initial containing block (= document) and scroll naturally. Wheel + saved-colors popups continue to use `position: fixed` (they anchor to picker-dot at long-press time and only need to outlive their own session, not survive scroll).
- 🐛 fix(P8) **R28**: popup BOTTOM-edge now anchors to the original-element's bottom-y (= main-node-icon for topology, hidden-slider-bottom for parallel). Stefan-2026-05-09: "Bei dem group expand muss auch die y-verankerung noch stattfinden. Da wie gesagt ist das main Node Icon der Anker". Implementation: `top: rect.bottom + scrollY` plus `transform: translateY(-100%)` shifts the popup up by its own height, so its bottom-edge sits exactly at the captured anchor-y. `min-height: rect.height` keeps the popup at least the original element's vertical extent. `justify-content: flex-end` on the popup-card ensures the topology's group-row / parallel's slider-bottoms sit at the popup-bottom = anchor-y. When content adds height, the popup grows UPWARD (translate pulls it up by the new height).
- 🛠️ infra: bloom-in animation reworked to compose with the inline `translateY(-100%)`. New `everyday-anchored-card-in` keyframes hold `translateY(-100%)` throughout while scaling 0.92 → 1; the inline transform is preserved at animation end via `fill-mode: both`.
- 🔧 config: VERSION 0.8.3 → 0.8.4, resource `?v=0.8.4-p46`.

## 2026-05-09 - P45: position-anchored popups (replace card-position, not viewport-modal)

- ✨ feat(P8) **R25**: topology-popup now mounts AT THE EXACT POSITION of the card (was: viewport-centred modal). Stefan-2026-05-09: "das popup soll auch an der gleichen stelle erscheinen wie die jetzige Karte". Implementation: `this.getBoundingClientRect()` captured on long-press → passed via inline `left/width/bottom` style on the popup container. Popup grows UPWARD from the card's group-icon-y, so the group-icon stays at the same screen position when the popup mounts. Compact slider in the underlying card goes `visibility: hidden` while the popup is open (layout slot reserved, no reflow).
- ✨ feat(P8) **R26a**: parallel-sliders popup mounts AT THE EXACT POSITION of the slider it replaces. Stefan-2026-05-09: "an stelle des Sliders des items gehen". Implementation: `everyday-vertical-pill-slider`'s `getBoundingClientRect()` captured on long-press → popup width grows to fit N parallel sliders, centred on the original slider's x-axis.
- 🛠️ infra: dropped the full-screen backdrop dim (`background: rgba(0,0,0,0.55)` + `inset: 0`) from anchored popups. The popup is no longer a modal-over-everything, it's an in-place replacement for a piece of the card. New `.anchored` + `.anchored-card` classes drive the position-aware sizing; `popup-card.anchored-card` uses `justify-content: flex-end` so the group-icon ends at the popup's bottom (= the original card's bottom).
- 🔧 config: VERSION 0.8.2 → 0.8.3, resource `?v=0.8.3-p45`.
- 📚 doc: pending Stefan-Q4-decision on default-show-parallel-sliders. Recommendation drafted in chat; if greenlit, P46 will introduce a render-mode-system (`viewMode: collapsed | topology | parallel`) replacing the popup-as-portal pattern entirely for the default-state case.

## 2026-05-09 - P44.1 hotfix: parallel-option hit-test for top-of-diamond drag

- 🐛 fix(P6) **immediate-Stefan-bug**: the upper picker dot (`Parallel` on the member-tile 4-option diamond) couldn't be selected via press-drag-select — the top dot stayed grey. Root cause: when P43 added the 4th `parallel` option to the member picker, only the RENDERER (`mode-picker.ts _renderedOptions`) was updated to 4-option diamond — the HIT-TEST (`pickerHoverFromPointer` in group-layout-expanded.ts) was still running the 3-option-triangle math. So a drag toward the top dot fell into the 3-opt 'saved' sector instead of the 4-opt 'parallel' sector. Renderer + hit-test now agree on the diamond layout.
- 🔧 config: VERSION 0.8.1 → 0.8.2, resource `?v=0.8.2-p44hf`.

## 2026-05-09 - P44: topology-popup mirrors inline-expand layout + slider-length config

- 🐛 fix(P8/P43) **R23**: topology-popup now reproduces the inline-expand layout EXACTLY (mindmap-baum visible as background, member-cols above tiles, group-row at bottom). Stefan-2026-05-09 R23: "das Popup soll schon so aussehen, wie der Inline expand vorher (also gleiches Layout, mit mindmapbaum, die lampen müssen an der gleichen stelle sein wie vorher)". P43's first cut had a simplified mindmap-as-header layout — too divergent from the in-card view. Now both paths share a `_renderTopologyTree()` helper as single source of truth.
- ✨ feat(P8) **R24**: NEW `group.full_length_sliders: boolean` config (default `false`). Stefan-2026-05-09: "später soll man zwischen beiden Varianten wählen können".
   - `false` (default) — slider-length-accounts-for-mindmap-and-is-therefore-shorter: member sliders 170 px (= the original inline-expand height; mindmap-baum has room).
   - `true` — full-length-expanded-slider: member sliders 260 px (matches the host compact-slider height; tighter mindmap aspect).
- 🛠️ infra(P43): topology-tree CSS rules (`.layout`, `.topology`, `.tile`, `.member-cols`, `.member-col`, `.picker-overlay`, etc.) duplicated into the body-portal stylesheet under `.everyday-popup-portal .topology-popup-card` scope so the portal-rendered popup gets the same visual treatment as the host's shadow-root version. Comment notes: keep both copies in sync on future style changes.
- 🔧 config: VERSION 0.8.0 → 0.8.1, resource `?v=0.8.1-p44`.

## 2026-05-09 - P43: topology popup + parallel-sliders popup + off-state thumb fallback (Q3 Decision B)

- ✨ feat(P8): **Q3 Decision B implemented** — Mindmap option in the compact-group long-press picker now opens a body-portal popup with the FULL N-slider topology view, NOT inline-expanded. Stefan-2026-05-09 R21: card stays compact; host icon never moves; nested-groups become trivial via portal recursion. Replaces the P40 inline `_compactExpanded` semantic.
- ✨ feat(P6.3 / R20): **NEW Parallel option** in the member-icon long-press picker — opens a popup with N sliders side-by-side for the same entity. Default modes = `[brightness, temperature, hue, saturation]`; subset configurable via `parallel_sliders.modes` in the card config. Stefan: "implementiere das gleich mit (welche slider angezeigt werden sollen)".
- 🎨 ui(P6): mode-picker `member` variant grew from 3-option triangle (Wheel/Temp/Saved at 90/210/330) to **4-option diamond** (Wheel 90 / Saved 0 / Temp 180 / Parallel 270). Wheel stays at 90° as the muscle-memory anchor. New PARALLEL glyph (3 vertical bars + dots) communicates the multi-axis pop-up.
- 🛠️ infra(P6/P7/P43): body-portal hosts FOUR popup types now — wheel + saved-colors (P41), topology + parallel-sliders (P43). Single `_popupPortal` div shared. Outside-click + popup-suppression logic extended to handle all four.
- 🐛 fix(P3) **R19 grey-thumb regression**: layered fallback in `_fillColor()` for the off-state colour-thumb. Previous P42 only used in-memory snapshots, which are EMPTY on a fresh page-load if the light hasn't been on since mount — that's why Stefan saw a grey thumb. New fallback chain: (1) snapshot, (2) live HA `hs_color`/`rgb_color` attributes (many integrations preserve these when off), (3) brand-orange via `--paper-item-icon-active-color` instead of grey `--primary-color`.
- 📚 docs(P38.1): expanded the **Effects-List 5-Pack** spec (default-effects population, long-press-delete, grayed-out-section, tap-to-restore, drag-reorder) into PHASE-PLAN per ottawa-v2's Telegram triage 2026-05-09. Architecture sketch + persistence model documented; pentagon vs context-swap layout decision pending. 12.5.-Launch deadline.
- 🔧 config: VERSION 0.7.2 → 0.8.0 (minor SemVer bump for the popup-architecture shift), resource `?v=0.8.0-p43`.

## 2026-05-09 - P42: thumb-size calc, off-state thumb colour, group-icon anchor regression fix

- ♻️ ui(P3): `--thumb-size` back to a calc — `calc(var(--everyday-light-card-width) * 0.8)`. Stefan-2026-05-09 R15 reverts P41's fixed 40 px. Default 60 → thumb 48; group 47 → thumb 37.6. The colour-thumb white ring follows: border = `(slider-width - thumb-size) / 2` = `slider-width × 0.1` = ~6 px / ~4.7 px.
- ♻️ ui(P3): `.color-thumb` `left` property removed (Stefan-2026-05-09 R14 "mach left = undefined"). The thumb is auto-centred horizontally by box-sizing:content-box + matching border on both sides — outer footprint = host width without explicit left positioning.
- ✨ feat(P3): off-state colour-thumb now shows the THEORETICAL active-state colour (Stefan-2026-05-09 R17). When the light is off + slider is in hue/saturation mode, the thumb fills with `hsv2rgb(lastSnapshot.hue, lastSnapshot.saturation, lastSnapshot.brightness ?? 1)` instead of staying transparent. Matches the user's mental model: "this is the colour the bulb will turn on with when I tap the slider". Brightness mode still goes empty when off (the level IS the data). Temp mode falls back to `var(--primary-color)` when no rgb can be derived.
- 🐛 fix(P5/P8): **GROUP-ICON-ANCHOR REGRESSION** — group icon Y-position no longer shifts between compact and expanded view (Stefan-2026-05-09 R16). Root cause: P32 (2026-05-08) fixed this for `iconPosition: 'top'` only; the default `'bottom'` never had the same anchor treatment, so when content (member-cols + topology-gap) is added on expand, the card grows downward and the bottom-anchored host moves. **Gap-closure mechanism**: a SHARED CSS var `--everyday-card-min-height` (default 380 px) consumed by BOTH compact and expanded modes ensures they have identical layout heights. `justify-content: flex-end` on `.layout` pins content to the bottom; compact mode reserves the upper space empty, expanded mode fills it. The host icon stays at the same viewport-y across the toggle. Per-card override possible via `--everyday-card-min-height: 420px;`.
- 🔧 config: VERSION 0.7.1 → 0.7.2, resource `?v=0.7.2-p42`.

## 2026-05-09 - P41: popup portal, off-state pre-apply, color-thumb thumb-sized, mindmap L-curves

- 🐛 fix(P6/P7): wheel + saved-colors popups now mount in a body-level **portal** instead of the card's shadow root. Stefan-2026-05-09 R10: popups landed "ganz unten am bildschirm" because HA's view applies a `transform` on an ancestor element, which makes `position: fixed` reposition relative to that ancestor instead of the viewport (per CSS spec). The portal lives directly under `<body>`, so no transform-ancestor interferes — popups now appear over the picker dot as intended. Bonus fix for R11 (clicks didn't work) — popups were off-screen, hence "unreachable".
- ♻️ revert(P40): `--pill-width` is RE-ALIASED to `--everyday-light-card-width` (Stefan-2026-05-09 R5+R6+R7-corr "da habe ich mich eben vertan"). The inner-vs-outer pill distinction P40 introduced is dropped.
- 🎨 ui(P3): `.color-thumb` is square at `--thumb-size × --thumb-size` (Stefan was confused with `--pill-width` at P40). `--thumb-size` is now a **fixed 40 px** value, not a calc — keeps the visible thumb consistent across host sizes (default 60, group 47, future themes). `left: var(--color-thumb-border)` and Stefan-formula border `(slider-width - thumb-size) / 2` give a 10 px white ring at default + 3.5 px in the group view. Outer footprint = host width.
- ✨ feat(P3): **off-state pre-apply** for hue / saturation / temp sliders. Stefan-2026-05-09 R9: when the light is OFF and the user drags a non-brightness slider, the `light.turn_on` call now includes the previously-snapshotted brightness AND the OTHER axis (sat for hue mode, hue for sat mode), so the bulb comes on at the user's prior state with only the dragged axis changed. Snapshots taken on every hass push when state==='on'. Falls back to sensible defaults (sat 100, hue 0, brightness — none) if no snapshot is available yet.
- ✨ feat(P3): **off-state slider display** for non-brightness modes. `_hassFraction` now returns the last snapshotted value when the light is off, so the slider visually sits at "where the axis was last set" instead of collapsing to 0. Brightness still collapses to 0 on off (as expected).
- 🎨 ui(P3): saturation track keeps its `--hue-color` stop colour across off → on transitions — uses the snapshotted hue when off so the user sees the right gradient.
- 🎨 ui(P4): mindmap-path bezier formula redesigned for **L-shaped outer arms** (Stefan-2026-05-09 R13: "der linke arm zb soll schon fast 'L' förmig sein. natürlich mit einer natürlichen Kurve … sieht dann seriöser aus"). New formula: `c1 = (groupX + dx*0.7, groupY - vs*0.05)` (start horizontal-out near group), `c2 = (memberX, groupY - vs*0.5)` (end vertical-up at member). Symmetric across groupX. For the centre member (dx=0) the curve collapses to a vertical line.
- 🎨 ui(P6): mindmap-trizack glyph in the `group-compact` picker option is **flatter** — all 3 child nodes at same y=6, outer arms fan out in matching L-curves (Stefan-2026-05-09 R12). Mirrors the expanded-view mindmap-path geometry so the icon reads as a preview of the topology view it expands into.
- ♻️ revert(P40-R8): dropped the slider-bottom mindmap-trizack hint icon. Stefan-2026-05-09: "lass dieses mindmap symbol im eingeklappten slider doch weg. (dass es so wie jetzt im mode picker drin ist, ist genug)". Property `showMindmapHint` kept on the slider for one cycle as deprecated bw-compat; render path removed.
- 🔧 config: VERSION 0.7.0 → 0.7.1, resource `?v=0.7.1-p41`.

## 2026-05-09 - P40: pill decoupled, color-thumb square, picker variants, expand-via-picker

- 🎨 ui(P3): `--pill-width` decoupled from `--everyday-light-card-width` (was aliased pre-P40). Default 60/50; group view 47/37. Stefan's formula `border = (slider-width - pill-width) / 2` is now valid and yields ~5 px in both. New CSS var `--color-thumb-border` exposes the formula.
- 🎨 ui(P3): `.color-thumb` (temp / hue / saturation) is now SQUARE — `width = height = --pill-width`, `left: 0`, `box-sizing: content-box`, `border: --color-thumb-border solid white`. The white ring bleeds OUTSIDE the pill so the outer footprint = host width = `--everyday-light-card-width`. Brightness `.thumb` is unchanged. Stefan-2026-05-09 R1+R2+R7.
- 🎨 ui(P3): `.track` re-anchored — was `inset: 0`, now centred via `left: 50%; transform: translateX(-50%); width: var(--pill-width)` so the pill is INNER while the colour-thumb's white ring extends to the host edge.
- 🐛 fix(P3): mixer-fader track override now resets `transform: none` to clear the parent horizontal rule's `translateY(-50%)`. Fixes a double-offset that pushed the mixer track up by half its height.
- 🐛 fix(P3): `:host([mindmap-hint])` uses `margin-bottom: 28px` (was `padding-bottom`). Padding shrank `.container` and broke the `--range` math at fraction = 1.0.
- ✨ feat(P6): mode-picker now has THREE variants — `member` (3-option triangle, unchanged), `group-compact` (4-option diamond + Mindmap option), `group-expanded` (2-option horizontal). Replaces the previous `omitTemp` boolean (kept as deprecated bw-compat). Each variant has its own angle-map AND its own hit-test sectors. Stefan-2026-05-09 R6+R8.
- 🐛 fix(P6): fixes the **R5 regression** Stefan reported ("im mode picker funktionieren color wheel und saved colors wieder nicht"). Root cause: `_pickerDotPosition` had a single hardcoded angle-map (`{temp:210, saved:330, wheel:90}`) used for ALL picker layouts. The `omitTemp` 2-option layout actually rendered Saved at 270°, so the popup centred on the picker-dot position 60° off — sometimes off-screen, often invisible against the card edges. Now the angles map per variant; the popup lands on the actual picker-dot.
- ✨ feat(P6): 'group-expanded' picker (omitTemp) re-laid out from top/bottom (90/270) to **left/right (0/180)** per Stefan-R6. Wheel right, Saved left.
- ✨ feat(P6/P8): NEW Mindmap option in the `group-compact` 4-option picker. Picking it expands the compact-collapsed view into the N-slider topology view. Stefan-2026-05-09 R8: replaces the previous double-tap = expand gesture.
- ✨ feat(P3/P8): GROUP compact double-tap REBOUND from `expand` to `cycle slider mode` (matches the member-tile double-tap semantic). Long-press still opens the picker — now 4-option with Mindmap = expand.
- ✨ feat(P3/P8): NEW small mindmap-trizack hint icon at the bottom of the compact slider (only when `memberIds.length >= 2`). Communicates "expansion is available via the picker". 1 host + 3 nodes + cubic-Bézier curves matching the topology view's mindmap-path. Pointer-events:none — purely decorative.
- 🛡️ guard: defensive checks added — `_pickerDotPosition` falls back to the icon origin (not angle 0) when the requested mode isn't in the variant's angle-map; `_applyPickerMode('mindmap', ...)` only flips `_compactExpanded` when `variant === 'group-compact'`, so a misrouted call from another variant is a silent no-op instead of a phantom expand.
- 🐛 fix(P6): mode-picker bloom animation — added `.dot:nth-child(4) { animation-delay: 120ms }` so the 4-option diamond fans out smoothly (was: 4th dot fired at 0 ms alongside the 1st).
- 📚 docs(P40): PHASE-PLAN gets a "Theme proposal" section after reading paris-v1's `themes/ARCHITECTURE.md`. Recommendation: everyday-light-card consumes Layer-2 HA-compat tokens (`--paper-item-icon-active-color`, etc.) and exposes its own `--everyday-*` overrides for theme-driven customisation. List of suggested follow-up tokens documented.
- 📚 docs(P40): inline comments scrubbed across all changed files — agent-package review (code-reviewer + frontend-developer + ux-architect) ran over the changes; their findings are folded into this release.
- ⚠️ known-tradeoff (Stefan-decision): UX-architect flagged that Wheel jumps from 90° (bottom) in `member` and `group-compact` to 0° (right) in `group-expanded`. Stefan's R6 explicitly asked for left/right horizontal, so we keep it; aware that the muscle-memory mismatch may surface in the demo.
- 🔧 config: VERSION 0.6.3 → 0.7.0 (minor SemVer bump for the picker-variant API + decoupled pill architecture), resource `?v=0.7.0-p40`.

## 2026-05-09 - Thumb size unify + volume progress + group picker omits temp + mixer opt-in

- 🎨 ui(P3): all slider thumbs now have the SAME geometry — width = 80 % of pill (`thumb-size`), height = `thumb-size` (Stefan-2026-05-09 screenshot 09:40:15). The colour-thumb (temp / hue / saturation) was previously full-width pill = 47 px and read as larger than the brightness thumb. Now both are the same circle. The 4 px solid white border (`calc(--pill-width * 0.08)`) is restored on the colour-thumb to give it the white-ring look Stefan asked for. Brightness thumb stays without border (the natural padding to track edge is the "imagined border").
- ✨ feat(P3): volume slider now renders the `.fill` (was brightness-only). Fill colour for volume = `var(--paper-item-icon-active-color, var(--state-light-active-color, #f88d2a))` so the progress reads as a level indicator. Mixer-fader handle stays white (overridable via future `--everyday-mixer-handle` CSS var).
- ♻️ revert(P3): horizontal slider default is back to the original PILL style (full-width gradient track + thumb traveling along X). Mixer-fader is now OPT-IN via new config `slider.style: 'mixer'`. P38 had set mixer as horizontal default by mistake.
- ✨ feat(P11-prep): re-introduced the speaker-row layout from P36, but as flat config flags instead of a variant value:
   - `slider.style: 'mixer'` (default `'pill'`).
   - `slider.show_buttons: true` (default `false`). Only meaningful for `media_player.*` + mixer style.
   - When all three combine: render `[name | mixer slider | − | +]` row layout with `media_player.volume_down/up` buttons. Same dark-translucent rounded card styling as Stefan's existing my-slider-v2 setup.
- 🐛 fix(P5/P6): long-press on the GROUP icon in the expanded view NOW opens the picker. Two fixes:
   - Removed `@click=${this._onGroupTap}` from the expanded group-tile template; replaced with a stop-only no-op `@click` so browser-generated clicks can't bubble. Tap-to-toggle now routes through gesture-detector's `onTap`. (Same pattern as the compact-collapsed group fix from P34.)
   - Added picker-overlay rendering inside the expanded group-tile when `_activePicker === groupEntityId`.
- ✨ feat(P6): GROUP picker drops the Temp option (Stefan-decision-2026-05-09). The group has no individual slider that could switch modes, so the temp option doesn't apply. New `omitTemp` prop on `<everyday-mode-picker>`: when true, only Wheel + Saved render at 90° (bottom) and 270° (top). `pickerHoverFromPointer` accepts an `omitTemp` flag for the corresponding 2-option hit-test (lower half → wheel, upper half → saved).
- 🔧 config: VERSION 0.6.2 → 0.6.3, resource `?v=0.6.3-thumb-fix`.

## 2026-05-09 - Mixer-fader thumb + group-picker in expanded + speaker_row revert

- ♻️ revert(P11-prep): dropped the `variant: 'speaker_row'` config + speaker-row template + speaker-row CSS that landed in P36. Stefan-2026-05-09-clear: only the THUMB STYLE was wanted, not the full row-with-buttons layout. The new mixer-fader thumb (below) is the actual deliverable.
- 🎨 ui(P3): horizontal slider redesigned to mixer-fader / DJ-board style. Track is now a thin centred band (14 px high, was full pill thickness), thumb is a portrait rectangular pill (20 × 44 with 12 px border-radius) that extends 15 px above + below the track. Strong drop-shadow on the thumb for the floating-knob look. New CSS vars (`--hbar-track-h`, `--hbar-thumb-w/h/r`) drive the geometry. Vertical slider stays in pill form (Stefan didn't ask for changes there).
- 🐛 fix(P3): white border on the colour-thumb is BACK (`border: 2px solid white`). Stefan-clarification: the white ring around the thumb is a feature he liked. The "white tip" complaint that triggered the drop in P33-P34 was actually the track-edge crescent (already fixed in P36 by dropping the .track 1 px border). Both are now correctly disambiguated.
- 🐛 fix(P5/P8): long-press on the GROUP icon in the expanded view now opens our mode-picker. New `_bindExpandedGroupGestures` mirrors the compact-collapsed long-press flow but binds to `.topology .tile.group`. Triggered from `updated()` on `compact`/`_compactExpanded`/`memberIds` changes. Disposable cleanup on disconnect.
- 🐛 fix(P3): bumped `doubleTapMs` from 300 (default) to 500 in `_rebindMemberGestures` so member-icon double-tap-to-cycle-mode is as forgiving as the compact-group double-tap. Stefan reported the cycle wasn't firing in expanded view — likely his tap timing falls between 300-500 ms.
- 📚 plan: PHASE-PLAN.md gets P38.1 (Effect option in mode-picker) entry — placeholder for the bot-message reference Stefan mentioned. 4-option layout vs 3-symmetric is an open design question.
- 🔧 config: VERSION 0.6.1 → 0.6.2, resource `?v=0.6.2-mixer-thumb`.

### Recap of P32 → P38 (the bekifften phase, for Stefan)

| Phase  | What was done                                                          | Final disposition |
| ------ | ---------------------------------------------------------------------- | ------------------ |
| P32    | Group picker on long-press + mindmap alignment + glow restore (v0.5.1) | Kept ✓             |
| P33    | Hue 0/360 wrap + temp white-tip drop + first double-tap defensive (v0.5.2) | Hue ✓; white-tip drop REVERTED in P38 (was a misinterpretation, white ring is a feature) |
| P34    | Color-thumb inset highlight dropped + doubleTapMs=500 + @click no-op (v0.5.3) | Inset highlight stayed dropped (was actually the right call); double-tap defensive ✓ |
| P35    | Volume slider mode + horizontal orientation + media_player support (v0.6.0) | Kept ✓             |
| P36    | Track 1 px border dropped (saturation crescent fix) + speaker_row variant added (v0.6.1) | Border drop ✓; speaker_row REVERTED in P38 (overscoped) |
| **P38** | Mixer-fader thumb for horizontal + white ring restored on color-thumb + group-picker in expanded view + member doubleTapMs bump + speaker_row revert (v0.6.2) | This release |

## 2026-05-09 - Saturation crescent fix + speaker_row variant

- 🐛 fix(P3): the thin white "crescent" Stefan saw at the top of the saturation slider (screenshot 00.48.49) was the `.track`'s 1 px white-ish border anti-aliased against the dark page background — most visible against fully-saturated colours. Border dropped entirely; the inset + outer shadows alone carry the depth treatment.
- ✨ feat(P11-prep): new `variant: 'speaker_row'` card config (media_player only). Renders the row layout Stefan asked for from his existing my-slider-v2 + button-card setup (attached YAML 2026-05-09): `[name | horizontal volume slider | − | +]` in a 60 px tall pill row. Minus / plus buttons fire `media_player.volume_down` / `volume_up` for the standard ±5 % nudges. Card frame has the rounded 30 px radius + dark translucent bg + soft drop-shadow that matches Stefan's existing speaker rows.
   - Example YAML:
     ```yaml
     type: custom:everyday-light-card
     entity: media_player.hifi
     variant: speaker_row
     ```
- 🔧 config: VERSION 0.6.0 → 0.6.1, resource `?v=0.6.1-speaker-row`.

## 2026-05-09 - Volume slider + horizontal orientation (scope broadens to media_player)

- ✨ feat(P11-prep): card scope broadens from light-only to light + media_player. The card now accepts `media_player.*` entities in `setConfig`. Default mode by domain: `light → 'brightness'`, `media_player → 'volume'`. Card-picker label updated to "Everyday Light Card" reflecting the broader scope; custom-element name stays `everyday-light-card` for backward compatibility (full repo rename to `everyday-light-card` is logged as P11 in PHASE-PLAN).
- ✨ feat(P3): new `'volume'` slider mode for media_player entities. Reads `media_player.volume_level` (HA exposes 0-1 already). Drag-to-zero mutes via `media_player.volume_mute`; non-zero drag unmutes if currently muted, then `media_player.volume_set` with the fraction. Label = percentage.
- ✨ feat(P3): new `slider.orientation: 'vertical' | 'horizontal'` config. Default 'vertical' (the existing pill). Horizontal swaps the dimensions so the pill lies flat — natural for media-player volume controls. CSS overrides keyed off the host attribute (`:host([orientation='horizontal'])`); pointer math branches on orientation. Gradients flip from `to top` to `to right`. `.fill` grows along X axis; `.thumb` and `.color-thumb` position via `left`. Works for ALL slider modes (brightness/temp/hue/saturation/volume) — light sliders can now be horizontal too (Stefan-2026-05-09).
- 📚 plan: PHASE-PLAN.md gets P9.1 (sideways expanded view, deferred follow-up) and P11 (repo rename to `everyday-light-card`) entries.
- 🔧 config: VERSION 0.5.3 → 0.6.0 (minor SemVer bump for the broadened entity-domain scope), resource `?v=0.6.0-volume-horizontal`.

## 2026-05-09 - Double-tap defensive layer + thumb white-crescent fully gone

- 🐛 fix(P3): white "Zipfelchen" on the temp slider is now ACTUALLY gone. Last commit replaced the 4 px white border with a 1 px `inset 0 0 0 1px rgba(255,255,255,0.18)` highlight - which Stefan still saw as a white crescent at the thumb apex (screenshot 22:35:34 confirms). Dropped the inset white shadow entirely. The colour-thumb now has only the soft outer drop-shadow `0 2px 4px rgba(0,0,0,0.18)` for edge definition. Thumb position is also conveyed by the label inside it.
- 🐛 fix(P8): double-tap on compact-collapsed group icon - layered defenses now applied:
   - `doubleTapMs: 500` (was 300 default) — gives the gesture-detector a more forgiving window so two taps separated by 350-450 ms still register as a double-tap instead of two single taps.
   - Re-added `@click` on the compact group tile as a STOP-only no-op (`stopPropagation` + `preventDefault`). Browser-generated click events are killed at the tile boundary so they can't bubble to ancestors or trigger any latent handlers. gesture-detector's `onTap` (which is suppressed during double-tap) remains the sole path that calls `_onGroupTap`.
- 🔧 config: VERSION 0.5.1 → 0.5.3, resource `?v=0.5.3-double-tap`. (0.5.2 was the prior cache-bust; bumping again to force a fresh fetch in case the previous cache-bust didn't propagate to Stefan's browser.)

## 2026-05-09 - Hue 0/360 wrap + temp white-tip + double-tap toggle bugs

- 🐛 fix(P3): hue slider at 0 % no longer jumps to the top. HA / many integrations normalize `hs_color: [0, sat]` → `hs_color: [360, sat]`. The slider read 360 back, computed `360/360 = 1.0` and visually jumped from bottom (red, hue 0°) to top (also red, hue 360°). `_hassFraction` for hue mode now wraps via `(h % 360 + 360) % 360` so both extremes collapse to 0. Stefan: "It should just stay at 0 and not jump up like that. That's childish."
- 🎨 ui(P3): dropped the 4 px solid white border on the colour-thumb (`.color-thumb`) that was creating a "Zipfelchen weiß" at the top of the temp slider when fraction = 1. Replaced with a soft outer drop-shadow + a 1 px inset white-18 % highlight so the thumb still has visual definition without an opaque white ring overlapping the gradient. Hue and saturation thumbs benefit too — the 4 px border was always there, just less visually jarring against their high-saturation top colours.
- 🐛 fix(P8): double-tap on the compact-collapsed group icon no longer toggles the group on/off before expanding. The DOM-level `@click=${this._onGroupTap}` fired for each tap of a double-tap (browser fires click events independently of our gesture-detector), so a double-tap = two toggles + an expand. Replaced with `onTap` routed through `attachGestures` — gesture-detector suppresses `onTap` when `onDoubleTap` is recognized, so the user gets ONLY the expand behaviour. Keyboard `@keydown` for Enter/Space stays so the tile remains keyboard-accessible.
- 🔧 config: VERSION 0.5.1 → 0.5.2, resource `?v=0.5.2-bugs`.

## 2026-05-09 - Group picker on long-press + mindmap alignment + glow restore

- ✨ feat(P6/P8): long-press on the compact-collapsed group icon now opens OUR mode-picker (Wheel / Saved / Temp) for the group entity, not HA's `hass-more-info` dialog. Stefan-2026-05-09: "es soll natürlich unser Press-drag-select menü erscheinen". Mirrors the member long-press flow: `_activePicker = groupEntityId`, picker-origin from the group tile's `.ic`, then `_applyPickerMode` on release. Compact slider's mode is now reactive to `_memberModes[groupEntityId]` so the Temp option flips the slider into temperature mode and `_setMemberMode` auto-applies the previously stored kelvin. Auto-revert on light-off and `color_temp_kelvin` snapshot loops both extended to include the group entity in addition to members.
- 🐛 fix(P4): mindmap dot positions were stale after the tile-icon shrink (member 48 → 34, group 64 → 46). Y constants recomputed to match new layout heights:
   - `'top'`: `GROUP_DOT_Y` 39 → 30, `MEMBER_ICON_CENTER_Y` 110 → 92.
   - `'bottom'`: `GROUP_DOT_Y` 311 → 288, `MEMBER_ICON_CENTER_Y` 202 → 195.
- 🎨 ui(P5): press-drag-select feedback restored its glow that bleeds beyond the `.ic` boundary (Stefan: "es soll etwas glowen über den DEFAULT_MEMBER_R rand hinaus"). Combined: inset orange tint inside the disc + layered outer rings extending 4 + 9 px past the edge (= radius 21 + 26 for member, 32 + 32+9 for group). Same rule applies to `.tile.group.lp .ic` so the group long-press shows the same glow.
- 🎨 ui(P5): `.tile.group .picker-overlay { top: 23 px }` so the group picker centres on the 46 × 46 group .ic; member picker stays at 17 px for the 34 × 34 member .ic.
- 🔧 config: VERSION 0.5.0 → 0.5.1, resource `?v=0.5.1-group-picker`.

## 2026-05-09 - Tile radii + gesture rebind + group-dot state + mode-reset

- 🎨 ui(P3/P5): tile-icon (`.tile .ic`) dimensions match the mindmap-path dot radii — `34 × 34` for member tiles (= `2 × DEFAULT_MEMBER_R = 17`) and `46 × 46` for group tiles (= `2 × DEFAULT_GROUP_R = 23`). Picker-overlay anchor `top: 17px` (was 24) so the mode-picker still centers on the new icon centre. The press-drag-select feedback flips from outer rings (`box-shadow: 0 0 0 3px / 6px`) to an INSET tint (`background: rgba(248,141,42,0.22)` + thin inner ring) — fills the disc at exactly DEFAULT_MEMBER_R, no overshoot.
- 🎨 ui(P8): compact-collapsed group tile gets the disc background + soft drop-shadow back ONLY for `.tile.group.compact-target .ic` (Stefan: "im nicht expandierten Modus brauchen wir aber die 'card' sonst siehts irgendwie falsch aus"). Disc is sized to 46 × 46 so it visually matches the mindmap group-dot. Expanded view group tile stays disc-less (drop-shadow on glyph only).
- 🐛 fix(P5): dropped the leftover `.tile.group:hover .ic { background: ... }` rule that was still painting the old disc bg on hover after we removed the default `.ic` background.
- ✨ feat(P8): compact-collapsed group gestures rebind. **tap** → toggle group on/off (existing); **double-tap** → expand to the N-slider view (was long-press); **long-press** → fire `hass-more-info` for the group entity, opening HA's standard light-control dialog (full brightness + colour + effect-list controls). Stefan: "der expand modus soll durch doppel tab getriggert werden. Durch long tab soll man die übergeordnete entity normal steuern können."
- ✨ feat(P5): every member's slider mode resets to brightness on compact-expand → collapse transition. Detected via `changed.get('_compactExpanded') === true` (previous value) so only the actual exit-from-expanded fires the reset, not initial mounts.
- 🐛 fix(P4): mindmap-path group-dot stroke is now state-aware. New props `groupOn?: boolean` + `groupRgb?: [r,g,b]`. When `groupOn === false` the group ring goes faint-gray (matches the off-state arms); on with rgb → coloured; on without rgb → falls back to gold. Without the props (standalone test page) → backward-compatible gold default. Stefan-tagged bug: "members sind aus aber der Kreis um das übergeordnete licht ist an".
- 🔧 config: VERSION 0.4.9 → 0.5.0, resource `?v=0.5.0-tile-radii`.

## 2026-05-08 - P7.1: Saved-Colors persistence (static + helper:input_text)

- ✨ feat(P7.1): saved-colors palette now persists across card reloads when configured via the existing `saved_colors.source` schema. Two sources implemented:
   - `'static'` + `static: [[r,g,b], ...]` — palette read from YAML; mutations stay in component state (no write-back, since YAML is read-only).
   - `'helper:input_text.<id>'` — palette read from the helper's state on every hass push (so external mutations sync automatically); add/remove writes back via `input_text.set_value` with a JSON-encoded list of `[r, g, b]` triples.
- 🛡️ guard: when persisting to a `helper:input_text`, the JSON payload is checked against the helper's `max` attribute (HA default 100; users with > 5-6 saved colors need to set max ≥ 255). On overflow, a toast surfaces telling the user to bump the helper limit instead of silently truncating.
- 🔧 wiring: `everyday-light-card.ts` passes `cfg.saved_colors` through to `<everyday-group-layout-expanded>` as `savedColorsConfig`. The component re-syncs from source whenever `hass` or `savedColorsConfig` changes.
- 📚 plan: PHASE-PLAN.md marks P7.1 done; ha_favorites source still pending (WebSocket research).
- 🔧 config: VERSION 0.4.8 → 0.4.9, resource `?v=0.4.9-saved-persist`.

## 2026-05-08 - Border tone, shadow direction, icon-disc-off

- 🎨 ui(P3): slider track border softer (alpha 0.09 → 0.05) via new CSS variable `--everyday-track-border`. Themable.
- 🎨 ui(P3/P5): outer drop-shadow direction now flips based on `icon_position`. Default (`'top'`) casts downward (positive Y); `'bottom'` casts upward (negative Y) so the visual narrative reads as "light comes from the group icon" — group at the bottom = light from below = upward-cast slider shadow. Driven by CSS vars `--everyday-light-card-shadow-y1` / `--everyday-light-card-shadow-y2` set on the layout host based on `iconPosition`. Stefan-tagged: `'top'` direction is hypothesis (default downward today); revisit if testing rejects.
- 🎨 ui(P5): tile-icon disc-background and box-shadow dropped. Replaced with a `drop-shadow` filter on the inner glyph (`ha-icon` for member tiles, inline `compact-glyph` SVG for group tile). Icons now float on the card without a visible disc backdrop. Active-state colour still works because the glyph inherits `currentColor` from `.tile.on .ic`.
- 📚 docs: PHASE-PLAN "Future enhancements" gets a Theme-token CSS variables entry tracking Stefan's "stylen per Variable" hypothesis (e.g. `--everyday-track-border`, `--everyday-light-card-shadow-y1/2`, future tile-shadow / mindmap-arm / popup-shadow tokens for downstream theming).
- 🔧 config: VERSION 0.4.7 → 0.4.8, resource `?v=0.4.8-shadow-dir`.

## 2026-05-08 - mindmap_dots config + slider track rebalance

- ✨ feat(P4/P5): new config field `group.mindmap_dots?: boolean`. Smart default per `icon_position`: `'top'` → `false` (HTML icon-tiles carry the state visual), `'bottom'` → `true` (matches the original P5 design - colored stroke rings around each member tile + halo under the group icon, picked up from the `DEFAULT_MEMBER_R / DEFAULT_GROUP_R` constants in `mindmap-path.ts`). For the `'bottom'` layout, restored Stefan's original +7 px down-offset on `GROUP_DOT_Y` so the dot's outer stroke peeks out below the group icon as a halo (`311` instead of `304`). 'top' layout uses `39` (icon-center 32 + 7) for symmetry when the user opts into dots there.
- 🎨 ui(P3): slider track shadow rebalance. Stefan: "inner ein bisschen weniger und outer ein bisschen mehr ... track border ein klitzekleines bisschen größer". New mix: `border: 1 px solid white-0.09` for a clean pill outline; inner inset `0 2 3 black-0.30` (was 0.45); outer close `0 4 10 black-0.55`; outer far `0 14 32 black-0.65`. Inner softer, outer punchier, edge-line readable. Inner-highlight (`white-0.05`) dropped — was too subtle to register.
- 🔧 config: VERSION 0.4.6 → 0.4.7, resource `?v=0.4.7-dots`.

## 2026-05-08 - icon_position config + slider shadow boost

- ✨ feat(P5/P8): new config field `group.icon_position?: 'top' | 'bottom'` (default `'bottom'`). `'bottom'` matches the original P5 design with sliders+tiles at the top and the group ("main light") icon at the bottom; `'top'` puts the group icon on top with sliders/tiles below (anchors the group-icon's screen-Y across compact ↔ expanded — Stefan-tagged "falsch herum" so opt-in only). Threaded through `everyday-light-card.ts` → `<everyday-group-layout-expanded>` as the `iconPosition` prop. The expanded view now renders `[member-cols, group-row]` for `'bottom'` and `[group-row, member-cols]` for `'top'`, with mindmap Y constants flipped accordingly (`'top'`: `GROUP_DOT_Y=32`, `MEMBER_ICON_CENTER_Y=110` arms terminate at slider-tops; `'bottom'`: `GROUP_DOT_Y=304`, `MEMBER_ICON_CENTER_Y=202` arms terminate at tile-icons, original P5 semantic). Compact view also branches on `iconPosition`.
- 🎨 ui(P3): slider track shadow boosted. Stefan: "den soft outer drop-shadow sieht man gar nicht ... mach mal mehr". Layered close+far outer shadows (`0 3px 8px rgba(0,0,0,0.45)` + `0 10px 24px rgba(0,0,0,0.55)`) plus the existing top-inset shadow + thin bottom-inset highlight. Reads as floating-and-recessed instead of just recessed. Stefan: "hammer Kombination".
- 🔧 config: VERSION 0.4.5 → 0.4.6, resource `?v=0.4.6-iconpos`.

## 2026-05-08 - Compact polish round 2: layout, slider depth, popup behavior

- 🎨 ui(P5/P8): expanded view now nests sliders + tiles in per-column wrappers (`.member-col`) inside the topology, so the slider sits directly above its tile (Stefan-2026-05-08-evening: "die slider auf dem Kopf" feedback on the prior layout where sliders ended up at the very bottom of the card). Mindmap arms terminate at slider-tops (`MEMBER_ICON_CENTER_Y = 110`) instead of cutting through the slider region to reach tile-icons. Topology gap reduced to 30 px (was 80) to suit the shorter arm path. `dotsEnabled=false` re-applied on the mindmap-path so the SVG dots don't peek through at slider tops.
- 🎨 ui(P3): slider track box-shadow swap. Dropped the visible 2 px white-ish ring (Stefan: "scheiße ... border ... muss weg"). Replaced with a top-inset shadow + thin bottom-inset highlight + soft outer drop-shadow, so the pill reads as "pressed into the card" with depth instead of an outlined frame.
- 🎨 ui(P5): tile-icon shadow simplified to a soft drop-shadow only - no more visible ring on the icons either.
- 🎨 ui(P8): group-icon glyph stays identical across compact ↔ expanded transitions. Both views now render the same inline mdi:ceiling-light SVG (the expanded view previously used `<ha-icon>` which resolved to a subtly different glyph - read as "the icon changed" on expand).
- ✨ feat(P6/P7): popup-aware outside-click + group-tap. While a wheel/saved-colors popup is open in compact-expanded view, outside-click now ONLY closes the popup (compact-collapse path is skipped). Tapping the main group icon while a popup is open closes the popup instead of toggling the group on/off - turning the group icon into the "back to expanded view" gesture during popup interactions.
- 🎨 ui(P6): wheel popup wrapper is now transparent (no rectangular card frame around the wheel - Stefan-2026-05-08-evening: "es soll einfach nur der Farbkreis da sein mit einem starken schatten"). Strong drop-shadow lives on the wheel SVG itself for the floating-wheel look.
- ✨ feat(P3/P6): picking an RGB colour from the wheel or saved-colors auto-reverts the slider mode to brightness when it was previously on `'temperature'` - so the slider stops showing stale kelvin data after the bulb has been switched into colour mode.
- 🔧 config: VERSION 0.4.4 → 0.4.5, resource `?v=0.4.5-polish2`.

## 2026-05-08 - Compact polish: temp-auto-apply, shadows everywhere, group-icon anchor

- ✨ feat(P6): activating `'temperature'` slider mode (via picker or double-tap cycle) now auto-applies the previously-stored `color_temp_kelvin` to the light, so the bulb visibly switches back to its previous temp instead of staying in its current colour mode. Per-member kelvin snapshot maintained on every hass push (`_memberLastTemp`); fallback `3500 K` for first-time temp users. New helper `_setMemberMode(id, newMode)` centralizes the side-effect; both double-tap cycling and picker-temp-toggle route through it.
- ♻️ ui(P8): "long-press for expand" hint removed from compact view. Discoverability crutch from the first cut, no longer needed.
- 🎨 ui(P3/P5): box-shadow `0 4px 14px rgba(0,0,0,0.3)` + 2 px white-ish ring promoted to a default treatment. Now applies to every slider track (`.track` in `vertical-pill-slider`) AND every tile-icon (member + group, in compact + expanded views). Previously the lift-off treatment was only on the compact group-icon. Mindmap connector arms get a `filter: drop-shadow(0 3px 6px ...)` on the `<g class="arms">` group (drop-shadow on `<g>` renders once per group, cheaper than per-`<path>`).
- ✨ feat(P8): layout reversal — group-icon now sits at the TOP of both the compact and the compact-expanded views. Stefan-2026-05-08 wanted the "main icon" to stay anchored at the same on-screen Y when the user expands the card. Old layout had it at the bottom of both views, but the expanded view's added content above pushed the group-icon ~46 px lower on expand. New layout puts group-row first; below it the (compact) group slider OR the (expanded) member tiles + member sliders. Mindmap Y constants flipped: `GROUP_DOT_Y = 39` (top), `MEMBER_ICON_CENTER_Y = 184` (bottom). Trade-off: in expanded view, member sliders now sit BELOW their tiles (was above). Iterates if Stefan dislikes.
- 🔧 config: VERSION 0.4.3 → 0.4.4, resource `?v=0.4.4-polish`.

## 2026-05-08 - Compact-expanded view: outside-click collapse + member gestures rebound

- 🐛 fix(P8): in the expanded compact view, member-icons did nothing on tap/double-tap/long-press - only group-tile click (toggle) and group-tile long-press (collapse-back) worked. Root cause: `_rebindMemberGestures` only fires when `memberIds` content changes. The card mounts in compact-collapsed view (zero `.tile.member` elements in DOM); the initial-mount rebind binds against an empty `_memberTileEls` NodeList. When the user later expands the view, member tiles render, but `memberIds` hasn't changed so the rebind path doesn't re-run and the freshly-rendered tiles never get gesture handlers attached. Fix: in `updated()`, also call `_rebindMemberGestures()` whenever `_compactExpanded` toggles to `true`.
- 🐛 fix(P8): outside-click collapse missed clicks "neben die Mindmap arme" (clicks inside the card host but on the empty topology background between tiles/sliders/group-icon). The previous `!path.includes(this)` check only triggered for clicks completely outside the card host. Replaced with a positive interactive-region check: collapse when the click did NOT land on a `.tile`, `.picker-overlay`, `.inplace-popup`, or `<everyday-vertical-pill-slider>`. Also gated by the existing 250 ms popup-suppression window.
- 🐛 fix(P8): the long-press that expands compact view fires a phantom click on pointerup that would have immediately collapsed the just-expanded view. Set `_popupOpenedAt = Date.now()` together with `_compactExpanded = true` so the existing suppression window covers compact-expansion too.
- 🔧 config: VERSION 0.4.2 → 0.4.3, resource `?v=0.4.3-compact-fix`.

## 2026-05-08 - REAL real compact-icon root cause (P5-stub leftover)

- 🐛 fix(P5/P8): the actual reason compact view never rendered for Stefan. The card had a P5-era short-circuit:
  ```ts
  const resolved = layoutMode === 'compact' ? null : resolveGroup(...);
  ```
  Originally written when `'compact'` was a stub keyword that "forced single-light view". I added the P8 compact implementation later (compact-prop on group-layout-expanded) but forgot to remove this guard - so `group.layout: compact` set resolved=null, which fell through to single-light render entirely. The compact prop was wired but never reached because group-layout-expanded was never rendered for compact users.
  - Stefan's tell: "unter dem Namen 'on'" - that's the single-light caption (`<span class="state">${stateObj.state}</span>`), confirming the wrong branch was rendering.
  - Fix: drop the layoutMode short-circuit. Always run resolveGroup. compact prop carries the layout switch into group-layout-expanded.
- 🔧 config: VERSION 0.4.1 → 0.4.2, resource `?v=0.4.2-grp-fix2`.

## 2026-05-08 - REAL compact-icon root cause + auto-revert slider mode

- 🐛 fix(P5): `light: platform: group` entities (e.g. `light.hall_spots`) were never being detected as groups. HA exposes the member list under `group_entities` for that integration, but my `getMembersFromState` only read `entity_id` (which is what Hue rooms use). Fix: accept either attribute.
  - **Side effect:** Stefan's "compact view shows no group icon" was actually the card falling back to the single-light view all along. The compact branch was never reached. With this fix, compact view actually renders for `light.hall_spots` and the new inline-SVG group icon (shipped last commit) becomes visible.
- ✨ feat: auto-revert slider mode to brightness on light-off (Stefan-default 2026-05-08). When a member's light transitions on→off, its transient slider mode (temperature/hue/saturation) resets to brightness, so the next on-tap shows the brightness slider. `slider.persistent_mode: true` opts out and keeps the cycled mode across off → on transitions.
- 🔧 config: VERSION 0.4.0 → 0.4.1, resource `?v=0.4.1-grp-fix`.

## 2026-05-08 - cycling double-tap + compact icon (inline SVG)

- ✨ feat: vertical-pill-slider supports `'hue'` and `'saturation'` modes in addition to `'brightness'` and `'temperature'`. Hue track shows a vertical rainbow gradient; saturation track fades from gray to the current hue at 100% saturation. Service calls drive `light.turn_on hs_color: [...]` preserving the orthogonal channel.
- ✨ feat: double-tap cycles slider mode based on the light's `color_mode`:
  - `color_temp` → `brightness ↔ temperature`
  - `hs` / `xy` / `rgb` / `rgbw` / `rgbww` → `brightness → hue → saturation → brightness`
- 🐛 fix: P8 compact view group icon now renders reliably. Replaced `<ha-icon>` with an inline SVG (`mdi:ceiling-light` path) - ha-icon kept failing in this branch for unknown reasons, the inline path is guaranteed-visible. Explicit `.ic` dimensions added so the 64×64 ring+icon container is always laid out.
- 🔧 config: VERSION 0.3.1 → 0.4.0 (slider-mode set expansion is API-relevant; treating it as a minor bump). Resource `?v=0.4.0-cycle-icons`.

## 2026-05-08 - P8 polish: popup-over-dot + double-tap + persistence + compact-fix

- 🐛 fix(P8): compact view group-icon was unreliable. Switched the gesture-target marker from a bare `data-compact-group` attribute to a class `compact-target`. Added a "long-press for expand" hint label so the interaction is discoverable. Bigger compact slider (260 px tall, matches design-mock `.sp-slider.sm.tall`). Subtle ring around the compact group icon to make it unmistakable.
- ✨ feat(P6/P7): popups now center over the **picker dot** (color-wheel option for the wheel popup; saved-colors option for the saved popup) instead of over the member icon. Stefan: "Mitte vom color-wheel über dem color-wheel icon". Computed via the existing `pickerHoverFromPointer` math at orbit-r 64 from the captured icon origin.
- ✨ feat(P6): `color_wheel.persistent: boolean` config (default `true`). Persistent mode keeps the popup open after a color is picked - user must click outside to dismiss. Single-pick mode (`persistent: false`) auto-closes after the first pick.
- ✨ feat(P7): same `saved_colors.persistent` for parity. Default true.
- ✨ feat(P5): double-tap on a member icon toggles the slider mode brightness ↔ temperature. (Stefan 2026-05-08: "wenn man doppelt tippt soll je nach wunsch ..."). The HSB / multi-slider variant is filed as P6.3.
- 🎨 fix: removed the popup titles. They were duplicating the member tile label below; default-hide per Stefan.
- 📚 docs: PHASE-PLAN P7.2 entry for circular-swatch-layout + drag-to-reorder (Stefan said "later").
- 🔧 config: VERSION 0.3.0 → 0.3.1, resource `?v=0.3.1-p8-polish`.

## 2026-05-08 - P8 done (MVP): compact-mode group expansion

- ✨ feat(P8): `group.layout: 'compact'` triggers a single-slider + single-icon compact view of the group. Long-press on the group icon expands in-place to the full N-slider expanded view (existing P5 layout). Outside-click collapses back. CSS-transition-driven animation; the more elaborate inline-fan-out animation (Mode B per CONCEPT A4) is deferred to P8.1.
- 🔧 group-layout-expanded: new `compact` prop + `_compactExpanded` state + `_bindCompactGestures` lifecycle (binds long-press only when compact-collapsed; tears down on expansion). Outside-click listener now also collapses compact view.
- 🔧 config: VERSION 0.2.2 → 0.3.0 (third minor — feature-complete demo path through P8), bundle 67 → 70 KB, resource `?v=0.3.0-p8`.
- 📚 docs: PHASE-PLAN P8 row ticked done with the deferred-animation note.

## 2026-05-08 - P7 stability: phantom-click + memberIds-content rebind

- 🐛 fix(P7): wheel + saved-colors popups dismissed instantly. Root cause: the click event the browser dispatches from the press-drag-select pointerup landed in the document outside-click listener immediately AFTER the popup opened. Path didn't include `.inplace-popup` (the popup just rendered, click was on the picker option) → listener correctly identified it as "outside" and closed the popup. Fix: 250 ms suppression window after popup-open; the listener ignores clicks within that window.
- 🐛 fix(P6): "after 3-5 long-presses the picker stops responding" bug. The earlier fix only blocked `hass`-trigger rebinds, but `resolveGroup()` returns a new `memberIds` array reference on every render, and lit's default `hasChanged: !==` flagged that as changed → rebind on every hass push. Fixed with content-based key tracking (`memberIds.join(',')`); rebind only when the actual member set changes.
- 🔧 config: VERSION 0.2.1 → 0.2.2, resource `?v=0.2.2-p7-stability`.

## 2026-05-08 - P7 polish: in-place popups + config-driven + tap-toggle + smooth wheel

- ✨ feat(P6+P7): color-wheel and saved-colors popups now expand **in-place** above the active member icon (instead of full-screen backdrop). `position: fixed` with computed top/left from `getBoundingClientRect` so the popup escapes any ancestor `overflow: hidden`. Bloom animation (scale 0.4 → 1) from icon-top.
- ✨ feat(P6): `gestures.long_press_ms` config (default `200`, was `500`). Stefan's tuning at the demo review - quick hold opens the picker much faster.
- ✨ feat(P7): smooth color-wheel variant. `color_wheel.type: smooth` enables a continuous conic+radial gradient with click-to-pick polar-coord-mapped HSV. White center bias on inner 8 % radius. `'stepped'` (default) and `'smooth'` both supported via the existing config schema.
- ✨ feat(P7): `slider.width` / `slider.height` config props - override the embedded slider dimensions (CSS-var pass-through). Defaults: 47×170 inside a group view, 60×220 single-light.
- 🐛 fix(P7): saved-colors UX per Stefan-ADR. Removed the ✓ button. Tapping `+` now saves the current rgb AND auto-exits edit-mode (single-add-per-session). Re-enter edit-mode via long-press on a swatch.
- ✨ feat(P5): tap on a member icon now toggles the light (default `gestures.member_icon.tap: 'toggle'`; was `'none'` per CONCEPT.md R1, Stefan-override 2026-05-08). Drag still controls brightness; long-press still opens the picker.
- 📚 docs: PHASE-PLAN P7.1 entry clarifies what "Persistence" means (Stefan asked).
- 🔧 config: VERSION 0.2.0 → 0.2.1, resource `?v=0.2.1-p7-polish`.

## 2026-05-08 - P7 done: saved-colors picker (Tier 1+2)

- ✨ feat(P7): `src/components/saved-colors-picker.ts` — 2×4 swatch grid (HA-Standard layout per Stefan's PHASE-1-COMMENTS feedback). Tap a swatch → applies that rgb. Long-press a swatch → enters edit-mode. Edit-mode adds a `−` button on each swatch (top-right), a dashed `+` cell at the end (saves the active light's current rgb), and a `✓` button to exit. Wiggle animation with stagger so adjacent swatches don't oscillate in lock-step.
- ✨ feat(P7): mode-picker "Saved" option now opens this picker instead of showing the P6 stub toast. Wired through `_savedColorsTarget` state in group-layout-expanded; popup uses the same fixed-position backdrop pattern as the color-wheel.
- ✨ feat(P7): default 8-color palette (warm orange, warm white, cool white, red, amber, green, blue, purple). User-managed entries stored in component state. Persistence to a helper / HA favourites = P7.1 follow-up.
- ✨ feat(P7): "save current rgb" guards against duplicates (within 5/255 per channel) and against off-state lights (warns via toast).
- 🔧 config: VERSION 0.1.3 → 0.2.0 (second minor — saved-colors milestone), bundle 57 → 68 KB, resource `?v=0.2.0-p7`.
- 📚 docs: PHASE-PLAN P7 row ticked done.

## 2026-05-08 - P6 bugfix: rebind no longer fires on hass updates

- 🐛 fix(P6): intermittent "press-drag-select doesn't take after 2-3 tries" bug. `_rebindMemberGestures` was triggered on every `hass` property change, which happens regularly when HA pushes any state update. A rebind mid-gesture tore down the active gesture detector, dropping subsequent pointermove / pointerup. Now triggers only when `memberIds` actually changes (members added/removed). Bound handlers reference `id` from a closure - they keep working across hass updates without rebind.
- 🔧 config: VERSION 0.1.2 → 0.1.3, resource `?v=0.1.3-p6-bugfix`.

## 2026-05-08 - P6 polish: orbit + circle thumb + icon-swap + group-y 183

- ✨ feat(P6): mode-picker `ORBIT_R 44 → 64`. Press-drag-select targets are no longer clustered tightly around the icon - much easier to land on with a finger drag. `pickerHoverFromPointer` outer-radius bumped 120 → 160 to keep the hit zone reachable.
- ✨ feat(P6): mode-picker icon-swap. New `currentMode` prop on everyday-mode-picker. When the slider is in temperature mode, the Temp picker option renders a sun/brightness glyph instead of a thermometer - communicating "tap to return to brightness". The toggle action itself was already implemented; now the visual matches.
- 🎨 fix(P3): temp thumb is now a proper circle (full pill width AND height). Was rendering as a horizontal oval after the previous "full-width" change.
- 🐛 fix(P5): GROUP_DOT_Y 188 → 183 (Stefan-final after one more round of bisecting).
- 📚 docs: PHASE-PLAN now lists P6.4 (swipe-navigation hard-swipe conflict) as a future enhancement. Stefan flagged it as "save for later" - capture-phase pointerdown listener or e.preventDefault() are likely paths.
- 🔧 config: VERSION 0.1.1 → 0.1.2, resource `?v=0.1.2-p6-polish`.

## 2026-05-08 - P6 tweaks: press-drag-select + temp flip + thumb + group-y

- ✨ feat(P6): press-drag-select gesture. Long-press on a member icon and KEEP your finger down → drag to one of the picker dots → release. Selection commits on release, no second tap needed. The 2-step flow (long-press → release → tap dot) still works as fallback if the user releases without dragging.
  - `gesture-detector.ts`: new `onLongPressMove(ev)` and `onLongPressEnd(ev)` callbacks. `setPointerCapture` on long-press start so pointermoves keep flowing even when the finger leaves the original tile (over the orbiting picker dots). `stopPropagation()` on every pointermove during long-press → blocks HA's swipe-navigation plugin from hijacking the gesture mid-drag.
  - `group-layout-expanded`: computes the picker origin from the icon's getBoundingClientRect on long-press start, then maps each pointermove to a polar coord vs that origin, sets `_pickerHover` accordingly. On end commits the selection.
- 🐛 fix(P3): temperature slider direction inverted to match the kelvin gradient. Top of slider (fraction=1) = warm = low K. Bottom (fraction=0) = cool = high K. Stefan: "aktuell ist unten warm und oben kalt".
- ✨ feat(P3): temperature thumb is now a full-width horizontal pill. Brightness mode keeps the round 80%-width thumb. The temp pill spans the slider visually as a kelvin-tape indicator.
- 🐛 fix(P5): group dot Y 184 → 188 (Stefan-final, after 176 → 192 → 184 bisect).
- 📚 docs: PHASE-PLAN now lists P6.2 (smooth color-wheel) and P6.3 (HSB 5th picker mode) as future enhancements per Stefan-2026-05-08-request.
- 🔧 config: VERSION 0.1.0 → 0.1.1, resource `?v=0.1.1-p6-tweaks`.

## 2026-05-08 - P6 done: mode-picker + color-wheel (DEMO BLOCKER cleared)

- ✨ feat(P6): `src/helpers/gesture-detector.ts` - functional pointer-event helper. Detects long-press (500 ms default), tap (within 300 ms / 8 px), double-tap (300 ms window). Cancel-on-drag built in. Returns a `dispose()` for clean teardown.
- ✨ feat(P6): `src/components/mode-picker.ts` - 3-circle in-place expansion (Temp / Wheel / Saved) in equilateral triangle (point-down). Geometry verbatim from `assets/design-mocks/mode-picker-inplace.html`. Animated bloom-out via CSS `@keyframes bloom` with 40 ms stagger between dots.
- ✨ feat(P6): `src/components/color-wheel.ts` - stepped variant SVG wheel, 12 hues × 4 rings default + white center. HSV→RGB per-segment. Click on a segment fires `color-pick` event with `{r, g, b}`. Smooth variant deferred to P6.1 (would use conic-gradient + polar-coord-mapping).
- ✨ feat(P6): group-layout-expanded wires it all together: long-press on member-icon expands the picker; tap Temp toggles that member's slider mode brightness↔temperature; tap Wheel opens the color-wheel popup (fixed-position backdrop, click-outside / Close button to dismiss); tap Saved → toast "arrives in Phase 7".
- ✨ feat(P6): per-member transient mode state (`_memberModes` map). Mode is NOT persisted to config - it's a tap-to-flip override. Defaults to `brightness`.
- ✨ feat(P6): document-level outside-click listener with `composedPath()` shadow-DOM membership check (CONCEPT.md Risk Eng-3 mitigation) closes picker on click-outside.
- 🔧 config: VERSION 0.0.12 → 0.1.0 (first minor bump - reflects demo-readiness milestone), bundle 41 → 57 KB, resource `?v=0.1.0-p6`.
- 📚 docs: PHASE-PLAN P6 row ticked done.

## 2026-05-08 - P5 tweaks: dash spacing + group dot fine-tune

- 🎨 fix(P5): wider gap in unavailable-arm dasharray. `3 3` (looked like tight stitching) → `3 8` (clearly intermittent). Stefan: "abstände zwischen den Strichen etwas größer".
- 🎨 fix(P5): group dot Y moved 192 → 184 (8 px below icon-center instead of 16). 176 was "zu hoch", 192 was "zu niedrig" → 184 is the tuned middle.
- 🔧 config: VERSION 0.0.11 → 0.0.12, resource `?v=0.0.12-p5-dash-y`.

## 2026-05-08 - P5 tweaks: gap-aware dot X + lower group dot + bigger radii

- 🐛 fix(P5): outer member-dots now align with the outer HTML tile centers. The previous `(i+0.5)/n × W` formula ignored the `.tiles gap: 18px`, clustering outer dots inward by `gap/3 ≈ 6 px` per side. New `tile-gap` attribute on mindmap-path drives a gap-aware formula `colWidth/2 + i × (colWidth + gap)`. group-layout-expanded passes `tileGap=18` to match its CSS.
- ✨ feat(P5): group dot positioned at y=192 instead of icon-center y=176 so the (now larger) dot peeks out a few px below the group icon - Stefan's "der große ist ein bisschen zu hoch" feedback.
- 🔧 config: `DEFAULT_MEMBER_R: 12 → 17`, `DEFAULT_GROUP_R: 16 → 23` per Stefan-explicit-spec.
- 🔧 config: VERSION 0.0.10 → 0.0.11, resource cache-busted to `?v=0.0.11-p5-tweaks`.

## 2026-05-08 - P5 polish: dots stacked under HTML icons

- ✨ feat(P5): mindmap SVG dots now sit **directly under** the HTML icon circles. New layout structure: `.topology` flex stack containing the SVG (absolute, z-index 0) + `.tiles` (z-index 2) + `.group-row` (z-index 2). The SVG's responsive viewBox is set so member-dots at y=24 (member-icon center) and group-dot at y=176 (group-icon center) land exactly under their HTML counterparts. Curves now visually emerge from under the icons rather than from a separate strip beneath them.
- 🔧 config: removed the `.mindmap-container` row from the flex flow; flex `gap: 80px` between `.tiles` and `.group-row` is the new curve-traversal area.
- 🔧 config: VERSION 0.0.9 → 0.0.10, resource cache-busted to `?v=0.0.10-p5-stack`.

## 2026-05-08 - P5 polish: re-enable mindmap dots (now circular)

- ✨ feat(P5): mindmap dots re-enabled in group-layout-expanded. With the responsive viewBox in place from the previous fix, SVG `<circle>` elements render as actual circles (24 px diameter) at curve endpoints — no more aspect-distortion ovals. The dots act as visible anchors between the HTML icon-tile rows above/below and the connecting curves.
- 🔧 config: VERSION 0.0.8 → 0.0.9, resource cache-busted to `?v=0.0.9-p5-dots`.

## 2026-05-08 - P5 fix: responsive viewBox kills the SVG distortion

- 🐛 fix(P5): SVG dots were being rendered (and stretched into ovals) in the card view. Two compounding bugs:
  - `?dots-enabled=${false}` is wrong - `?attr=${false}` only removes the attribute. Property default stays `true`. Switched to `.dotsEnabled=${false}` (property binding, unambiguous).
  - `preserveAspectRatio="none"` distorted SVG circles into ovals AND warped the curves. Removed the `stretch=true` / `none` path entirely.
- ✨ feat(P5): mindmap-path is now responsive by default. ResizeObserver tracks the host's actual rendered size; the SVG viewBox is set to `0 0 W H` in CSS pixels, so `preserveAspectRatio` is irrelevant - viewBox aspect always matches container, no scaling distortion. Geometry (group/member positions, Bézier control points) computed in pixel space and adapts smoothly to whatever container the host lives in.
- 🔧 config: `responsive=false` opt-out for callers that want pixel-exact reproducibility against `view-width`/`view-height` defaults (e.g. golden-image tests). `stretch` prop kept as deprecated no-op.
- 🔧 config: VERSION 0.0.7 → 0.0.8, resource cache-busted to `?v=0.0.8-p5-resize`.

## 2026-05-08 - P5 fix: mindmap alignment + isolated test lights

- 🐛 fix(P5): mindmap arms now land **directly under each member tile**. Two changes:
  - `mindmap-path._memberXs()` uses tile-grid-aligned formula `(i + 0.5) / n × viewWidth` instead of the margin-based even-spacing. Margin is preserved as legacy opt-in for callers that explicitly set it.
  - `mindmap-path` accepts a `stretch` boolean attribute. When set, the SVG uses `preserveAspectRatio="none"` so the X axis stretches to fill its container 1:1 instead of being centered with letterbox bars (the bug that clustered the arms in the middle).
  - `group-layout-expanded` passes `stretch=${true}` and `dots-enabled=${false}`.
- 🧪 test(P5): isolated test-light infrastructure on hassio so Stefan can verify group toggle without touching real Hue lamps:
  - `test-fixtures/everyday_test_lights.yaml` (repo mirror) → `/config/packages/everyday_test_lights.yaml` on hassio
  - 3 input_boolean + 3 input_number + 3 input_text helpers backing 3 template lights `light.hall_spot_{1,2,3}` (brightness + rgb)
  - 1 `light: platform: group` named `light.hall_spots`
  - `homeassistant.packages` repointed from typo'd `resources/matt8070/packages` (which never existed) to `packages`. Old typo'd line was a no-op since the dir didn't exist.
  - HA core restart triggered; entities verified via REST.
- 🔧 config: VERSION 0.0.6 → 0.0.7, resource `?v=0.0.7-p5-align`.

## 2026-05-08 - P5 done: group-layout-expanded + group-toggle-restore

- ✨ feat(P5): `src/components/group-layout-expanded.ts` - lit element rendering N sliders + N member-tiles + mindmap-path + group-tile per the `assets/design-mocks/group-layout-expanded-n3.html` reference. Slider-Sizing override `--everyday-light-card-width: 47px / -height: 170px` matching the mock.
- ✨ feat(P5): `src/helpers/group-detection.ts` - `resolveGroup(hass, entityId, manualMembers?)`. Three resolution paths: (1) manual override via `group.manual_members`, (2) entity is itself a HA light group (has `entity_id` array attribute), (3) entity is a member - linear scan of `light.*` states finds the parent group. Returns null for single lights → card falls back to P3 single-slider view.
- ✨ feat(P5): `src/helpers/group-toggle.ts` - `groupToggleWithRestore` (CONCEPT R3). On→off triggers `scene.create scene_id: everyday_group_<sanitized>, snapshot_entities: [members]`, then `light.turn_off entity: group`. Off→on tries `scene.turn_on scene.everyday_group_<id>` first, falls back to plain `light.turn_on` if scene doesn't exist (first-ever toggle).
- ✨ feat(P5): card auto-detects group membership and renders the expanded layout when ≥2 members are found. Stefan-Override via `group.layout: 'compact'` forces the single-light view (compact mode itself is P8). `group.manual_members` override available.
- 🔧 config: `getCardSize()` returns 6 for groups, 4 for single lights (correct stack-card height reservation).
- 🔧 config: VERSION 0.0.5 → 0.0.6, bundle 28 → 40 KB, resource `?v=0.0.6-p5`.
- 📚 docs: PHASE-PLAN P5 row ticked done.

## 2026-05-08 - P4 bugfix: N=1 stem + test-page serving notes

- 🐛 fix(P4): N=1 now renders a straight stem from the group node up to the single member (the Bézier formula degenerates to a vertical line when `memberX === groupX`, which is exactly what we want). Previously the N=1 cell rendered only the two dots with nothing between them.
- 📚 docs: `assets/test/README.md` explains why `file://` doesn't work and how to start a local HTTP server. Mirrors the in-page warning.
- 🧪 test: `assets/test/mindmap-test.html` shows a yellow info box if opened via `file://` so the CORS cause is obvious without browser-DevTools spelunking.
- 🔧 config: VERSION 0.0.4 → 0.0.5, resource cache-busted to `?v=0.0.5-p4-n1-fix`.

## 2026-05-08 - P4 done: mindmap-path SVG component

- ✨ feat(P4): `src/components/mindmap-path.ts` - lit-element pure SVG component, state-reactive per-arm stroke (rgb_color + brightness/255 opacity), cubic-Bézier "Triton-S-curve" geometry verbatim from `assets/design-mocks/mindmap-geometries-n2-n8.html` (Stefan's canonical N=3 design).
- ✨ feat(P4): comb fallback at N≥6. Each member drops vertically to a horizontal connector, single trunk to group node. Trunk colored by average rgb of on-members + max brightness (CONCEPT.md R2). Reduces the "wirr"-ness Stefan flagged in PHASE-1-COMMENTS for high N.
- ✨ feat(P4): off / unavailable members keep a faint guide rendering so the spatial structure stays visible. Unavailable arms get a dasharray.
- 🧪 test(P4): `assets/test/mindmap-test.html` standalone test page. Loads production bundle as ES module, renders 3 scenarios (all-warm-on, mixed-on/off, all-off) × N=1..8. Open in browser to eyeball the geometry without HA.
- 🔧 config: side-effect import `./components/mindmap-path.js` from card entry → component registered as customElement when bundle loads. Card itself doesn't use it yet (P5 wires it into group-layout-expanded).
- 🔧 config: VERSION 0.0.3 → 0.0.4, bundle 28 868 → 33 509 B, resource cache-busted to `?v=0.0.4-p4`.
- 📚 docs: PHASE-PLAN P4 row ticked done.

## 2026-05-08 - P3 bugfix: visual cap + click-jitter

- 🐛 fix(P3): `.fill` now has top-rounded corners (`border-radius: pill-half pill-half 0 0`) so the colored cap peeks above the thumb - that was the "missing element above thumb" Stefan saw on screenshot.
- 🐛 fix(P3): geometry remapped to match my-slider-v2 PoC: `fill_height = pill-width + fraction × range`, `thumb_bottom = pill-half - thumb-size/2 + fraction × range`. Fill is no longer too short at low brightness; thumb is correctly placed below the cap.
- 🐛 fix(P3): click-jitter ("thumb springt zum click → zurück → zum click"). Root cause was `_commitNow` clearing `_dragValue` synchronously on pointerup, so lit re-rendered with the OLD hass state during the 50-200 ms before HA's update arrived. Fix: keep `_dragValue` until `updated()` confirms hass matches `_committedValue` (within 0.02 tolerance) - or 1.5 s fallback timeout.
- 🐛 fix(P3): pointer capture moved from `ev.target` (inner shadow-DOM nodes that re-render mid-drag) to the LitElement host. Pointer stream survives any inner re-rendering. Listeners attached in `connectedCallback` so they fire on the captured target.
- 🐛 fix(P3): cursor → thumb mapping is now 1:1 inside the visual range (`[halfWidth, pillHeight - halfWidth]`). Tap on the very bottom = thumb at min, tap on the very top = thumb at max, tap in the middle = thumb under finger.
- ✨ feat(P3): `.dragging` class kills CSS transitions during an active drag so the thumb tracks the finger without lag.
- 🔧 config: VERSION bumped 0.0.2 → 0.0.3, resource cache-busted to `?v=0.0.3-p3-bugfix`.

## 2026-05-08 - P3 done: vertical-pill-slider live

- ✨ feat(P3): `src/components/vertical-pill-slider.ts` - lit element, brightness + temperature modes, optimistic drag with 50ms debounce, ported visual baseline from my-slider-v2 PoC (rgb-fill from bottom, white thumb with %-label / kelvin-gradient + colored thumb with mireds-label)
- ✨ feat(P3): card now renders the slider for any `light.*` entity instead of the Phase-2 stub text. `mode: temperature` switches the rendering. Non-`light.*` entities show a clear error.
- 🔧 config: VERSION bumped 0.0.1 → 0.0.2 (visible in console banner)
- 🚀 deploy: bundle (26 531 B minified) scp'd to hassio, Lovelace resource cache-bumped to `?v=0.0.2-p3` (resource_id unchanged, just URL refreshed)
- 📚 docs: PHASE-PLAN P3 row ticked done

## 2026-05-08 - P2 done: scaffold-build + HA-deploy live

- ✨ feat(P2): `npm install` + `npm run build` produce `dist/everyday-light-card.js` (19366 B, terser-minified, single chunk)
- ✨ feat(P2): editor stub `src/editor/everyday-light-card-editor.ts` so the dynamic import in `getConfigElement()` resolves at build- and runtime (full editor is Phase 9 / R8)
- 🔧 config: `rollup.config.mjs` - `inlineDynamicImports: true` (single-file HACS bundle) + `sourcemap: true` (always emit, TS plugin requires it)
- 🚀 deploy: `dist/everyday-light-card.js` scp'd to `hassio:/config/www/` (alias resolves to HA host:23)
- 🔧 config: HA Lovelace resource registered - `/local/everyday-light-card.js?v=0.0.1`, type `module`, resource_id `ff1c8fcbccb04acbb85ace77296b377f` (resource count 28→29)
- 📚 docs: PHASE-PLAN P2 row ticked done

## 2026-05-08 - P1 done + design-mocks migrated

- 🎨 assets: 13 design-mocks von Stefan migriert (`assets/design-mocks/`) - HTML + CSS-tokens + JSX-tweaks-panel + 7-dot-Icon
- 📚 docs: PHASE-1-COMMENTS.md mit Stefan-Bewertung pro Mock (3er-Mindmap brauchbar, Saved-Colors-Mocks bescheiden, Rest "weitermachen")
- 🔧 config: PHASE-PLAN.md updated - P0+P1 als done markiert, P4+P7 haben Iterations-Vorbehalte für ihre eigenen Phasen
- 🔧 config: GitHub repos `f17mkx/everyday-light-card-dev` (privat, gepushed) + `f17mkx/everyday-light-card` (public, leer)
- 🔧 config: Conductor-DB-INSERT für repo-ID `c8dc3933-9344-4360-a05a-71e83d552981` (storage_version=3, display_order=8)

## 2026-05-07 - P0 bootstrap

- ✨ feat: repo initialized, lit + rollup + TypeScript scaffold
- ✨ feat: Phase-2 stub `src/everyday-light-card.ts` registers element + getStubConfig
- ✨ feat: full config schema in `src/types/config.ts` with Stefan A1-A9 decisions
- 📚 docs: CONCEPT.md, PHASE-PLAN.md, CLAUDE-DESIGN-PROMPTS.md, RELEASE-WORKFLOW.md, HANDOFF.md
- 🔧 config: hacs.json + manifest.json + package.json + tsconfig.json + rollup.config.mjs + .gitignore + .releaseignore
- 🎨 assets: 7-dot-hexagon icon (R9), Mindmap-N{2,3,4,5,6,8} starter SVGs (Stefan-editable in Inkscape)
- 🔨 scripts: sync-to-public.sh (rsync-based 2-repo sync)
